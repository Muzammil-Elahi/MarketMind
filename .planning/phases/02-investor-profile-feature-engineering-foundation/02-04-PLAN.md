---
phase: 02-investor-profile-feature-engineering-foundation
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/pages/profile.py
  - src/app.py
autonomous: true
requirements: [PROFILE-01, PROFILE-02]
must_haves:
  truths:
    - "Scalar-fields form (first-time user, no profile row exists) renders with unselected/default values (empty selects, empty multiselects, capital defaulting to 0) rather than a separate 'no profile yet' placeholder screen — the page heading/subheading copy covers both first-visit and edit contexts identically (D-12)."
    - "Holdings grid with zero rows renders the empty-state copy 'No holdings added yet. Add a ticker below to get started.' above the st.data_editor."
    - "Profile save failure (e.g. a Supabase write error) renders the exact error copy 'We couldn't save your profile. Please try again.'"
    - "Holdings grid invalid-ticker submit (D-08) renders the exact error copy 'We couldn't recognize \"{ticker}\" — check the symbol and try again.' plus a red-border highlight, reusing login.py's _highlight_empty_fields-style CSS-injection technique — scoped to the holdings data_editor widget as a whole (via its own key=\"holdings_editor\"), since st.data_editor exposes no per-cell/per-row CSS key the way individual st.text_input widgets do; this is a deliberate, capability-driven adaptation of the UI-SPEC's per-row intent, documented here rather than silently narrowed."
    - "Holdings grid renders as a standard editable table at any populated row count via st.data_editor with no custom layout beyond the native widget."
    - "Scalar-fields form allows partial completion (some fields filled, others left blank) and saves successfully with no required-field warning, since all six new profiles columns are nullable (RESEARCH.md Pattern 2)."
    - "Holdings grid rows with cost-basis left blank save successfully (D-07's optional cost-basis field) via st.data_editor's native handling of an empty/blank numeric cell — no custom empty-cell styling needed."
    - "Holdings grid at many rows relies on st.data_editor's native fixed-height internal scroll — no custom overflow handling required."
    - "The 'Existing Holdings' section heading and grid column headers are count-agnostic across 0/1/many rows — no singular/plural copy variants needed."
    - "Scalar-fields form has no free-text inputs (all st.selectbox/st.multiselect/st.checkbox/st.number_input), so long-text overflow does not apply to this element."
    - { statement: "Scalar-fields form's profile fetch on page load (D-13, uncached) is expected to be near-instant with no custom skeleton/spinner — native Streamlit rerun behavior applies. Verified at implementation/visual-QA time, not by a wired assertion this phase.", verification: backstop }
    - { statement: "Holdings grid's holdings fetch on page load (D-13, uncached) is expected to be near-instant with no custom skeleton/spinner — native Streamlit rerun behavior applies. Verified at implementation/visual-QA time, not by a wired assertion this phase.", verification: backstop }
    - { statement: "Holdings grid ticker cell with an unusually long or malformed entry is handled by st.data_editor's native fixed-width text-cell behavior with no custom wrapping — not specified further, verified at implementation/visual-QA time. Malformed/invalid tickers are separately caught by the D-08 submit-time validation regardless of display length.", verification: backstop }
    - "The Investor Profile page is entirely absent from st.navigation for a logged-out user — only reachable after require_auth() succeeds, matching D-03's established conditional-nav pattern."
  artifacts:
    - path: "src/pages/profile.py"
      provides: "render_profile_page() — the require_auth()-gated investor profile builder: scalar fields + dynamic holdings grid + D-08 ticker validation + save"
  key_links:
    - from: "src/app.py"
      to: "src/pages/profile.py"
      via: "st.navigation({\"Home\": [...], ...}) includes profile_page only in the logged-in branch"
      pattern: "render_profile_page"
    - from: "src/pages/profile.py"
      to: "src/data/profile.py"
      via: "fetch_profile/upsert_profile/fetch_holdings/upsert_holdings/validate_ticker calls, passing st.session_state['access_token'] and user.id explicitly"
      pattern: "from src\\.data\\.profile import"
    - from: "src/pages/profile.py"
      to: "src/auth/session.py"
      via: "require_auth() called as the first statement in render_profile_page(), no inline auth logic"
      pattern: "require_auth\\(\\)"
  prohibitions:
    - "MUST NOT render any personalized investment-advice or directive-language text (e.g. recommending a user buy/sell/hold a specific asset) anywhere on this page based on the captured profile fields — this page captures investor preferences only, it does not recommend; the full compliance audit (no-directive-language, disclaimers) is Phase 6's job (COMPLY-01/COMPLY-02), this is a preventive scope guard for this phase only."
    - "MUST NOT surface any src/features/ output (technical indicators, returns, volatility) on this page — src/pages/profile.py MUST NOT import from src.features (Plan 02-02's prohibition, cross-referenced here since this is the phase's only UI surface where that could accidentally leak in)."
---

<objective>
Build the investor profile builder page — a single always-editable Streamlit form (D-12) covering risk tolerance, time horizon, preferred/excluded sectors, preferred asset types, and capital, plus a dynamic holdings grid (ticker/quantity/optional cost basis) with D-08 submit-time ticker validation — and register it in `src/app.py`'s auth-gated navigation.

Purpose: this is the actual user-facing delivery of PROFILE-01/PROFILE-02 — everything in Plans 02-01 (schema) and 02-03 (CRUD) exists to support this page. Note: the deterministic edge-coverage probe classified PROFILE-01 as `unclassified` (a UI data-entry requirement, not a data-shape/algorithmic one it recognizes) — flagged for manual review rather than silently dropped; this plan's `must_haves` below (lifted verbatim from `02-UI-SPEC.md`'s UI Considerations table plus `02-CONTEXT.md`'s D-01 through D-13) are PROFILE-01's actual resolved acceptance criteria in lieu of an auto-generated probe judgment.

This phase runs with `workflow.human_verify_mode: end-of-phase` — no mid-phase blocking checkpoint is used for this page; Task 2's `<verify>` includes a `<human-check>` block documenting the manual walkthrough for the end-of-phase UAT pass instead.

Output: `src/pages/profile.py` (`render_profile_page()`), `src/app.py` updated to register it in the logged-in navigation branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-investor-profile-feature-engineering-foundation/02-CONTEXT.md
@.planning/phases/02-investor-profile-feature-engineering-foundation/02-RESEARCH.md
@.planning/phases/02-investor-profile-feature-engineering-foundation/02-UI-SPEC.md
@.planning/phases/02-investor-profile-feature-engineering-foundation/02-03-SUMMARY.md
</context>

<interfaces>
From `src/data/profile.py` (Plan 02-03, already on disk): `fetch_profile(access_token, user_id) -> dict | None`, `upsert_profile(access_token, user_id, *, risk_tolerance=None, time_horizon=None, preferred_sectors=None, excluded_sectors=None, preferred_asset_types=None, capital=None) -> None`, `fetch_holdings(access_token, user_id) -> list[dict]`, `upsert_holdings(access_token, user_id, rows: list[dict]) -> None`, `validate_ticker(ticker: str) -> bool`.

From `src/auth/session.py` (Phase 1, already on disk): `require_auth() -> User | None` — call first, use `.id` for `user_id` and `st.session_state["access_token"]` for the token.

From `02-UI-SPEC.md` Copywriting Contract (exact strings, reuse verbatim): Primary CTA "Save Profile" · Page heading "Investor Profile" · Page subheading "Tell us how you invest so we can tailor your recommendations. You can update this anytime." · Holdings heading "Existing Holdings" · Empty state "No holdings added yet. Add a ticker below to get started." · Invalid-ticker error "We couldn't recognize \"{ticker}\" — check the symbol and try again." · Save failure "We couldn't save your profile. Please try again." · Save success "Profile saved."
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Investor profile page — scalar form + holdings grid + validation + save</name>
  <files>src/pages/profile.py</files>
  <read_first>
    src/pages/login.py (full file — `st.form`/`st.form_submit_button` pattern, `_highlight_empty_fields` CSS-injection technique and `FIELD_ERROR_BORDER_COLOR`, module-level copy-constants pattern — all to be replicated, not reinvented)
    src/pages/home.py (auth-gate pattern — `require_auth()` as the first statement)
    src/data/profile.py (the five functions this page calls, once Plan 02-03 completes)
    .planning/phases/02-investor-profile-feature-engineering-foundation/02-UI-SPEC.md (full file — Design System, Color, Typography, Copywriting Contract, UI Considerations)
    .planning/phases/02-investor-profile-feature-engineering-foundation/02-CONTEXT.md (D-01 through D-13)
  </read_first>
  <action>
    Create `src/pages/profile.py` exporting `render_profile_page() -> None`. First statement: `user = require_auth()` (D-04, no inline auth logic). Read `access_token = st.session_state["access_token"]` and `user_id = user.id`.

    Declare module-level copy constants mirroring `login.py`'s pattern: `PAGE_HEADING = "Investor Profile"`, `PAGE_SUBHEADING = "Tell us how you invest so we can tailor your recommendations. You can update this anytime."`, `HOLDINGS_HEADING = "Existing Holdings"`, `HOLDINGS_EMPTY_STATE = "No holdings added yet. Add a ticker below to get started."`, `INVALID_TICKER_ERROR_TEMPLATE = "We couldn't recognize \"{ticker}\" — check the symbol and try again."`, `SAVE_FAILURE_ERROR = "We couldn't save your profile. Please try again."`, `SAVE_SUCCESS_MESSAGE = "Profile saved."`, `SAVE_BUTTON_LABEL = "Save Profile"`.

    Declare option lists: `RISK_TOLERANCE_OPTIONS = ["Conservative", "Moderate", "Aggressive"]` (D-01); `TIME_HORIZON_OPTIONS = ["&lt;1yr", "1-3yr", "3-5yr", "5-10yr", "10+yr"]` (D-02); `SECTORS = ["Tech", "Healthcare", "Financials", "Energy", "Consumer", "Industrials", "Real Estate", "Utilities", "Materials", "Communication"]` (D-03); `ASSET_TYPE_OPTIONS = ["Stocks", "ETFs", "Crypto", "Gold", "Forex"]` (D-04).

    On every call (D-13 — no caching decorator anywhere in this file; if documenting this design choice in a code comment, phrase it without writing the literal decorator name, e.g. "no caching wraps this read" rather than naming the decorator, so the comment does not accidentally match this file's own no-caching acceptance grep), call `existing_profile = fetch_profile(access_token, user_id)` and `existing_holdings = fetch_holdings(access_token, user_id)` before rendering, to pre-fill widget defaults. If `existing_profile` is `None`, treat every scalar field as unset/default (D-12 — no separate "no profile yet" screen).

    Render `st.title(PAGE_HEADING)` and `st.write(PAGE_SUBHEADING)` outside the form. Wrap everything else in one `with st.form("profile_form"):` block (D-12 — a single always-editable form, one Save Profile CTA, for both first-time creation and later edits):
    - `st.selectbox` for Risk Tolerance (`RISK_TOLERANCE_OPTIONS`, `index=None` when unset, else the matching existing value's index).
    - `st.selectbox` for Time Horizon (`TIME_HORIZON_OPTIONS`, same pre-fill logic).
    - `st.multiselect` for Preferred Sectors (`options=SECTORS`, `default=` existing value or `[]`).
    - `st.multiselect` for Excluded Sectors (`options=SECTORS`, `default=` existing value or `[]`).
    - Preferred Asset Types as five individual `st.checkbox` widgets laid out via `st.columns(5)` — one per `ASSET_TYPE_OPTIONS` entry (per UI-SPEC's Design System table, which specifies a checkbox group, not a single multiselect, for this field) — each pre-checked if that value is present in the existing `preferred_asset_types` list.
    - `st.number_input` for Capital (`min_value=0.0`, `value=` existing value or `0.0`, D-05).
    - `st.subheader(HOLDINGS_HEADING)`. If `existing_holdings` is empty, render `st.caption(HOLDINGS_EMPTY_STATE)` above the grid. Build `holdings_df` from `existing_holdings` with exactly the columns `ticker`/`quantity`/`cost_basis` (an empty frame with those columns if there are no rows). Render `edited_holdings = st.data_editor(holdings_df, num_rows="dynamic", key="holdings_editor", column_config={...labeled "Ticker"/"Quantity"/"Cost Basis"...})`.
    - `submitted = st.form_submit_button(SAVE_BUTTON_LABEL)`.

    After the form block, on `submitted`: convert `edited_holdings` to a list of row dicts, skipping any row with a blank/empty ticker (an unfilled newly-added row). For each remaining row, call `validate_ticker(row["ticker"])` from `src.data.profile`; collect every ticker that fails. If any invalid tickers exist: render `st.error(INVALID_TICKER_ERROR_TEMPLATE.format(ticker=ticker))` once per invalid ticker, apply the red-border highlight to the holdings editor as a whole by reusing `login.py`'s CSS-injection technique scoped to `div.st-key-holdings_editor` (not a specific row/cell — `st.data_editor` exposes no per-cell CSS key the way individual `st.text_input` widgets do, so this is a deliberate widget-level adaptation of the UI-SPEC's per-row intent), and do **not** save anything (neither scalar fields nor holdings).

    If every ticker is valid (or there are no holdings rows at all): wrap `upsert_profile(access_token, user_id, risk_tolerance=..., time_horizon=..., preferred_sectors=..., excluded_sectors=..., preferred_asset_types=[checked asset types], capital=...)` and `upsert_holdings(access_token, user_id, validated_rows)` in a `try`/`except Exception` block. On success, `st.success(SAVE_SUCCESS_MESSAGE)` then `st.rerun()` (so the pre-fill on the next render reflects exactly what was just persisted, matching D-13 and `login.py`'s rerun-after-mutate pattern). On any exception, `st.error(SAVE_FAILURE_ERROR)`.

    Do not import anything from `src.features` in this file, and do not render any recommendation, forecast, or personalized advice text anywhere on this page — it captures preferences only.
  </action>
  <acceptance_criteria>
    - `render_profile_page` is defined and callable with no arguments; its first executable statement calls `require_auth()`.
    - The file contains the exact strings `"Investor Profile"`, `"Save Profile"`, `"Existing Holdings"`, `"No holdings added yet. Add a ticker below to get started."`, `"Profile saved."`.
    - The file contains a `st.data_editor(` call with `num_rows="dynamic"`.
    - The file contains five `st.checkbox` calls (or a loop producing five) for `ASSET_TYPE_OPTIONS`.
    - No `st.cache_data` decorator appears anywhere in this file.
    - No `import` statement references `src.features` anywhere in this file.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "def render_profile_page" src/pages/profile.py &amp;&amp; grep -c "require_auth" src/pages/profile.py &amp;&amp; grep -c "Save Profile" src/pages/profile.py &amp;&amp; grep -c "Existing Holdings" src/pages/profile.py &amp;&amp; grep -c "No holdings added yet" src/pages/profile.py &amp;&amp; grep -c "Profile saved" src/pages/profile.py &amp;&amp; grep -c 'num_rows="dynamic"' src/pages/profile.py &amp;&amp; ! grep -q "st.cache_data" src/pages/profile.py &amp;&amp; ! grep -q "from src.features" src/pages/profile.py &amp;&amp; python -c "import ast; ast.parse(open('src/pages/profile.py').read())"</automated>
  </verify>
  <done>`src/pages/profile.py` renders the single always-editable profile form (scalar fields + holdings grid), validates every holdings ticker on submit before saving anything, and persists via `src/data/profile.py`'s CRUD functions with the exact UI-SPEC Copywriting Contract strings.</done>
</task>

<task type="auto">
  <name>Task 2: Register the profile page in app navigation</name>
  <files>src/app.py</files>
  <read_first>
    src/app.py (current file — the conditional `st.navigation` shape to extend, not replace)
  </read_first>
  <action>
    Modify `src/app.py`: add `from src.pages.profile import render_profile_page`; create `profile_page = st.Page(render_profile_page, title="Investor Profile", url_path="profile")`; add `profile_page` to the existing logged-in `st.navigation({"Home": [home_page]})` branch only (for example `st.navigation({"Home": [home_page], "Profile": [profile_page]})` or `st.navigation({"Home": [home_page, profile_page]})` — either grouping is acceptable). The logged-out branch (`st.navigation([login_page])`) must remain unchanged and must never include `profile_page` (D-03/Pattern 5 — hidden entirely, not visible-but-redirecting, until login).
  </action>
  <acceptance_criteria>
    - `src/app.py` imports `render_profile_page` from `src.pages.profile`.
    - `profile_page` appears in the logged-in `st.navigation(...)` call only; the logged-out `st.navigation([login_page])` line is unchanged.
    - `python -c "import ast; ast.parse(open('src/app.py').read())"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "from src.pages.profile import render_profile_page" src/app.py &amp;&amp; grep -c "profile_page" src/app.py &amp;&amp; python -c "import ast; ast.parse(open('src/app.py').read())"</automated>
    <human-check>Run `streamlit run src/app.py` with the local Supabase stack up (`npx supabase status`). Log in with an existing test account. Confirm an "Investor Profile" page now appears in the sidebar nav alongside Home, and confirm it does NOT appear before logging in. Open it; fill in each scalar field (risk tolerance, time horizon, at least one preferred and one excluded sector, at least one preferred asset type, a capital amount); add two holdings rows — one with a real ticker (e.g. AAPL) and one with an obviously-invalid ticker (e.g. ZZZZZZINVALID) — and click "Save Profile". Confirm the invalid-ticker error and red-border highlight appear on the holdings grid and that nothing was saved (reload the page — fields should still be at their prior/empty state). Fix the invalid ticker to a real one and click "Save Profile" again; confirm "Profile saved." appears and the page reloads with the just-saved values pre-filled. Reload the page again (simulating a fresh visit) and confirm the same values are still pre-filled — this is PROFILE-02's "edited values reflected immediately, no stale cache" success criterion. Remove a holdings row via the data editor's native delete affordance, save again, and confirm it is gone after reload.</human-check>
  </verify>
  <done>`src/app.py` registers the Investor Profile page in the auth-gated navigation branch only; a signed-in user can reach, pre-fill, edit, validate, save, and reload the profile form end-to-end, with the page entirely absent from navigation while logged out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser session ↔ Streamlit process (profile page) | `st.session_state` is the only per-user store; untrusted form input (ticker, sector selections, capital, quantity, cost basis) crosses here |
| Streamlit process ↔ Supabase Postgres (via Plan 02-03's CRUD layer) | JWT-bearing HTTPS calls; RLS is the actual enforcement boundary |
| Streamlit process ↔ Yahoo/yfinance (D-08 ticker validation, via `src.data.profile.validate_ticker`) | Untrusted external network response, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-05 | Tampering (XSS-adjacent) | `src/pages/profile.py` CSS-injection highlight (holdings invalid-ticker) | medium | mitigate | Only the static, developer-controlled string `"holdings_editor"` (the widget's own `key=`) is interpolated into `unsafe_allow_html` — never a raw ticker string or any other user-entered value, matching `login.py`'s existing `_highlight_empty_fields` discipline exactly |
| T-02-01 | Elevation of Privilege / Information Disclosure | `public.holdings` RLS | high | mitigate | Owned by Plan 02-01 (schema) / Plan 02-03 (test proof) — this page only ever calls through the already-proven `src/data/profile.py` chokepoint, never raw Supabase calls |
| T-02-02 | Denial of Service (accidental) | GRANT statements on `public.holdings` | medium | mitigate | Owned by Plan 02-01 |
| T-02-03 | Tampering | `src/data/profile.py` Supabase writes | high | mitigate | Owned by Plan 02-03 |
| T-02-04 | Tampering | `src/data/profile.py` payload construction | high | mitigate | Owned by Plan 02-03 |
| T-02-SC | Tampering (supply chain) | `pandas-ta-classic` install | high | mitigate | Owned by Plan 02-02 |
</threat_model>

<verification>
The grep/AST checks above confirm the page's structure and copy fidelity; the `<human-check>` block on Task 2 is the actual end-to-end proof (deferred to this project's `end-of-phase` human-verify mode) that a signed-in user can build, validate, save, and reload their profile.
</verification>

<success_criteria>
A signed-in user can reach "Investor Profile" from the nav (and only after logging in), fill in and save every field including a dynamic holdings grid, have an invalid ticker rejected with a clear error and no partial save, and see their saved values reflected immediately on the next page load — satisfying PROFILE-01 and PROFILE-02's ROADMAP success criteria.
</success_criteria>

<output>
Create `.planning/phases/02-investor-profile-feature-engineering-foundation/02-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `render_profile_page()` — `src/pages/profile.py`
- `profile_page` registered in `st.navigation`'s logged-in branch — `src/app.py`
