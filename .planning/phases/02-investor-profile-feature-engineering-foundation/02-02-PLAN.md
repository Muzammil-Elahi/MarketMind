---
phase: 02-investor-profile-feature-engineering-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/features/__init__.py
  - src/features/technical.py
  - src/features/feature_frame.py
  - tests/test_features_technical.py
  - tests/test_features_leakage.py
autonomous: false
requirements: [PROFILE-01]
must_haves:
  truths:
    - "assemble_feature_frame(df) is the single shared entry point both this phase's tests and a future backtest harness / live inference will call — no duplicated feature-computation logic exists elsewhere (ROADMAP Phase 2 success criterion #4)."
    - "Every rolling-window computation in src/features/technical.py uses center=False (pandas' default) — never center=True and never .shift(-1) — so no function can pull future rows into a value dated at or before today (Pitfall 4, D-11)."
    - "src/features/technical.py and feature_frame.py have zero Streamlit and zero I/O imports — callers must pass in a DataFrame already obtained from fetch_ohlcv(); the module never fetches its own data (ARCHITECTURE.md Pattern 3, RESEARCH.md module-boundary discipline)."
    - "A leakage smoke test (tests/test_features_leakage.py) fails if truncating the input DataFrame changes any already-computed feature value, and fails if a synthetic future-derived signal leaks into any feature value dated before its source date (D-11, ROADMAP Phase 2 success criterion #3)."
    - "The feature pipeline computes features identically for all 5 supported asset classes (stocks/ETFs/crypto/gold/forex) via single-asset, row-order rolling windows with no cross-asset calendar alignment — that alignment problem is explicitly deferred to Phase 3 (D-09, RESEARCH.md Pattern 3)."
  artifacts:
    - path: "src/features/technical.py"
      provides: "compute_returns(df), compute_volatility(df, window), compute_sma(df, window), compute_rsi(df, window) — pure functions, DataFrame in, Series out, no I/O"
    - path: "src/features/feature_frame.py"
      provides: "assemble_feature_frame(df) -> DataFrame — the single shared entry point calling technical.py functions only"
    - path: "src/features/__init__.py"
      provides: "package marker documenting the zero-I/O module boundary"
  key_links:
    - from: "src/features/feature_frame.py"
      to: "src/features/technical.py"
      via: "assemble_feature_frame() calls compute_returns/compute_volatility/compute_sma/compute_rsi and assembles their outputs into one DataFrame aligned to the input index"
      pattern: "compute_returns\\(df\\)"
  prohibitions:
    - "MUST NOT surface any src/features/ output (technical indicators, returns, volatility) directly in any UI element this phase — the feature pipeline is zero-UI-surface infrastructure for Phase 3/4 to consume later, per 02-CONTEXT.md's Phase Boundary; src/pages/profile.py (Plan 02-04) MUST NOT import from src.features."
---

<objective>
Build `src/features/` — a pure-Python, zero-I/O feature engineering module computing point-in-time technical/factor features (returns, volatility, SMA, RSI) from cached OHLCV data via `pandas-ta-classic`, with an automated leakage smoke test proving no future information ever reaches a past feature value. Install and pin `pandas-ta-classic` behind a package-legitimacy human-verify gate first, since it is flagged `SUS` by the automated legitimacy audit.

Purpose: this is infrastructure ROADMAP Phase 2 success criteria #3/#4 require directly — Phase 3 (recommendation engine) and Phase 4 (prediction/backtesting) will both import `assemble_feature_frame()` later; this phase's job is only to prove the pipeline itself is correct and leakage-safe, with no model or UI consuming it yet (D-09/D-10/D-11).

Output: `src/features/technical.py`, `src/features/feature_frame.py`, `src/features/__init__.py`, plus `tests/test_features_technical.py` and `tests/test_features_leakage.py` — all green, and `pandas-ta-classic==0.6.52`/`pandas==2.3.3` pinned in `requirements.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-investor-profile-feature-engineering-foundation/02-CONTEXT.md
@.planning/phases/02-investor-profile-feature-engineering-foundation/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Verify pandas-ta-classic package legitimacy before install</name>
  <action>Pause for human verification of pandas-ta-classic's package legitimacy before Task 2 installs it — this checkpoint is mandatory per the Package Legitimacy Gate (SUS verdict) and is never auto-approvable.</action>
  <what-built>Nothing yet — this gate precedes installing `pandas-ta-classic==0.6.52`, which the automated package-legitimacy audit flagged `SUS` (too-new: published 2026-06-24, ~26 days old at research time; unknown download count on PyPI). It was already this project's STACK.md-approved choice (replacing the abandoned `pandas-ta` and build-hostile `TA-Lib`), but had not previously been run through the legitimacy gate.</what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/pandas-ta-classic/ and confirm the package page, maintainer identity, and release history look like a genuine continuation of the well-known `twopirllc/pandas-ta` project — not a fresh namesquat with no real history.
    2. Visit https://github.com/xgboosted/pandas-ta-classic and confirm real commit history (not a single-commit dump), a README describing itself as a maintained fork/continuation of `pandas-ta`, and some active issues/PR activity.
    3. Confirm the exact package name to be installed is `pandas-ta-classic` — not a lookalike such as `pandas-ta-classics` or `pandasta-classic`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install and pin pandas-ta-classic + pandas</name>
  <files>requirements.txt</files>
  <read_first>
    requirements.txt (current 5-line pinned manifest from Phase 1 — append, do not reorder or remove existing lines)
    .planning/phases/02-investor-profile-feature-engineering-foundation/02-RESEARCH.md (Standard Stack table and Installation block, lines 74-105 — exact versions and rationale for pinning `pandas==2.3.3` over a range)
  </read_first>
  <action>
    Run `pip install pandas-ta-classic==0.6.52` and `pip install pandas==2.3.3`. Add both as new lines to `requirements.txt` (append after the existing five lines; do not touch `requirements-dev.txt` — these are runtime dependencies of `src/features/`, not dev tooling). Pin the exact version `pandas==2.3.3` (the already-installed/tested 2.3.x release this codebase's `tests/test_cache.py` already exercises) rather than RESEARCH.md's suggested range `pandas>=2.3,<3` — this project's `requirements.txt` convention (established in Phase 1) is exact `==` pins for every entry, with no ranges. After install, run `python -c "import pandas_ta"` to confirm the import succeeds before writing any feature code in Task 3.
  </action>
  <acceptance_criteria>
    - `requirements.txt` contains exactly the line `pandas-ta-classic==0.6.52` and exactly the line `pandas==2.3.3`, in addition to the five pre-existing Phase 1 lines (unchanged).
    - `python -c "import pandas_ta"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "pandas-ta-classic==0.6.52" requirements.txt &amp;&amp; grep -c "pandas==2.3.3" requirements.txt &amp;&amp; grep -c "streamlit==1.59.2" requirements.txt &amp;&amp; python -c "import pandas_ta"</automated>
  </verify>
  <done>`pandas-ta-classic==0.6.52` and `pandas==2.3.3` are installed and pinned in `requirements.txt` alongside Phase 1's five existing entries, with no existing line removed or reordered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Point-in-time technical features + leakage smoke test</name>
  <files>src/features/__init__.py, src/features/technical.py, src/features/feature_frame.py, tests/test_features_technical.py, tests/test_features_leakage.py</files>
  <read_first>
    src/data/prices.py (the module-boundary discipline analog — a thin, docstring-enforced re-export module; apply the same "zero I/O, single entry point" discipline here)
    tests/test_cache.py (`_sample_df()`, lines 30-31 — confirms this codebase's OHLCV DataFrames use capitalized column names, e.g. `"Close"`, matching yfinance's actual `yf.download()` output shape — NOT the lowercase `"close"` used in RESEARCH.md's own illustrative code samples, which were written generically and not cross-checked against this codebase's established convention)
    .planning/phases/02-investor-profile-feature-engineering-foundation/02-RESEARCH.md (Pattern 3 lines 286-320, Pattern 4, Code Examples section lines 369-444, Anti-Patterns to Avoid)
  </read_first>
  <behavior>
    - `compute_returns(df)` returns `df["Close"].pct_change()` — a `pd.Series` aligned to `df.index`, `NaN` at row 0.
    - `compute_volatility(df, window=20)` returns the rolling standard deviation of `compute_returns(df)` via `.rolling(window, center=False).std()` — never `center=True`.
    - `compute_sma(df, window=20)` matches `df["Close"].rolling(window, center=False).mean()` exactly (a plain rolling mean is a strong, independently-verifiable assertion regardless of which `pandas_ta` call path is used internally).
    - `compute_rsi(df, window=14)` returns a `pd.Series` the same length as `df`, with every non-`NaN` value in the closed range `[0, 100]` (RSI's defined range — exact Wilder-smoothing values are `pandas_ta`'s responsibility, not hand-verified here).
    - `assemble_feature_frame(df)` returns a `pd.DataFrame` indexed identically to `df.index`, with exactly the columns `"returns"`, `"volatility_20"`, `"sma_20"`, `"rsi_14"`.
    - Leakage — truncation invariance: for a ~100-row synthetic OHLCV frame, `assemble_feature_frame(df.iloc[:70])` must equal `assemble_feature_frame(df).iloc[:70]` exactly (`pd.testing.assert_frame_equal`) — proves no later row ever changes an earlier feature value.
    - Leakage — synthetic future-signal injection: adding a column to the input `df` that is deterministically derived from a future date's close price must never appear (even indirectly, via `NaN`-filled early rows) in `assemble_feature_frame(df)`'s output for any row dated before that future date.
  </behavior>
  <action>
    Create `src/features/__init__.py` as a package marker with a module docstring stating this package has zero Streamlit/I/O imports and every function takes an already-fetched `DataFrame` as input (mirroring `src/data/prices.py`'s boundary-discipline docstring).

    Create `src/features/technical.py` importing `pandas as pd` and `pandas_ta as ta` at module level, with the four functions specified in `<behavior>`. For `compute_sma`/`compute_rsi`, call `pandas_ta`'s functional API with a positional `Series` argument — `ta.sma(df["Close"], length=window)` / `ta.rsi(df["Close"], length=window)` — rather than the `df.ta.sma(...)` DataFrame accessor, to sidestep the accessor's column-name auto-detection ambiguity (RESEARCH.md Open Question 1, unverified against the installed package). If `pandas_ta.sma`/`pandas_ta.rsi` standalone functions are unavailable in the installed `0.6.52` package at implementation time, fall back to the `df.ta` accessor against a local lowercase-renamed copy of the input DataFrame (`df.rename(columns=str.lower)`, never mutating the caller's original DataFrame) before calling `.ta.sma(length=window)`/`.ta.rsi(length=window)`. Never pass `center=True` anywhere in this file, and never use `.shift(-n)` for any of these four functions.

    Create `src/features/feature_frame.py` with `assemble_feature_frame(df: pd.DataFrame) -> pd.DataFrame`, building a fresh `pd.DataFrame(index=df.index)` and assigning `"returns"`, `"volatility_20"` (window=20), `"sma_20"` (window=20), `"rsi_14"` (window=14) from the corresponding `technical.py` calls — this is the single shared entry point; it must call only `technical.py` functions, never reimplement any rolling-window logic inline.

    Create `tests/test_features_technical.py` with unit tests for each of the four behavior bullets above, using a small deterministic synthetic OHLCV DataFrame (date-indexed, `"Close"`/`"High"`/`"Low"`/`"Open"` columns, at least 30 rows so `sma_20`/`rsi_14` produce non-`NaN` tail values).

    Create `tests/test_features_leakage.py` adapting RESEARCH.md's Code Examples section (`_sample_ohlcv`, `test_truncation_invariance_no_future_data_changes_past_features`, `test_synthetic_future_signal_never_appears_before_its_source_date`) — but build `_sample_ohlcv`'s synthetic frame with capitalized `"Close"`/`"High"`/`"Low"`/`"Open"` columns (matching this codebase's real convention per the `read_first` note above), not RESEARCH.md's lowercase example columns.
  </action>
  <acceptance_criteria>
    - `compute_returns`, `compute_volatility`, `compute_sma`, `compute_rsi` are defined in `src/features/technical.py` with the signatures in `<behavior>`.
    - `assemble_feature_frame` is defined in `src/features/feature_frame.py` and returns exactly the four named columns.
    - No occurrence of `center=True` or `.shift(-1)`/`.shift(-` anywhere in `src/features/technical.py` or `src/features/feature_frame.py`.
    - `src/features/technical.py` and `src/features/feature_frame.py` contain no `import streamlit` and no `yfinance`/`sqlite3` import.
    - `pytest tests/test_features_technical.py tests/test_features_leakage.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_features_technical.py tests/test_features_leakage.py -x -q &amp;&amp; ! grep -q "center=True" src/features/technical.py src/features/feature_frame.py &amp;&amp; ! grep -Eq "\.shift\(-" src/features/technical.py src/features/feature_frame.py &amp;&amp; ! grep -q "import streamlit" src/features/technical.py src/features/feature_frame.py</automated>
  </verify>
  <done>`src/features/technical.py` and `feature_frame.py` compute point-in-time-safe returns/volatility/SMA/RSI with zero I/O and zero centered/lookahead windows, proven by a passing unit test suite plus a leakage smoke test covering both truncation invariance and synthetic future-signal injection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev machine ↔ PyPI (`pandas-ta-classic` install) | Untrusted third-party package supply chain; `SUS`-flagged by the automated legitimacy gate |
| `src/features/` ↔ its caller | Zero-I/O module boundary — the module must never reach out for its own data, only transform what it's handed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-SC | Tampering (supply chain) | `pandas-ta-classic==0.6.52` install | high | mitigate | Task 1's blocking human-verify checkpoint confirms the package is a genuine `pandas-ta` fork-continuation before any install runs; never auto-approvable |
| T-02-01 | Elevation of Privilege / Information Disclosure | `public.holdings` RLS | high | mitigate | Owned by Plan 02-01 (schema) / Plan 02-03 (test proof) — not touched by this plan |
| T-02-02 | Denial of Service (accidental) | GRANT statements on `public.holdings` | medium | mitigate | Owned by Plan 02-01 |
| T-02-03 | Tampering | `src/data/profile.py` Supabase writes | high | mitigate | Owned by Plan 02-03 — not applicable to this plan (no Supabase writes here) |
| T-02-04 | Tampering | `src/data/profile.py` payload construction | high | mitigate | Owned by Plan 02-03 |
| T-02-05 | Tampering (XSS-adjacent) | `src/pages/profile.py` CSS-injection highlight | medium | mitigate | Owned by Plan 02-04 |
</threat_model>

<verification>
`pytest tests/test_features_technical.py tests/test_features_leakage.py -x -q` passes; the negative greps confirm no `center=True`/`.shift(-n)`/Streamlit import anywhere in `src/features/`; `requirements.txt` pins the two new packages exactly.
</verification>

<success_criteria>
`src/features/technical.py` and `feature_frame.py` exist, are zero-I/O, and are proven point-in-time-safe by a passing leakage smoke test — ready for Phase 3/4 to import `assemble_feature_frame()` later with no rework needed.
</success_criteria>

<output>
Create `.planning/phases/02-investor-profile-feature-engineering-foundation/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `compute_returns(df)`, `compute_volatility(df, window)`, `compute_sma(df, window)`, `compute_rsi(df, window)` — `src/features/technical.py`
- `assemble_feature_frame(df)` — `src/features/feature_frame.py`
- `pandas-ta-classic==0.6.52`, `pandas==2.3.3` entries — `requirements.txt`
