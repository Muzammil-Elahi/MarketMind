---
phase: 03-deterministic-recommendation-engine
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/recommendation/similarity.py
  - src/components/__init__.py
  - src/components/disclaimer.py
  - src/components/charts.py
  - tests/test_recommendation_similarity.py
  - tests/test_components.py
autonomous: true
requirements: [REC-01, REC-02]
must_haves:
  truths:
    - "similarity_score(momentum_score, volatility_score, risk_tolerance) is a pure numpy cosine-similarity function over a fixed 2-dimension [momentum, volatility] archetype vector per risk_tolerance level (D-02) -- it takes no user_id, session, or interaction-history argument of any kind, so it is structurally incapable of depending on prior interaction data (the classic collaborative-filtering cold-start problem does not apply to this content-based design)."
    - "similarity_score is fully deterministic: two calls with identical arguments -- including a freshly-constructed profile with no history of any kind -- return bit-identical results (cold-start non-issue, directly testable without any session/state setup)."
    - "similarity_score's output is bounded to [0, 1] for the non-negative percentile-rank inputs this codebase always passes it (cosine similarity of two non-negative vectors)."
    - "The disclaimer banner (STATE.md's 'introduce the component as early as Phase 3' decision) renders the exact 03-UI-SPEC.md Copywriting Contract sentence via a single shared render_disclaimer_banner() function -- both src/pages/recommendations.py and src/pages/search.py (Plans 06/07) call this same function rather than each hardcoding their own copy of the string, so Phase 6's consolidation audit has one call site per page, not duplicated text."
    - "The sub-factor breakdown bar chart and the historical price line chart are each built by ONE shared, independently-testable function (build_breakdown_figure/build_price_history_figure) reused at both compact (recommendation card) and larger (drill-in/search) render sizes -- per 03-UI-SPEC.md's 'same rendering function reused ... not two independently-built components' requirement -- using the declared Chart mark color #334155 for all data ink."
  artifacts:
    - path: "src/recommendation/similarity.py"
      provides: "RISK_ARCHETYPES, cosine_similarity(a, b), similarity_score(momentum_score, volatility_score, risk_tolerance) -> float"
    - path: "src/components/disclaimer.py"
      provides: "DISCLAIMER_TEXT, render_disclaimer_banner() -> None"
    - path: "src/components/charts.py"
      provides: "CHART_MARK_COLOR, build_breakdown_figure(sub_scores_display), render_breakdown_bar_chart(sub_scores_display, key), build_price_history_figure(price_df), render_price_history_chart(price_df, key)"
  key_links:
    - from: "src/recommendation/engine.py (Plan 05)"
      to: "src/recommendation/similarity.py"
      via: "engine.py calls similarity_score(momentum_score, volatility_score, risk_tolerance) as the D-02 sub-score, blended into the composite alongside profile_fit and the factor scores"
      pattern: "def similarity_score\\("
    - from: "src/pages/recommendations.py, src/pages/search.py (Plans 06/07)"
      to: "src/components/disclaimer.py, src/components/charts.py"
      via: "both pages call render_disclaimer_banner() near the top of their render function and render_breakdown_bar_chart()/render_price_history_chart() for their visual output -- neither page duplicates chart-building or disclaimer-copy logic"
      pattern: "render_disclaimer_banner\\("
  prohibitions:
    - "MUST NOT give similarity_score a parameter representing user identity, session, or any form of interaction/purchase history -- the design is explicitly content-based (profile-archetype <-> asset), not user-user/item-item collaborative filtering, and must stay structurally incapable of depending on such data."
    - "MUST NOT phrase DISCLAIMER_TEXT or any chart label with directive/imperative financial-advice language (buy, sell, you should) -- the disclaimer and chart labels are informational only."
---

<objective>
Build the content-based similarity sub-score (`similarity.py`, D-02) and two shared, reusable UI-layer components -- the persistent educational disclaimer banner (`components/disclaimer.py`, STATE.md's early-introduction decision) and the Plotly chart builders (`components/charts.py`, 03-UI-SPEC.md's Design System requirement) -- both consumed identically by the two pages built in Plans 06/07.

Purpose: `similarity.py` completes D-01's third sub-score (profile-fit was Plan 02, factor scores were Plan 01, similarity is here) so `engine.py` (Plan 05) has every input it needs. `components/` is a net-new package this phase introduces (not anticipated by 03-PATTERNS.md's file list, since neither a disclaimer nor a chart existed before this phase) specifically so the disclaimer copy and the two chart-building functions each have exactly one implementation, reused by both new pages rather than duplicated.

Output: `src/recommendation/similarity.py`, `src/components/__init__.py`, `src/components/disclaimer.py`, `src/components/charts.py`, plus two test files -- all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md
@.planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Content-based similarity sub-score (D-02)</name>
  <files>src/recommendation/similarity.py, tests/test_recommendation_similarity.py</files>
  <read_first>
    .planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md (Pattern 3, lines 247-281, code block to adapt closely; cold-start framing, Sources section entries on collaborative-filtering cold start)
    .planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md (D-02)
  </read_first>
  <behavior>
    - `cosine_similarity(np.array([1.0, 0.0]), np.array([1.0, 0.0]))` returns `1.0`; `cosine_similarity(np.array([0.0, 0.0]), np.array([1.0, 0.0]))` returns `0.0` (zero-vector guard, no division-by-zero error).
    - `RISK_ARCHETYPES` has exactly the three keys `"Conservative"`, `"Moderate"`, `"Aggressive"` (matches `src/pages/profile.py`'s `RISK_TOLERANCE_OPTIONS` exactly).
    - `similarity_score(momentum_score=0.9, volatility_score=0.2, risk_tolerance="Aggressive")` returns a strictly higher value than `similarity_score(momentum_score=0.9, volatility_score=0.2, risk_tolerance="Conservative")` (an asset with high momentum/low stability should read as a closer match to the Aggressive archetype than the Conservative one).
    - `similarity_score` for any `risk_tolerance` not in `RISK_ARCHETYPES` falls back to `"Moderate"`'s archetype rather than raising `KeyError` (defensive default for a not-yet-set profile field, matching Phase 2's nullable-fields precedent).
    - Calling `similarity_score` twice with identical arguments (including no additional profile/session/history data of any kind) returns bit-identical results both times -- the cold-start-is-a-non-issue proof: this function's signature has no parameter through which prior interaction history could even be threaded.
    - Every value `similarity_score` returns for `momentum_score`/`volatility_score` inputs in `[0, 1]` is itself in `[0, 1]`.
  </behavior>
  <action>
    Create `src/recommendation/similarity.py` importing `numpy as np` only. Define `RISK_ARCHETYPES = {"Conservative": np.array([0.3, 0.9]), "Moderate": np.array([0.6, 0.6]), "Aggressive": np.array([0.9, 0.2])}` (per RESEARCH.md Pattern 3's target vectors over `[momentum, stability/volatility]` dimensions -- explicitly a tunable v1 design choice, not derived from an external benchmark, per Assumptions Log A2). Define `cosine_similarity(a: np.ndarray, b: np.ndarray) -> float`: compute `denom = np.linalg.norm(a) * np.linalg.norm(b)`; if `denom == 0` return `0.0`; else return `float(np.dot(a, b) / denom)`. Define `similarity_score(momentum_score: float, volatility_score: float, risk_tolerance: str) -> float`: look up `archetype = RISK_ARCHETYPES.get(risk_tolerance, RISK_ARCHETYPES["Moderate"])`; build `asset_vector = np.array([momentum_score, volatility_score])`; return `cosine_similarity(asset_vector, archetype)`. No `user_id`/session/history parameter anywhere in this file's public functions.

    Create `tests/test_recommendation_similarity.py` covering every `<behavior>` bullet, including an explicit "cold-start non-issue" test that inspects `inspect.signature(similarity_score).parameters` and asserts the parameter set is exactly `{"momentum_score", "volatility_score", "risk_tolerance"}` (structural proof no interaction-history channel exists), plus the two determinism/repeatability calls.
  </action>
  <acceptance_criteria>
    - `RISK_ARCHETYPES`, `cosine_similarity`, `similarity_score` are all defined in `src/recommendation/similarity.py` with the signatures above.
    - `pytest tests/test_recommendation_similarity.py -x -q` passes.
    - `inspect.signature(similarity_score).parameters` contains no key named `user_id`, `session`, or `history`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_recommendation_similarity.py -x -q</automated>
  </verify>
  <done>`similarity.py` computes a deterministic, bounded, content-based profile-archetype <-> asset similarity score with no interaction-history dependency of any kind -- proven by a passing unit test suite including a structural cold-start-non-issue assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared disclaimer banner + Plotly chart builders</name>
  <files>src/components/__init__.py, src/components/disclaimer.py, src/components/charts.py, tests/test_components.py</files>
  <read_first>
    .planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md (Color table lines 71-83 for CHART_MARK_COLOR; Copywriting Contract line 104 for the exact disclaimer sentence; UI Considerations row 136 for "same rendering function reused" requirement)
    src/pages/home.py (module-level docstring/structure style for a small, focused Streamlit-facing module)
  </read_first>
  <behavior>
    - `DISCLAIMER_TEXT` equals exactly `"For informational and educational purposes only — not financial advice. Scores and rankings are not personalized recommendations, and past performance does not predict future results."` (UI-SPEC Copywriting Contract, verbatim, including the em dash).
    - `build_breakdown_figure({"profile_fit": 62, "momentum": 71, "volatility": 55, "quality": 48, "similarity": 67})` returns a `plotly.graph_objects.Figure` with exactly 5 bars, values matching the input dict's values in the input dict's key order (SUB_SCORE_ORDER from `explain.py`, Plan 02 -- REC-02 ordering), and every bar's marker color equal to `CHART_MARK_COLOR`.
    - `build_price_history_figure(price_df)` (a DataFrame with a `"Close"` column) returns a `plotly.graph_objects.Figure` with exactly one line trace whose y-values equal `price_df["Close"].tolist()`, colored `CHART_MARK_COLOR`.
    - `CHART_MARK_COLOR` equals exactly `"#334155"` (UI-SPEC Color table).
  </behavior>
  <action>
    Create `src/components/__init__.py` as a package marker with a docstring noting this is a new package this phase introduces for UI elements shared across pages (disclaimer banner, chart builders) -- explicitly not part of `src/recommendation/` (which stays zero-I/O/zero-Streamlit) nor page-specific, since both `src/pages/recommendations.py` and `src/pages/search.py` (Plans 06/07) need identical copies of each.

    Create `src/components/disclaimer.py` importing `streamlit as st`. Define `DISCLAIMER_TEXT` per `<behavior>` exactly. Define `render_disclaimer_banner() -> None` rendering `DISCLAIMER_TEXT` inside `st.container(border=True)` (matching the UI-SPEC's Secondary/`#F0F2F6` card-surface banner treatment) via `st.caption(DISCLAIMER_TEXT)` (Label-role typography per UI-SPEC's Typography table row for disclaimer banner text).

    Create `src/components/charts.py` importing `plotly.graph_objects as go` and `pandas as pd`. Define `CHART_MARK_COLOR = "#334155"`. Define `build_breakdown_figure(sub_scores_display: dict[str, int]) -> go.Figure`: build a horizontal bar chart (`go.Bar(x=list(sub_scores_display.values()), y=list(sub_scores_display.keys()), orientation="h", marker_color=CHART_MARK_COLOR)`) wrapped in `go.Figure(data=[bar])`, preserving the input dict's iteration order (never re-sorted by value) so REC-02's fixed sub-factor display order is respected. Define `render_breakdown_bar_chart(sub_scores_display: dict, key: str) -> None` calling `st.plotly_chart(build_breakdown_figure(sub_scores_display), key=key)`. Define `build_price_history_figure(price_df: pd.DataFrame) -> go.Figure`: build a single line trace (`go.Scatter(x=price_df.index, y=price_df["Close"], mode="lines", line_color=CHART_MARK_COLOR)`) wrapped in `go.Figure(data=[scatter])`. Define `render_price_history_chart(price_df: pd.DataFrame, key: str) -> None` calling `st.plotly_chart(build_price_history_figure(price_df), key=key)`.

    Create `tests/test_components.py` covering every `<behavior>` bullet for `build_breakdown_figure`/`build_price_history_figure`/`DISCLAIMER_TEXT`/`CHART_MARK_COLOR` (the pure figure-building functions, not the `st.plotly_chart`-calling wrappers, which require a running Streamlit script context to test meaningfully and are exercised instead by Plans 06/07's human-check).
  </action>
  <acceptance_criteria>
    - `DISCLAIMER_TEXT`, `render_disclaimer_banner` are defined in `src/components/disclaimer.py`; `CHART_MARK_COLOR`, `build_breakdown_figure`, `render_breakdown_bar_chart`, `build_price_history_figure`, `render_price_history_chart` are defined in `src/components/charts.py`.
    - `pytest tests/test_components.py -x -q` passes.
    - No occurrence of "buy", "sell", or "you should" (case-insensitive) in `src/components/disclaimer.py` or `charts.py`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_components.py -x -q &amp;&amp; ! grep -Eiq "\bbuy\b|\bsell\b|you should" src/components/disclaimer.py src/components/charts.py</automated>
  </verify>
  <done>A single shared disclaimer banner and a single shared pair of chart-building functions exist, ready for both new pages (Plans 06/07) to reuse without duplicating copy or chart logic -- proven by a passing unit test suite for every pure/figure-building function.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Profile-derived numeric inputs -> `similarity_score` | No untrusted external input crosses here; inputs are already-computed percentile scores |
| Feature-derived price data -> `build_price_history_figure` | Already-fetched, already-cached OHLCV data (via the Plan 01 loader) rendered as chart data, not re-fetched or re-validated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-SC-1 | Tampering (supply chain) | `numpy` install | medium | mitigate | Owned by Plan 03 -- this plan only consumes the already-approved package |
| T-03-SC-2 | Tampering (supply chain) | `plotly` install | medium | mitigate | Owned by Plan 03 -- this plan only consumes the already-approved package |
| T-03-04 | Tampering (business-rule integrity) | Profile-driven hard-exclude filter | medium | mitigate | Owned by Plan 02 / Plan 05 -- not applicable to this plan |
</threat_model>

<verification>
`pytest tests/test_recommendation_similarity.py tests/test_components.py -x -q` passes; the negative greps confirm no directive-language strings in the new UI-facing files.
</verification>

<success_criteria>
`similarity.py` completes D-01's three sub-scores with a deterministic, structurally cold-start-immune similarity function; `components/disclaimer.py` and `components/charts.py` provide the single shared implementation of every visual element both new pages need, ready for Plan 05 (engine) and Plans 06/07 (pages) to consume with no rework.
</success_criteria>

<output>
Create `.planning/phases/03-deterministic-recommendation-engine/03-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `RISK_ARCHETYPES`, `cosine_similarity(a, b)`, `similarity_score(momentum_score, volatility_score, risk_tolerance)` — `src/recommendation/similarity.py`
- `DISCLAIMER_TEXT`, `render_disclaimer_banner()` — `src/components/disclaimer.py`
- `CHART_MARK_COLOR`, `build_breakdown_figure(sub_scores_display)`, `render_breakdown_bar_chart(sub_scores_display, key)`, `build_price_history_figure(price_df)`, `render_price_history_chart(price_df, key)` — `src/components/charts.py`
</content>
