---
phase: 03-deterministic-recommendation-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/recommendation/profile_fit.py
  - src/recommendation/explain.py
  - tests/test_recommendation_profile_fit.py
  - tests/test_recommendation_explain.py
autonomous: true
requirements: [REC-02, REC-03]
must_haves:
  truths:
    - "is_excluded(asset_row, profile) is the single authoritative hard-exclude decision (D-01's profile-fit sub-score's exclusionary gate): an asset whose sector is in the profile's excluded_sectors, or whose asset_class is not in a non-empty preferred_asset_types list, is excluded — the engine (Plan 05) must call this BEFORE any factor/similarity/composite computation runs, holding excluded assets out of scoring entirely, never merely down-weighting them."
    - "compute_profile_fit(asset_row, profile) is a rule-based [0, 1]-bounded score assuming is_excluded(asset_row, profile) is already False for its input — never re-implements the exclusion check itself, avoiding two independently-computed exclusion paths that could drift."
    - "explain(sub_scores, risk_tolerance) selects its top factor(s) directly from the SAME sub_scores dict the caller will display (Pattern 5's traceability requirement, D-06) -- never a separately-computed 'most important factor.'"
    - "Sub-factor tie-break for explanation selection uses the fixed SUB_SCORE_ORDER = ['profile_fit', 'momentum', 'volatility', 'quality', 'similarity'] -- REC-03 adjacency: when exactly two sub-scores are tied for the top value, the two-factor template fires, selecting the tied pair in SUB_SCORE_ORDER (never dict-iteration or random order); REC-03 ordering: this selection is fully deterministic for identical input."
    - "REC-03 empty: when the top value is shared by three or more sub-scores (no single factor or pair is meaningfully dominant), the one-factor template still fires on whichever tied factor ranks first in SUB_SCORE_ORDER -- the explanation sentence is never left blank."
    - "Neither explain.py's FACTOR_LABELS nor its two sentence templates contain any directive/imperative financial-advice verb (buy, sell, you should) or personalized-recommendation phrasing -- informational/descriptive framing only (prohibition against directive language in template-generated copy)."
  artifacts:
    - path: "src/recommendation/profile_fit.py"
      provides: "is_excluded(asset_row, profile) -> bool, compute_profile_fit(asset_row, profile) -> float"
    - path: "src/recommendation/explain.py"
      provides: "SUB_SCORE_ORDER, FACTOR_LABELS, explain(sub_scores, risk_tolerance) -> str"
  key_links:
    - from: "src/recommendation/explain.py"
      to: "src/recommendation/engine.py (Plan 05)"
      via: "engine.py calls explain(sub_scores, risk_tolerance) with the exact same sub_scores dict it stores/returns for REC-02's breakdown display"
      pattern: "def explain\\("
  prohibitions:
    - "MUST NOT down-weight an excluded asset instead of removing it -- is_excluded's True result must cause the caller to drop the row from the universe entirely before any scoring math runs (prohibition against merely down-weighting excluded categories)."
    - "MUST NOT use imperative/directive financial-advice language (buy, sell, you should) or imply a personalized recommendation anywhere in FACTOR_LABELS or the two sentence templates."
---

<objective>
Build the profile-driven hard-exclude/fit rule engine (`profile_fit.py`, D-01's third sub-score) and the deterministic template-based one-sentence explanation generator (`explain.py`, D-06/REC-03) — both pure, zero-I/O, zero-LLM functions consumed by `engine.py` (Plan 05).

Purpose: `profile_fit.is_excluded` is the single source of truth for prohibition #3 (excluded sectors/asset-types held out of scoring entirely, not down-weighted) that `engine.py` must call before any composite math runs; `explain.py` is REC-03's entire deterministic-templating implementation, with no LLM call anywhere.

Output: `src/recommendation/profile_fit.py`, `src/recommendation/explain.py`, plus two test files — all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md
@.planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Profile-fit rule engine (hard-exclude + rule-based fit score)</name>
  <files>src/recommendation/profile_fit.py, tests/test_recommendation_profile_fit.py</files>
  <read_first>
    src/features/technical.py (zero-I/O module docstring/discipline to replicate)
    .planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md (Pattern 4, lines 283-287; Open Questions 1 and 3, lines 438-451)
    .planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md (D-01)
  </read_first>
  <behavior>
    - `is_excluded({"sector": "Energy", "asset_class": "Stocks"}, {"excluded_sectors": ["Energy"]})` returns `True`.
    - `is_excluded({"sector": "Tech", "asset_class": "Crypto"}, {"preferred_asset_types": ["Stocks"]})` returns `True` (asset_class not in a non-empty preferred_asset_types list -- Open Question 1's hard-filter resolution).
    - `is_excluded({"sector": "Tech", "asset_class": "Stocks"}, {"preferred_asset_types": []})` returns `False` (an empty/unset preferred_asset_types list means no asset-class filter is applied, matching Phase 2's nullable-fields precedent).
    - `is_excluded({"sector": None, "asset_class": "Crypto"}, {"excluded_sectors": ["Energy"], "preferred_asset_types": []})` returns `False` (non-stock asset with no sector never matches a sector-based exclusion).
    - `compute_profile_fit({"sector": "Tech", "asset_class": "Stocks", "momentum_pct": 0.6}, {"preferred_sectors": ["Tech"], "time_horizon": "10+yr"})` returns a value `> compute_profile_fit({"sector": "Healthcare", ...}, {"preferred_sectors": ["Tech"], "time_horizon": "<1yr"}, ...)` for an otherwise-identical asset with a non-preferred sector and a short time horizon (score responds to profile inputs, per FEATURES.md's "profile answers must visibly change output" finding).
    - `compute_profile_fit(...)` always returns a value in the closed range `[0, 1]` regardless of which profile fields are `None`/missing (Phase 2's nullable-fields precedent -- a partially-filled profile must never raise or return an out-of-range score).
  </behavior>
  <action>
    Create `src/recommendation/profile_fit.py` with a zero-I/O module docstring matching `src/features/technical.py`'s discipline (pandas/no imports needed at all here -- both functions operate on plain dicts, not DataFrames). Define `is_excluded(asset_row: dict, profile: dict) -> bool`: read `excluded_sectors = profile.get("excluded_sectors") or []`; if `asset_row.get("sector") in excluded_sectors` (and sector is not `None`) return `True`; read `preferred_asset_types = profile.get("preferred_asset_types") or []`; if `preferred_asset_types` is non-empty and `asset_row.get("asset_class") not in preferred_asset_types` return `True`; else return `False`. Define `compute_profile_fit(asset_row: dict, profile: dict) -> float`, documented as assuming `is_excluded(asset_row, profile)` is already `False` for its input (callers, i.e. `engine.py`, must pre-filter): start `score = 0.5` (neutral baseline); if `asset_row.get("sector")` is in `profile.get("preferred_sectors") or []`, add `0.3`; if `profile.get("time_horizon")` is in `("5-10yr", "10+yr")` and `asset_row.get("momentum_pct")` is not `None` and `>= 0.5`, add `0.2` (Open Question 3's resolution -- `time_horizon` only nudges the profile-fit sub-score, never a second similarity-archetype dimension); return `min(score, 1.0)`.

    Create `tests/test_recommendation_profile_fit.py` covering every `<behavior>` bullet with plain-dict fixtures, following `tests/test_features_technical.py`'s synthetic-data style (no DataFrame needed here).
  </action>
  <acceptance_criteria>
    - `is_excluded` and `compute_profile_fit` are defined in `src/recommendation/profile_fit.py` with the signatures above.
    - `pytest tests/test_recommendation_profile_fit.py -x -q` passes.
    - No `import pandas`, `import streamlit`, `import yfinance` in `profile_fit.py` (plain-dict pure functions, no DataFrame dependency).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_recommendation_profile_fit.py -x -q &amp;&amp; ! grep -Eq "import (streamlit|yfinance)" src/recommendation/profile_fit.py</automated>
  </verify>
  <done>`profile_fit.py` provides a single authoritative hard-exclude decision and a [0,1]-bounded rule-based fit score that responds visibly to sector/asset-type/time-horizon profile inputs -- proven by a passing unit test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic template-based explanation (REC-03/D-06)</name>
  <files>src/recommendation/explain.py, tests/test_recommendation_explain.py</files>
  <read_first>
    .planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md (Pattern 5, lines 289-314, including the exact traceability requirement paragraph)
    .planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md (Copywriting Contract rows for the two explanation-sentence templates, lines 102-103, and the Notes bullet on factor-label fidelity, lines 112-115)
  </read_first>
  <behavior>
    - `explain({"profile_fit": 0.5, "momentum": 0.9, "volatility": 0.5, "quality": 0.5, "similarity": 0.5}, "Moderate")` returns exactly `"Strong momentum matches your Moderate risk profile."` (a single clear top factor, no tie -- one-factor template).
    - `explain({"profile_fit": 0.5, "momentum": 0.8, "volatility": 0.8, "quality": 0.3, "similarity": 0.2}, "Aggressive")` returns exactly `"Strong momentum and low volatility match your Aggressive risk profile."` (exactly two sub-scores tied for the top value -- two-factor template; REC-03 adjacency, factors selected in SUB_SCORE_ORDER = momentum before volatility since momentum precedes volatility in that fixed list).
    - `explain({"profile_fit": 0.7, "momentum": 0.7, "volatility": 0.7, "quality": 0.7, "similarity": 0.7}, "Conservative")` returns exactly `"Strong alignment with your preferred sectors matches your Conservative risk profile."` (REC-03 empty: 5-way tie at the top, "no single factor meaningfully dominant" -- falls back to the ONE-factor template on whichever tied factor ranks first in SUB_SCORE_ORDER, i.e. profile_fit, never left blank and never a nonsensical multi-factor list).
    - Calling `explain` twice with identical arguments returns the identical string both times (deterministic, no hidden state).
    - No occurrence of "buy", "sell", or "you should" (case-insensitive) anywhere in `FACTOR_LABELS.values()` or either template string.
  </behavior>
  <action>
    Create `src/recommendation/explain.py`, zero-I/O, importing nothing beyond the standard library. Define `SUB_SCORE_ORDER = ["profile_fit", "momentum", "volatility", "quality", "similarity"]` (the single fixed order used both for REC-02's displayed breakdown order in `engine.py`, Plan 05, and for this file's tie-break -- do not redefine this list a second time anywhere else in the codebase; `engine.py` imports it from here). Define `FACTOR_LABELS = {"profile_fit": "alignment with your preferred sectors", "momentum": "momentum", "volatility": "low volatility", "quality": "price stability", "similarity": "profile similarity"}`. Define `TWO_FACTOR_TEMPLATE = "Strong {factor_a} and {factor_b} match your {risk_tolerance} risk profile."` and `ONE_FACTOR_TEMPLATE = "Strong {factor_a} matches your {risk_tolerance} risk profile."` (exact strings from the UI-SPEC Copywriting Contract). Define `explain(sub_scores: dict[str, float], risk_tolerance: str) -> str`: build `ordered = sorted(sub_scores.items(), key=lambda kv: (-kv[1], SUB_SCORE_ORDER.index(kv[0])))`; compute `top_value = ordered[0][1]`; compute `tied_top = [key for key, value in ordered if value == top_value]`; if `len(tied_top) == 2`, return `TWO_FACTOR_TEMPLATE.format(factor_a=FACTOR_LABELS[tied_top[0]], factor_b=FACTOR_LABELS[tied_top[1]], risk_tolerance=risk_tolerance)`; else (a clear single winner, `len(tied_top) == 1`, OR three-or-more tied) return `ONE_FACTOR_TEMPLATE.format(factor_a=FACTOR_LABELS[ordered[0][0]], risk_tolerance=risk_tolerance)`.

    Create `tests/test_recommendation_explain.py` covering every `<behavior>` bullet exactly (assert the literal returned strings for the first three cases, not just substring checks, since exact-string fidelity to the UI-SPEC Copywriting Contract is the acceptance bar).
  </action>
  <acceptance_criteria>
    - `SUB_SCORE_ORDER`, `FACTOR_LABELS`, `explain` are all defined in `src/recommendation/explain.py`.
    - `pytest tests/test_recommendation_explain.py -x -q` passes.
    - No occurrence of the words "buy", "sell", or "you should" (case-insensitive) in `src/recommendation/explain.py`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_recommendation_explain.py -x -q &amp;&amp; ! grep -Eiq "\bbuy\b|\bsell\b|you should" src/recommendation/explain.py</automated>
  </verify>
  <done>`explain.py` deterministically generates REC-03's one-sentence explanation directly from the same sub-scores dict the UI will display, with a fixed sub-factor tie-break order and no directive language -- proven by exact-string unit tests covering the clear-winner, exact-two-way-tie, and three-plus-way-tie cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Investor profile data -> `profile_fit.is_excluded` | Profile fields (already RLS-protected, Phase 2) drive a business-rule filter; no new trust boundary crossing, but incorrect logic here is the enforcement point for prohibition #3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-04 | Tampering (business-rule integrity) | `profile_fit.is_excluded` | medium | mitigate | `is_excluded` is the single authoritative exclusion decision, called by `engine.py` (Plan 05) BEFORE any scoring math runs so an excluded asset never reaches `compute_profile_fit`/`factor_scoring`/`similarity` at all -- proven by this plan's own unit tests plus Plan 05's integration test asserting an excluded asset never appears in `build_recommendations`'s output regardless of its raw factor scores |
| T-03-02 | Information Disclosure | Per-user profile data flowing into `explain`/`compute_profile_fit` | low | accept | Both functions are pure/stateless/uncached -- no persistence or cross-request leakage is possible from this plan's code |
| T-03-01 | Tampering | Ticker string -> `fetch_ohlcv` chokepoint | high | mitigate | Owned by Plan 01 (loader) / Plan 07 (search page) -- not applicable to this plan (no I/O here) |
| T-03-SC-1 / T-03-SC-2 | Tampering (supply chain) | `numpy`/`plotly` install | medium | mitigate | Owned by Plan 03 |
</threat_model>

<verification>
`pytest tests/test_recommendation_profile_fit.py tests/test_recommendation_explain.py -x -q` passes; the negative greps confirm no directive-language strings and no unnecessary I/O imports.
</verification>

<success_criteria>
`profile_fit.py` and `explain.py` exist as pure, zero-I/O, zero-LLM functions, ready for `engine.py` (Plan 05) to import with no rework -- the hard-exclude rule and the explanation-generation logic are both deterministic and directly traceable to their inputs.
</success_criteria>

<output>
Create `.planning/phases/03-deterministic-recommendation-engine/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `is_excluded(asset_row, profile)`, `compute_profile_fit(asset_row, profile)` — `src/recommendation/profile_fit.py`
- `SUB_SCORE_ORDER`, `FACTOR_LABELS`, `TWO_FACTOR_TEMPLATE`, `ONE_FACTOR_TEMPLATE`, `explain(sub_scores, risk_tolerance)` — `src/recommendation/explain.py`
</content>
