---
phase: 03-deterministic-recommendation-engine
plan: 08
type: execute
wave: 5
depends_on: ["03-06", "03-07"]
files_modified:
  - src/app.py
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04]
must_haves:
  truths:
    - "Both recommendations_page (url_path='recommendations') and search_page (url_path='search') are registered only in the logged-in st.navigation branch, matching the existing D-03 conditional-nav pattern -- neither page is reachable, visible, or a broken link while logged out."
    - "The url_path string 'search' registered here matches exactly the url_path string src/pages/recommendations.py's (Plan 06) 'View Details' button already passes to st.switch_page, so the cross-page navigation actually resolves."
    - "This phase's full user-facing loop is walkable end-to-end by a human: sign in, view the ranked shortlist across all 5 asset classes with visible score breakdowns and explanations, click View Details into the Search page, search an arbitrary ticker, and see the disclaimer banner on both pages."
  artifacts:
    - path: "src/app.py"
      provides: "recommendations_page and search_page registered in the logged-in st.navigation branch"
  key_links:
    - from: "src/app.py"
      to: "src/pages/recommendations.py, src/pages/search.py"
      via: "st.navigation({...}) includes both new pages only in the logged-in branch"
      pattern: "render_recommendations_page"
  prohibitions:
    - "MUST NOT register either new page in the logged-out st.navigation([login_page]) branch."
---

<objective>
Register the Recommendations and Search pages (Plans 06/07) in `src/app.py`'s auth-gated navigation, completing Phase 3's user-facing wiring, then walk the full end-to-end flow as this phase's end-of-phase human verification (`workflow.human_verify_mode: end-of-phase`).

Purpose: this is the final integration step -- every prior plan in this phase (universe/factor-scoring/loader, profile-fit/explain, dependencies, similarity/components, engine, both pages) converges here into a single reachable, navigable feature.

Output: `src/app.py` updated; a human-confirmed working recommendation-and-search flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md
@.planning/phases/03-deterministic-recommendation-engine/03-06-SUMMARY.md
@.planning/phases/03-deterministic-recommendation-engine/03-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register both new pages in app navigation + end-to-end verification</name>
  <files>src/app.py</files>
  <read_first>
    src/app.py (current file -- the conditional st.navigation shape to extend, not replace; the exact `# noqa: E402` import-ordering convention established for every page import after the sys.path insert block)
  </read_first>
  <action>
    Modify `src/app.py`: add `from src.pages.recommendations import render_recommendations_page  # noqa: E402` and `from src.pages.search import render_search_page  # noqa: E402` alongside the existing page imports. Create `recommendations_page = st.Page(render_recommendations_page, title="Recommendations", url_path="recommendations")` and `search_page = st.Page(render_search_page, title="Search", url_path="search")` (this exact `url_path="search"` string must match what `src/pages/recommendations.py`'s "View Details" button already passes to `st.switch_page`, Plan 06). Add both to the existing logged-in `st.navigation({...})` call only (for example `st.navigation({"Home": [home_page], "Profile": [profile_page], "Recommendations": [recommendations_page], "Search": [search_page]})` or an equivalent grouping) -- the logged-out branch (`st.navigation([login_page])`) must remain unchanged and must never include either new page.
  </action>
  <acceptance_criteria>
    - `src/app.py` imports `render_recommendations_page` from `src.pages.recommendations` and `render_search_page` from `src.pages.search`.
    - `recommendations_page`/`search_page` appear only in the logged-in `st.navigation(...)` call; the logged-out `st.navigation([login_page])` line is unchanged.
    - `python -c "import ast; ast.parse(open('src/app.py').read())"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "from src.pages.recommendations import render_recommendations_page" src/app.py &amp;&amp; grep -c "from src.pages.search import render_search_page" src/app.py &amp;&amp; grep -c 'url_path="search"' src/app.py &amp;&amp; python -c "import ast; ast.parse(open('src/app.py').read())" &amp;&amp; pytest -x -q</automated>
    <human-check>Run `streamlit run src/app.py` with the local Supabase stack up (`npx supabase status`). Log in with an existing test account (create one via the login page's "Create Account" tab if needed, and fill in an investor profile via the Profile page first so the ranked list is personalized). Confirm "Recommendations" and "Search" now appear in the sidebar nav alongside Home/Profile, and confirm neither appears before logging in. Open "Recommendations": confirm the disclaimer banner renders near the top; confirm all 5 asset-class section headers (Stocks/ETFs/Crypto/Gold/Forex) render in that order even if a class has few/no cards; confirm each card shows a "{score}/100" composite score, an expandable "Score Breakdown" bar chart with 5 bars in Profile Fit/Momentum/Volatility/Quality/Similarity order, and a one-sentence explanation that plausibly matches the highest bar(s). Click "View Details" on any card; confirm it navigates to the Search page with that ticker pre-filled and its score/chart already showing. Return to Search directly and try three cases: (a) a valid, well-known ticker not in the recommended list (e.g. a large-cap stock outside the curated universe) -- confirm it scores; (b) an obviously-invalid ticker (e.g. "ZZZZZZINVALID") -- confirm the "We couldn't find..." error renders, no crash; (c) leave the search box empty and submit -- confirm the "Search for any asset..." empty-state copy renders, no fetch attempted. Confirm the disclaimer banner also renders on the Search page.</human-check>
  </verify>
  <done>`src/app.py` registers both new pages in the auth-gated navigation branch only; a signed-in user can reach the ranked shortlist, drill into any card via "View Details," search an arbitrary ticker, and see the disclaimer banner on both pages -- end-to-end, human-confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser session <-> Streamlit process (nav registration) | No new trust boundary -- this plan only wires existing, already-threat-modeled pages into navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-03 | Spoofing / Elevation of Privilege | Both new pages reachable only when logged in | high | mitigate | Registered exclusively in the logged-in `st.navigation` branch (Task 1), matching D-03's established "hidden entirely, not visible-but-redirecting" pattern -- each page also independently calls `require_auth()` as defense-in-depth (Plans 06/07) |
| T-03-01 | Tampering | Search ticker input -> `fetch_ohlcv` chokepoint | high | mitigate | Owned by Plans 01/07 -- fully exercised, not re-tested, by this plan's human-check |
| T-03-SC-1 / T-03-SC-2 | Tampering (supply chain) | `numpy`/`plotly` install | medium | mitigate | Owned by Plan 03 |
| T-03-04 | Tampering (business-rule integrity) | Profile-driven hard-exclude filter | medium | mitigate | Owned by Plan 02/05 |
| T-03-02 | Information Disclosure | Per-user profile-derived scoring | medium | mitigate | Owned by Plans 05/06/07 |
</threat_model>

<verification>
The grep/AST checks confirm registration and `url_path` fidelity; `pytest -x -q` runs the full suite (every phase's tests, including this phase's, must be green together); the `<human-check>` block is the actual end-to-end proof of REC-01 through REC-04, deferred to this project's `end-of-phase` human-verify mode.
</verification>

<success_criteria>
A signed-in user can reach Recommendations and Search from the nav (only after logging in), see a personalized, deterministic, explainable ranked shortlist across all 5 asset classes, and search any ticker with the correct not-found/insufficient-data/scored state — satisfying every ROADMAP Phase 3 success criterion.
</success_criteria>

<output>
Create `.planning/phases/03-deterministic-recommendation-engine/03-08-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `recommendations_page`, `search_page` registered in `st.navigation`'s logged-in branch — `src/app.py`
</content>
