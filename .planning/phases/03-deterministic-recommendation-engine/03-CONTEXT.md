# Phase 3: Deterministic Recommendation Engine - Context

**Gathered:** 2026-08-04
**Status:** Ready for planning

<domain>
## Phase Boundary

A fully deterministic (zero-LLM, zero-network-at-score-time) recommendation engine: a hybrid factor + collaborative-filtering-style scorer that ranks a curated cross-asset-class universe against a user's investor profile, shows a visible sub-factor breakdown per recommendation, generates a one-sentence plain-English reason via templating (no LLM), and supports free-text ticker search/drill-in for any asset regardless of whether it's in the recommended universe.

Explicitly NOT this phase: LLM reranking/annotation (Phase 5), price prediction/forecasting (Phase 4), any model training.

</domain>

<decisions>
## Implementation Decisions

### Scoring model design
- **D-01:** Composite score = weighted sum of normalized sub-scores: profile-fit score + factor scores (momentum/volatility/quality-style) + collaborative-similarity score. Each sub-score/weight must be independently displayable to satisfy REC-02's visible-breakdown requirement.
- **D-02:** The "collaborative-filtering-style" component is asset-to-asset (or profile-to-asset-archetype) similarity — e.g. cosine similarity over factor vectors — not true user-user CF, since there's no real interaction history yet. This matches `ARCHITECTURE.md`'s `similarity.py` design (profile ↔ asset-archetype collaborative-style scoring).
- **D-03:** Cross-asset-class normalization uses z-score/percentile ranking computed **within each asset class's own universe** before combining into the composite — avoids unfairly comparing raw crypto volatility to gold volatility. Resolves the "cross-asset-class factor-weight normalization" question flagged as unresearched since Phase 2 (Phase 2 CONTEXT D-09).

### Universe & asset selection
- **D-04:** Recommendable universe is a fixed, curated list per asset class (config/constant, not live-pulled index membership) — e.g. an S&P100-ish stock subset, ~10-20 popular ETFs, top 10-15 crypto by market cap, gold (GC=F/GLD), major forex pairs. Keeps compute/rate-limit cost bounded and predictable on the free tier.
- **D-05:** Shortlist shows top N **per asset class** (e.g. top 3 stocks + top 3 ETFs + top 3 crypto + gold + top forex pairs), not one flat top-10-15 across all classes — guarantees every supported asset class is represented in the ranked list rather than letting scoring dominance from one class crowd out others.

### Explanation generation
- **D-06:** The required one-sentence reason (REC-03) is generated by a deterministic template driven by whichever sub-factor(s) contributed most to that asset's score (e.g. "Strong momentum and low volatility match your Moderate risk profile."). No LLM call. Must stay directly traceable to the visible sub-factor breakdown from D-01/D-02, not generic per-asset-class boilerplate.

### Search & asset drill-in
- **D-07:** REC-04 search is direct ticker lookup via the existing `fetch_ohlcv` chokepoint (Phase 1/2 caching infra) — not fuzzy name-to-ticker matching. Works for any valid yfinance symbol, not limited to the curated D-04 universe.
- **D-08:** When a searched asset has insufficient price history to compute features reliably (e.g. new IPO, thin data), still render the historical price chart but show "insufficient data for scoring" instead of a composite score/breakdown — don't reject the search outright, since REC-04 requires viewing any supported asset.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Project scope & requirements
- `.planning/PROJECT.md` — core value, constraints ($0 budget, free-tier-only), Key Decisions table (LLM reranks/annotates but never replaces the deterministic score)
- `.planning/REQUIREMENTS.md` — REC-01, REC-02, REC-03, REC-04 requirement definitions and traceability
- `.planning/ROADMAP.md` §"Phase 3: Deterministic Recommendation Engine" — goal, success criteria, dependencies (depends on Phase 2)

### Architecture & research (directly informed this discussion)
- `.planning/research/ARCHITECTURE.md` — see especially: `recommendation/` module structure (`factor_scoring.py`, `similarity.py`, `engine.py`), the `final_score = w1*factor_score + w2*similarity_score` weighted-hybrid formula, "recommendation engine has no Gemini/agent dependency" constraint, Anti-Pattern guidance that the agent (Phase 5) may only reorder/explain within already-scored candidates — never introduce new candidates or change factor scores
- `.planning/research/FEATURES.md` — see especially: "Score/rating transparency" table-stakes entry (composite score needs a visible sub-factor breakdown), "Cross-asset-class unified experience" complexity note (normalizing crypto/forex/gold/equity volatility regimes into one factor-scoring framework — informs D-03)
- `.planning/research/PITFALLS.md` — free-tier/caching pitfalls carried forward from Phase 1/2

### Prior phase artifacts (inputs this phase consumes)
- `.planning/phases/02-investor-profile-feature-engineering-foundation/02-CONTEXT.md` — profile field design (D-01–D-08: risk tolerance 3-level, time horizon buckets, ~10-category sectors, asset-type multi-select) that this phase's profile-fit sub-score consumes directly; D-09 flags the cross-asset normalization question this phase's D-03 resolves
- `src/features/technical.py`, `src/features/feature_frame.py` — the point-in-time feature computation this phase's factor scoring consumes (returns, volatility, SMA, RSI at minimum)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/auth/session.py` `require_auth()` — call first (and only) on any new recommendation-list/search page, per established Phase 1 pattern.
- `src/data/prices.py` `fetch_ohlcv` — the single chokepoint for all price data, including D-07's search/drill-in lookups. Never call yfinance directly.
- `src/features/feature_frame.py` `assemble_feature_frame()` and `src/features/technical.py` (`compute_returns`, `compute_volatility`, `compute_sma`, `compute_rsi`) — the feature inputs the factor scorer consumes. Recommendation engine code must call these, not reimplement indicator math.
- Profile data from Supabase (`profiles` table, extended in Phase 2) — read via the existing Supabase client pattern (`src/data/supabase_client.py`) for the profile-fit sub-score.

### Established Patterns
- Single-chokepoint pattern: all yfinance access routes through `src/data/cache.py` → `src/data/prices.py`. The new `src/recommendation/` module must consume prices/features only via existing modules, staying I/O-free like `src/features/` (per ARCHITECTURE.md's `recommendation/engine.py` design goal of zero network/LLM calls).
- `require_auth()`-first pattern on every page.

### Integration Points
- New `src/recommendation/` package (`factor_scoring.py`, `similarity.py`, `engine.py` per ARCHITECTURE.md's proposed structure), pure functions of (profile, feature set) → ranked list, independently unit-testable with no network/LLM calls.
- New recommendation-list page and search/drill-in page registered in `src/app.py`'s `st.navigation`, following the `home.py`/`login.py`/`profile.py` pattern.

</code_context>

<specifics>
## Specific Ideas

- The visible sub-factor breakdown (D-01) and the template-based explanation (D-06) must be traceable to the *same* underlying sub-scores — not two independently-computed things that could drift out of sync.
- Per-asset-class top-N shortlist (D-05) was chosen specifically so no single asset class (e.g. stocks dominating on raw factor strength) crowds out representation from crypto/gold/forex/ETFs.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope. No scope-creep suggestions came up during this session.

### Reviewed Todos (not folded)
None reviewed — `todo.match-phase` was not run with matches surfaced during this session.

</deferred>

---

*Phase: 3-Deterministic Recommendation Engine*
*Context gathered: 2026-08-04*
