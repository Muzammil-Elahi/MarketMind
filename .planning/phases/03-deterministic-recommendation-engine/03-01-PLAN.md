---
phase: 03-deterministic-recommendation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/recommendation/__init__.py
  - src/recommendation/universe.py
  - src/recommendation/factor_scoring.py
  - src/pages/_universe_loader.py
  - tests/test_recommendation_universe.py
  - tests/test_recommendation_factor_scoring.py
  - tests/test_universe_loader.py
autonomous: true
requirements: [REC-01, REC-02, REC-04]
must_haves:
  truths:
    - "The curated universe (D-04) is a fixed, static per-asset-class ticker list (STOCK_UNIVERSE/ETF_UNIVERSE/CRYPTO_UNIVERSE/GOLD_UNIVERSE/FOREX_UNIVERSE) defined in src/recommendation/universe.py, never live-pulled from an index/market-cap source."
    - "Every within-class normalization in factor_scoring.py groups by asset_class via groupby().transform() (D-03) — changing one asset class's raw factor values never changes another asset class's computed percentile (Pitfall 1's core regression, satisfies REC-02 adjacency: each asset's breakdown is self-contained and independent of other assets' values)."
    - "A within-class group of fewer than MIN_GROUP_SIZE (3) members falls back to DEFAULT_PERCENTILE_FALLBACK (0.5) instead of producing NaN/inf from a degenerate rank/z-score computation (Pitfall 2)."
    - "src/recommendation/universe.py and factor_scoring.py import only pandas (no numpy, no streamlit, no yfinance, no sqlite3) — zero-I/O, matching src/features/technical.py's module-boundary discipline."
    - "infer_asset_class(ticker) deterministically classifies any free-text ticker string into one of the 5 supported asset classes using yfinance's own suffix convention (=X forex, -USD crypto, =F or a known gold ticker for Gold, else Stocks) — used by REC-04's search path to know which curated class to normalize a searched asset against."
    - "load_asset_feature_row/fetch_scorable_row in src/pages/_universe_loader.py apply the MIN_HISTORY_ROWS (20) gate — an asset with fewer than 20 non-NaN assemble_feature_frame rows is never handed to the scoring layer (REC-02 empty: no composite score/breakdown is computed for insufficient-data assets; fetch_scorable_row separately reports not_found (D-07, fetch failed or empty DataFrame) vs insufficient_data (D-08, fetch succeeded but too little history) so callers can render the correct distinct UI state)."
    - "src/pages/_universe_loader.py is the only place outside src/recommendation/ that calls fetch_ohlcv/assemble_feature_frame per curated/searched ticker — both src/pages/recommendations.py (Plan 06) and src/pages/search.py (Plan 07) reuse it rather than duplicating the fetch-and-assemble loop."
  artifacts:
    - path: "src/recommendation/universe.py"
      provides: "STOCK_UNIVERSE, ETF_UNIVERSE, CRYPTO_UNIVERSE, GOLD_UNIVERSE, FOREX_UNIVERSE, ASSET_CLASSES, ASSET_CLASS_TICKERS, ASSET_CLASS_SECTORS, MIN_HISTORY_ROWS, infer_asset_class(ticker) -> str"
    - path: "src/recommendation/factor_scoring.py"
      provides: "compute_momentum_score(universe_df), compute_volatility_score(universe_df), compute_quality_score(universe_df), MIN_GROUP_SIZE, DEFAULT_PERCENTILE_FALLBACK -- all pure, zero-I/O, within-class-normalized"
    - path: "src/pages/_universe_loader.py"
      provides: "fetch_scorable_row(ticker, asset_class, sector), load_asset_feature_row(ticker, asset_class, sector), load_universe_rows(tickers_with_metadata) -- the shared I/O loop reused by both new pages"
  key_links:
    - from: "src/pages/_universe_loader.py"
      to: "src/data/prices.py, src/features/feature_frame.py"
      via: "fetch_scorable_row calls fetch_ohlcv(ticker) then assemble_feature_frame(df) -- never reimplements fetch or feature math"
      pattern: "fetch_ohlcv\\("
    - from: "src/recommendation/factor_scoring.py"
      to: "pandas groupby"
      via: "every percentile/normalization call includes .groupby(\"asset_class\") before .transform(...) (D-03)"
      pattern: "groupby\\(.asset_class.\\)"
  prohibitions:
    - "MUST NOT call yf.Ticker(...).info or any live sector/fundamentals lookup anywhere in universe.py -- sector tags are hardcoded static metadata (Pitfall 3)."
    - "MUST NOT import streamlit, yfinance, or sqlite3 in universe.py or factor_scoring.py -- both stay zero-I/O pure-pandas modules (recommendation engine has no Gemini/agent/network dependency, prohibition against any LLM/network call to compute a score)."
---

<objective>
Build the curated cross-asset-class universe definition (D-04), the deterministic within-class factor-normalization functions (D-03, momentum/volatility/quality), and the shared I/O loader that fetches+assembles a scorable feature row for one asset via the existing `fetch_ohlcv`/`assemble_feature_frame` chokepoints, applying the D-08 minimum-history gate.

Purpose: this is the data foundation every later plan in this phase reads from — the composite scorer (Plan 05), the recommendation list page (Plan 06), and the search/drill-in page (Plan 07) all consume `universe.py`'s ticker lists/`infer_asset_class` and `factor_scoring.py`'s within-class percentiles, and both pages reuse `_universe_loader.py` instead of duplicating a fetch-and-assemble loop.

Output: `src/recommendation/universe.py`, `src/recommendation/factor_scoring.py`, `src/pages/_universe_loader.py`, plus three test files — all green, zero network calls in the tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md
@.planning/phases/03-deterministic-recommendation-engine/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curated universe + within-class factor scoring</name>
  <files>src/recommendation/__init__.py, src/recommendation/universe.py, src/recommendation/factor_scoring.py, tests/test_recommendation_universe.py, tests/test_recommendation_factor_scoring.py</files>
  <read_first>
    src/features/technical.py (module-docstring/zero-I/O discipline to replicate exactly)
    src/pages/profile.py lines 58-72 (SECTORS/ASSET_TYPE_OPTIONS constant-list pattern; STOCK_UNIVERSE sector strings must match SECTORS exactly, ASSET_CLASSES values must match ASSET_TYPE_OPTIONS exactly)
    .planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md (Code Examples "Curated universe structure" lines 370-413 for the 5 ticker lists verbatim; Pattern 2 lines 218-245 for groupby().transform(); Pitfall 1 lines 342-347; Pitfall 2 lines 349-354)
    .planning/phases/03-deterministic-recommendation-engine/03-PATTERNS.md (universe.py and factor_scoring.py sections)
  </read_first>
  <behavior>
    - `infer_asset_class("EURUSD=X")` returns `"Forex"`; `infer_asset_class("BTC-USD")` returns `"Crypto"`; `infer_asset_class("GC=F")` and `infer_asset_class("GLD")` return `"Gold"`; `infer_asset_class("AAPL")` returns `"Stocks"` (default fallback — Stocks/ETFs share no distinguishing ticker suffix, Stocks is used as the broader default).
    - `ASSET_CLASSES == ["Stocks", "ETFs", "Crypto", "Gold", "Forex"]` (matches `src/pages/profile.py`'s `ASSET_TYPE_OPTIONS` order exactly).
    - No ticker appears in more than one of the 5 universe lists; every `STOCK_UNIVERSE` sector string is a member of `src/pages/profile.py`'s `SECTORS` list.
    - `compute_momentum_score`/`compute_volatility_score`/`compute_quality_score(universe_df)` each return a `pd.Series` aligned to `universe_df.index`, values in the closed range `[0, 1]`.
    - Within-class isolation (Pitfall 1 regression): for a synthetic `universe_df` with one artificially high-volatility "Crypto" row and a separate "Gold" group of >=3 rows, changing the Crypto row's raw `volatility_20` value must not change any Gold row's computed `compute_volatility_score` output.
    - Degenerate group fallback (Pitfall 2 regression): a synthetic `asset_class` group with fewer than `MIN_GROUP_SIZE` (3) rows produces `DEFAULT_PERCENTILE_FALLBACK` (0.5) for every row in that group on every factor score, never `NaN`/`inf`.
    - `compute_volatility_score` is the *inverse* of the raw `volatility_20` within-class percentile (lower raw volatility -> higher score, "stability"-style reading) — for a synthetic 2-class universe where one Stocks row has strictly lower `volatility_20` than every other Stocks row, that row's `compute_volatility_score` is strictly the highest in its class.
  </behavior>
  <action>
    Create `src/recommendation/__init__.py` as a package marker with a docstring stating this package is pure/zero-I/O (mirrors `src/features/__init__.py`'s pattern), imports only `pandas`/`numpy`, and never fetches its own data.

    Create `src/recommendation/universe.py` importing only `from __future__ import annotations` (optional) — no `pandas`/`numpy`/`streamlit`/`yfinance` imports needed here, this file is pure Python literals. Define `STOCK_UNIVERSE` (list of `(ticker, sector)` tuples), `ETF_UNIVERSE`, `CRYPTO_UNIVERSE` (list of tickers), `GOLD_UNIVERSE = ["GC=F", "GLD"]`, `FOREX_UNIVERSE` — copy RESEARCH.md's "Curated universe structure" code example verbatim (24 stocks across the 10 `profile.py` sectors, 12 ETFs, 12 crypto, 2 gold, 8 forex pairs). Define `ASSET_CLASSES = ["Stocks", "ETFs", "Crypto", "Gold", "Forex"]`. Define `ASSET_CLASS_TICKERS = {"Stocks": [t for t, _sector in STOCK_UNIVERSE], "ETFs": ETF_UNIVERSE, "Crypto": CRYPTO_UNIVERSE, "Gold": GOLD_UNIVERSE, "Forex": FOREX_UNIVERSE}`. Define `ASSET_CLASS_SECTORS = dict(STOCK_UNIVERSE)` (a ticker -> sector map; only meaningful for Stocks, `.get(ticker)` returns `None` for every other class). Define `MIN_HISTORY_ROWS = 20` (A4 — the longest lookback window among `returns`/`volatility_20`/`sma_20`/`rsi_14` is 20, per RESEARCH.md Pattern 6). Define `infer_asset_class(ticker: str) -> str` per the `<behavior>` rules above, checking `ticker.endswith("=X")` -> `"Forex"`, then `ticker.endswith("-USD")` -> `"Crypto"`, then `ticker in GOLD_UNIVERSE or ticker.endswith("=F")` -> `"Gold"`, else `"Stocks"`.

    Create `src/recommendation/factor_scoring.py` importing `pandas as pd` only (no numpy — `.rank(pct=True)` is pure pandas). Define `MIN_GROUP_SIZE = 3` and `DEFAULT_PERCENTILE_FALLBACK = 0.5` (Pitfall 2). Define a private `_safe_group_percentile(universe_df: pd.DataFrame, column: str) -> pd.Series` that computes `group_sizes = universe_df.groupby("asset_class")[column].transform("size")` and `pct = universe_df.groupby("asset_class")[column].transform(lambda s: s.rank(pct=True))`, then returns `pct.where(group_sizes >= MIN_GROUP_SIZE, DEFAULT_PERCENTILE_FALLBACK)`. Define `compute_momentum_score(universe_df) -> pd.Series` returning `_safe_group_percentile(universe_df, "returns")`. Define `compute_volatility_score(universe_df) -> pd.Series` returning `1 - _safe_group_percentile(universe_df, "volatility_20")` (inverted — low raw volatility scores high). Define `compute_quality_score(universe_df) -> pd.Series`: first compute a raw per-row `quality_raw = 1 - (universe_df["rsi_14"] - 50).abs() / 50` (RSI near neutral 50 reads as highest "quality," per RESEARCH Open Question 2's resolution to derive quality from existing technicals, not a new fundamentals data source), assign it as a temporary column, then return `_safe_group_percentile` of that temporary column (percentile-ranked within class like the other two factors, for consistency). No error handling/try-except anywhere in this file (pure/zero-I/O discipline, per PATTERNS.md).

    Create `tests/test_recommendation_universe.py` and `tests/test_recommendation_factor_scoring.py` covering every `<behavior>` bullet above, following `tests/test_features_technical.py`'s synthetic-data style (small deterministic `pd.DataFrame`/list fixtures, no mocking, no network).
  </action>
  <acceptance_criteria>
    - `ASSET_CLASSES`, `ASSET_CLASS_TICKERS`, `ASSET_CLASS_SECTORS`, `MIN_HISTORY_ROWS`, `infer_asset_class` are all defined in `src/recommendation/universe.py`.
    - `compute_momentum_score`, `compute_volatility_score`, `compute_quality_score`, `MIN_GROUP_SIZE`, `DEFAULT_PERCENTILE_FALLBACK` are all defined in `src/recommendation/factor_scoring.py`.
    - `pytest tests/test_recommendation_universe.py tests/test_recommendation_factor_scoring.py -x -q` passes.
    - No `import streamlit`, `import yfinance`, or `import sqlite3` anywhere in `src/recommendation/universe.py` or `factor_scoring.py`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_recommendation_universe.py tests/test_recommendation_factor_scoring.py -x -q &amp;&amp; ! grep -Eq "import (streamlit|yfinance|sqlite3)" src/recommendation/universe.py src/recommendation/factor_scoring.py</automated>
  </verify>
  <done>`universe.py` defines the fixed 5-class curated ticker universe plus `infer_asset_class`, and `factor_scoring.py` computes momentum/volatility/quality sub-scores strictly within each asset class via `groupby().transform()`, with a degenerate-group fallback — proven by a passing test suite covering both the within-class-isolation and small-group-fallback regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared universe data loader (fetch + assemble + D-08 gate)</name>
  <files>src/pages/_universe_loader.py, tests/test_universe_loader.py</files>
  <read_first>
    src/data/prices.py (the only sanctioned `fetch_ohlcv` import path)
    src/features/feature_frame.py (`assemble_feature_frame` signature/output columns)
    tests/test_ticker_validation.py (unittest.mock.patch style for mocking `fetch_ohlcv` — mirror this exact patching approach, patching the name as imported into `src.pages._universe_loader`, not `src.data.prices`)
    .planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md (Pitfall 4, lines 362-366; Pattern 6, lines 316-320)
  </read_first>
  <behavior>
    - `fetch_scorable_row(ticker, asset_class, sector)` returns `{"status": "not_found"}` when `fetch_ohlcv` raises, or when it returns an empty `DataFrame` (D-07, mirrors `validate_ticker`'s existing empty-DataFrame check).
    - `fetch_scorable_row(...)` returns `{"status": "insufficient_data", "chart_df": df}` when `fetch_ohlcv` succeeds with a non-empty `df` but `assemble_feature_frame(df).dropna()` has fewer than `MIN_HISTORY_ROWS` (20) rows (D-08, Pitfall 4).
    - `fetch_scorable_row(...)` returns `{"status": "ok", "chart_df": df, "feature_row": {"ticker": ..., "asset_class": ..., "sector": ..., "returns": ..., "volatility_20": ..., "rsi_14": ...}}` when at least `MIN_HISTORY_ROWS` non-NaN feature rows exist — `feature_row`'s numeric values come from the *last* (most recent) row of the dropna'd feature frame.
    - `load_asset_feature_row(ticker, asset_class, sector)` returns the `feature_row` dict when `fetch_scorable_row` status is `"ok"`, else `None` (thin wrapper, coarser than `fetch_scorable_row` — used by the recommendations page which only needs scorable/unscorable, not the D-07/D-08 distinction).
    - `load_universe_rows(tickers_with_metadata: list[tuple[str, str, str | None]])` calls `load_asset_feature_row` once per entry and returns `(scorable_rows: list[dict], unscorable_tickers: list[str])`.
  </behavior>
  <action>
    Create `src/pages/_universe_loader.py` with a module docstring explaining this file performs I/O (unlike `src/recommendation/`) and is deliberately located under `src/pages/` rather than `src/recommendation/` for that reason — both `src/pages/recommendations.py` and `src/pages/search.py` import from here rather than duplicating the fetch-and-assemble loop. Import `from src.data.prices import fetch_ohlcv`, `from src.features.feature_frame import assemble_feature_frame`, `from src.recommendation.universe import MIN_HISTORY_ROWS`. Implement `fetch_scorable_row`, `load_asset_feature_row`, `load_universe_rows` exactly per `<behavior>` above — wrap the `fetch_ohlcv` call in `try/except Exception` returning `{"status": "not_found"}` on any exception (mirrors `validate_ticker`'s fail path, but D-07's search context treats a hard failure as "not found" rather than `validate_ticker`'s form-context fail-open).

    Create `tests/test_universe_loader.py` patching `src.pages._universe_loader.fetch_ohlcv` (the name as imported into this module, per `tests/test_ticker_validation.py`'s exact patching convention) with `unittest.mock.patch`, covering every `<behavior>` bullet with synthetic OHLCV DataFrames of varying row counts (>=20 rows with real Close-price variation for the "ok" case so `volatility_20`/`rsi_14` are non-NaN, <20 rows for the "insufficient_data" case, an empty `DataFrame` and a `side_effect=Exception(...)` for the two "not_found" cases).
  </action>
  <acceptance_criteria>
    - `fetch_scorable_row`, `load_asset_feature_row`, `load_universe_rows` are all defined in `src/pages/_universe_loader.py`.
    - `pytest tests/test_universe_loader.py -x -q` passes.
    - No live network call occurs in the test file (only `fetch_ohlcv` is ever mocked, never called for real).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_universe_loader.py -x -q</automated>
  </verify>
  <done>`src/pages/_universe_loader.py` fetches and assembles one asset's feature row via the existing chokepoints, applies the D-08 minimum-history gate, and distinguishes D-07 (not found) from D-08 (insufficient data) so callers can render the correct state — proven by a fully-mocked unit test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Free-text/curated ticker string -> `fetch_ohlcv` chokepoint | Untrusted string (a search query, in later plans) crosses into the existing cached data-fetch path here |
| `src/pages/_universe_loader.py` <-> `src/recommendation/` | I/O boundary — the loader hands only plain-dict feature rows into the zero-I/O `recommendation/` package, never a live DataFrame or fetch call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-01 | Tampering | Ticker string -> `fetch_ohlcv` (via `_universe_loader.py`, exercised fully by Plan 07's search page) | high | mitigate | This plan's loader calls only `fetch_ohlcv(ticker)` — a plain function argument, never string-interpolated into SQL/shell/query text; `src/data/cache.py`'s existing `?`-bound-parameter discipline (T-01-03) is inherited unchanged, no new SQL path is introduced |
| T-03-05 | Denial of Service (shared rate-limit budget) | Recommendation/search page's per-ticker fetch loop across the curated universe | low | accept | Already mitigated by `fetch_ohlcv`'s existing `st.cache_data(ttl=CACHE_TTL_SECONDS)` layer (Phase 1) — this loader calls `fetch_ohlcv` per ticker, never bypassing it, so repeat loads within the TTL window hit cache, not live yfinance |
| T-03-SC-1 | Tampering (supply chain) | `numpy` install | medium | mitigate | Owned by Plan 03 — blocking human-verify checkpoint before pin |
| T-03-SC-2 | Tampering (supply chain) | `plotly` install | medium | mitigate | Owned by Plan 03 — blocking human-verify checkpoint before pin |
| T-03-03 | Spoofing / Elevation of Privilege | `src/pages/recommendations.py`/`search.py` (`require_auth()` gate) | high | mitigate | Owned by Plans 06/07 |
| T-03-04 | Tampering (business-rule integrity) | Profile-driven hard-exclude filter | medium | mitigate | Owned by Plan 02 (`profile_fit.is_excluded`) / Plan 05 (`engine.py` integration) — not touched by this plan |
</threat_model>

<verification>
`pytest tests/test_recommendation_universe.py tests/test_recommendation_factor_scoring.py tests/test_universe_loader.py -x -q` passes; the negative grep confirms zero I/O imports in `universe.py`/`factor_scoring.py`; no live network call occurs in any test in this plan (loader tests mock `fetch_ohlcv` exclusively).
</verification>

<success_criteria>
The curated universe, within-class factor normalization, and shared fetch-assemble-gate loader all exist, are zero-network in their own test suites, and are ready for Plan 05's `engine.py` and Plans 06/07's pages to consume with no rework.
</success_criteria>

<output>
Create `.planning/phases/03-deterministic-recommendation-engine/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `STOCK_UNIVERSE`, `ETF_UNIVERSE`, `CRYPTO_UNIVERSE`, `GOLD_UNIVERSE`, `FOREX_UNIVERSE`, `ASSET_CLASSES`, `ASSET_CLASS_TICKERS`, `ASSET_CLASS_SECTORS`, `MIN_HISTORY_ROWS`, `infer_asset_class(ticker)` — `src/recommendation/universe.py`
- `compute_momentum_score(universe_df)`, `compute_volatility_score(universe_df)`, `compute_quality_score(universe_df)`, `MIN_GROUP_SIZE`, `DEFAULT_PERCENTILE_FALLBACK` — `src/recommendation/factor_scoring.py`
- `fetch_scorable_row(ticker, asset_class, sector)`, `load_asset_feature_row(ticker, asset_class, sector)`, `load_universe_rows(tickers_with_metadata)` — `src/pages/_universe_loader.py`
</content>
