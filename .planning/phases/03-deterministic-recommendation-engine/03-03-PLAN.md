---
phase: 03-deterministic-recommendation-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [REC-01, REC-02]
must_haves:
  truths:
    - "numpy==2.3.4 and plotly==5.24.1 are both pinned in requirements.txt after this plan, alongside every pre-existing Phase 1/2 entry, unmodified and unreordered."
    - "Neither package is installed/pinned before a human explicitly approves its legitimacy via the blocking checkpoint (numpy is RESEARCH-audited SUS due to a package-legitimacy tool metadata gap; plotly was not covered by 03-RESEARCH.md's Package Legitimacy Audit table at all and is treated as [ASSUMED] per the Package Legitimacy Gate's fallback policy)."
  artifacts:
    - path: "requirements.txt"
      provides: "numpy==2.3.4 (src/recommendation/similarity.py's cosine-similarity dependency), plotly==5.24.1 (the sub-factor breakdown and price-history charts' st.plotly_chart dependency, per 03-UI-SPEC.md's Design System row)"
  key_links: []
  prohibitions:
    - "MUST NOT install or pin either package before its checkpoint is explicitly approved by a human -- never auto-approvable regardless of workflow.auto_advance/yolo mode, per the Package Legitimacy Gate."
---

<objective>
Add and pin the two new third-party dependencies this phase introduces -- `numpy` (cosine-similarity math for `similarity.py`, Plan 04) and `plotly` (the sub-factor breakdown and price-history charts `03-UI-SPEC.md` mandates, Plan 04's `components/charts.py`) -- behind blocking human-verify legitimacy checkpoints.

Purpose: `numpy` was RESEARCH-audited and flagged `SUS` by the automated package-legitimacy tool (a known tool-metadata-gap false positive per 03-RESEARCH.md's override note, but still procedurally gated, not silently overridden). `plotly` was never covered by 03-RESEARCH.md's Package Legitimacy Audit table at all -- 03-UI-SPEC.md introduces it as a binding Design System requirement that RESEARCH.md's research pass missed, so per the Package Legitimacy Gate's fallback policy it is treated as `[ASSUMED]` and gated here for the first time. Both packages are prerequisites for Plan 04 (which imports `numpy` in `similarity.py` and `plotly` in `components/charts.py`).

Output: `requirements.txt` with `numpy==2.3.4` and `plotly==5.24.1` pinned, both installed and import-verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md
@.planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Verify numpy and plotly package legitimacy before install</name>
  <action>Pause for human verification of both numpy's and plotly's package legitimacy before Task 2 installs either -- this checkpoint is mandatory per the Package Legitimacy Gate and is never auto-approvable, even though this project's workflow.mode is yolo.</action>
  <what-built>Nothing yet -- this gate precedes pinning two new runtime dependencies. `numpy==2.3.4`: 03-RESEARCH.md's automated Package Legitimacy Audit flagged it `SUS` with reasons `unknown-downloads`/`no-repository` -- a known limitation of the audit tool's PyPI metadata source for a foundational package already present transitively via `pandas` (already vetted, used since Phase 2), not a genuine risk signal, per RESEARCH.md's explicit override note. `plotly==5.24.1`: this package was never run through the Package Legitimacy Gate at all -- 03-RESEARCH.md's audit table only covers `numpy`; 03-UI-SPEC.md introduces `plotly` as a binding chart-library requirement (matching CLAUDE.md's Supporting Libraries table) that the research pass did not anticipate, so per the Gate's fallback policy it is treated as `[ASSUMED]` and verified here for the first time.</what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/numpy/ and confirm the package page shows NumPy Developers as the maintaining organization, a long release history (NumPy is a foundational, decades-old scientific-Python package), and a linked source repository at https://github.com/numpy/numpy with active commit/release history.
    2. Visit https://pypi.org/project/plotly/ and confirm the package page shows Plotly as the maintaining organization, a release history matching the versions listed by `pip index versions plotly` (latest 6.x line, with a 5.24.1 entry in the 5.x line), and a linked source repository at https://github.com/plotly/plotly.py with active commit/release history.
    3. Confirm the exact package names to be installed are `numpy` and `plotly` -- not lookalikes such as `numpyi`, `python-numpy`, `plotly-express`, or `plotlyjs`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with both installs, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install and pin numpy + plotly</name>
  <files>requirements.txt</files>
  <read_first>
    requirements.txt (current 7-line pinned manifest from Phase 1/2 -- append, do not reorder or remove existing lines)
    .planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md (Standard Stack / Installation block, lines 74-105 -- numpy version rationale)
    .planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md (Design System row, line 25 -- plotly rationale, "latest 5.x")
  </read_first>
  <action>
    Run `pip install numpy==2.3.4` and `pip install plotly==5.24.1`. Add both as new lines to `requirements.txt` (append after the existing seven lines; do not touch `requirements-dev.txt`). Pin the exact already-installed/tested `numpy==2.3.4` (matches this dev environment's transitively-installed version, per RESEARCH.md's Installation note) rather than jumping to the newest PyPI release sight-unseen. Pin `plotly==5.24.1` -- the latest release within the 5.x line CLAUDE.md's Supporting Libraries table specifies ("latest 5.x"), not the newer 6.x major line (an unvetted breaking-change surface this phase's research did not evaluate). After both installs, run `python -c "import numpy; import plotly"` to confirm both imports succeed before Plan 04 writes any code depending on them.
  </action>
  <acceptance_criteria>
    - `requirements.txt` contains exactly the line `numpy==2.3.4` and exactly the line `plotly==5.24.1`, in addition to the seven pre-existing Phase 1/2 lines (unchanged, unreordered).
    - `python -c "import numpy; import plotly"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "numpy==2.3.4" requirements.txt &amp;&amp; grep -c "plotly==5.24.1" requirements.txt &amp;&amp; grep -c "streamlit==1.59.2" requirements.txt &amp;&amp; python -c "import numpy; import plotly"</automated>
  </verify>
  <done>`numpy==2.3.4` and `plotly==5.24.1` are installed and pinned in `requirements.txt` alongside the seven pre-existing entries, with no existing line removed or reordered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev machine <-> PyPI (`numpy`/`plotly` installs) | Untrusted third-party package supply chain; one `SUS`-flagged by the automated legitimacy gate, one never previously audited |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-SC-1 | Tampering (supply chain) | `numpy==2.3.4` install | medium | mitigate | Task 1's blocking human-verify checkpoint confirms the package's identity/maintainer/repository before any install runs; never auto-approvable |
| T-03-SC-2 | Tampering (supply chain) | `plotly==5.24.1` install | medium | mitigate | Task 1's blocking human-verify checkpoint confirms the package's identity/maintainer/repository before any install runs -- first legitimacy check this package has ever received in this project, since 03-RESEARCH.md's audit table omitted it |
</threat_model>

<verification>
`python -c "import numpy; import plotly"` exits 0 after Task 2; the grep checks confirm both new lines are pinned in `requirements.txt` with no pre-existing line disturbed; the blocking checkpoint in Task 1 confirms human sign-off preceded both installs.
</verification>

<success_criteria>
Both `numpy` and `plotly` are installed, pinned, and import-verified, with their legitimacy explicitly confirmed by a human before installation -- Plan 04 can now safely import both with no further gating.
</success_criteria>

<output>
Create `.planning/phases/03-deterministic-recommendation-engine/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `numpy==2.3.4`, `plotly==5.24.1` entries — `requirements.txt`
</content>
