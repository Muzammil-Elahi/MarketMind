---
phase: 03-deterministic-recommendation-engine
plan: 07
type: execute
wave: 4
depends_on: ["03-05", "03-04", "03-01"]
files_modified:
  - src/pages/search.py
  - tests/test_recommendation_search.py
autonomous: true
requirements: [REC-04]
must_haves:
  truths:
    - "render_search_page() calls require_auth() first and only, before any other statement (D-04 pattern)."
    - "resolve_search_result(ticker, profile) calls fetch_ohlcv only through src.pages._universe_loader.fetch_scorable_row -- it never imports yfinance directly and never builds a second, independently-implemented data-fetch path (D-07)."
    - "REC-04 empty: resolve_search_result('', profile) and resolve_search_result('   ', profile) return {'status': 'empty_query'} without calling fetch_ohlcv at all -- proven by a mock-call-count assertion, not just a return-value check."
    - "D-07: an invalid/unresolvable ticker (fetch_ohlcv raises, or returns an empty DataFrame) yields {'status': 'not_found', ...} -- the page renders the exact 'We couldn't find \"{ticker}\" -- check the symbol and try again.' error copy, never a crash."
    - "D-08: a valid ticker with fewer than MIN_HISTORY_ROWS non-NaN feature rows yields {'status': 'insufficient_data', 'chart_df': ...} -- the page renders the historical price chart plus the exact 'Insufficient data for scoring' badge and explanatory copy, never rejecting the search outright and never a fabricated/zero-filled score."
    - "REC-04 adjacency (single source of truth): for a ticker already present in the curated universe, resolve_search_result's returned composite_score is numerically identical to the composite_score src.recommendation.engine.build_recommendations computes for that same ticker against the same synthetic peer data -- proven by a test that calls both paths with identical mocked inputs and asserts equality, never a separately-implemented scoring formula."
    - "Search deliberately does NOT apply profile_fit's hard-exclude filter (resolve_search_result calls score_universe with apply_hard_exclude=False) -- a searched asset outside the user's preferred_asset_types/excluded_sectors still returns a real score, since D-07 explicitly frames search as an escape hatch for viewing any asset regardless of curated-universe/preference restrictions (RESEARCH.md Open Question 1's resolution), which is distinct from the ranked recommendation list's prohibition #3 hard-exclude behavior."
    - "REC-04 ordering: the search result view renders exactly one asset's detail (ticker, chart, and -- when scorable -- composite score/breakdown/explanation) -- there is no multi-result list and therefore no sort/tie-break concern on this page."
  artifacts:
    - path: "src/pages/search.py"
      provides: "render_search_page() -- the require_auth()-gated free-text ticker search/drill-in page; resolve_search_result(ticker, profile) -> dict -- the testable core search-and-score function"
  key_links:
    - from: "src/pages/search.py"
      to: "src/pages/_universe_loader.py"
      via: "resolve_search_result calls fetch_scorable_row(ticker, ...) for the searched ticker and load_universe_rows(...) for its inferred asset class's curated peers"
      pattern: "fetch_scorable_row\\("
    - from: "src/pages/search.py"
      to: "src/recommendation/engine.py"
      via: "resolve_search_result calls score_universe(profile, combined_df, apply_hard_exclude=False) -- the identical scoring function build_recommendations uses"
      pattern: "score_universe\\("
  prohibitions:
    - "MUST NOT call yfinance directly or build a second fetch_ohlcv-equivalent -- every price lookup goes through src.pages._universe_loader's fetch_scorable_row."
    - "MUST NOT string-interpolate the searched ticker into any SQL/shell/query text -- it is passed only as a plain function argument to fetch_scorable_row (mirrors src/data/profile.py's validate_ticker discipline)."
    - "MUST NOT render a composite score/breakdown for an asset that failed the MIN_HISTORY_ROWS gate -- the insufficient_data branch renders the chart only, per D-08."
---

<objective>
Build the Search page (REC-04's user-facing surface): a free-text ticker lookup that reuses the exact same recommendation-engine scoring path as the curated ranked list, distinguishing D-07's "ticker not found" from D-08's "insufficient history" so the correct UI state renders for each, with a testable `resolve_search_result` core separated from the thin Streamlit rendering wrapper.

Purpose: REC-04's "single source of truth" requirement (no second, independently-computed scoring path) is the central design constraint here -- `resolve_search_result` reuses `engine.score_universe` directly rather than reimplementing any scoring math, and reuses the Plan 01 loader for all I/O.

Output: `src/pages/search.py` (`render_search_page()`, `resolve_search_result()`), `tests/test_recommendation_search.py` -- all green with `fetch_ohlcv` fully mocked, no live network call.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md
@.planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md
@.planning/phases/03-deterministic-recommendation-engine/03-PATTERNS.md
@.planning/phases/03-deterministic-recommendation-engine/03-05-SUMMARY.md
@.planning/phases/03-deterministic-recommendation-engine/03-04-SUMMARY.md
@.planning/phases/03-deterministic-recommendation-engine/03-01-SUMMARY.md
</context>

<interfaces>
From `src/pages/_universe_loader.py` (Plan 01): `fetch_scorable_row(ticker, asset_class, sector) -> dict` (`status` one of `"not_found"`/`"insufficient_data"`/`"ok"`, see Plan 01's `<behavior>`), `load_universe_rows(tickers_with_metadata) -> tuple[list[dict], list[str]]`.
From `src/recommendation/universe.py` (Plan 01): `infer_asset_class(ticker) -> str`, `ASSET_CLASS_TICKERS`, `ASSET_CLASS_SECTORS`.
From `src/recommendation/engine.py` (Plan 05): `score_universe(profile, universe_df, apply_hard_exclude=True) -> pd.DataFrame`.
From `src/components/charts.py` (Plan 04): `render_price_history_chart(price_df, key)`, `render_breakdown_bar_chart(sub_scores_display, key)`.
From `src/components/disclaimer.py` (Plan 04): `render_disclaimer_banner()`.
From `src/auth/session.py`: `require_auth()`.
From `src/data/profile.py`: `fetch_profile(access_token, user_id)`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_search_result -- testable core search-and-score logic</name>
  <files>src/pages/search.py, tests/test_recommendation_search.py</files>
  <read_first>
    tests/test_ticker_validation.py (mocking convention: patch the name as imported into the module under test)
    .planning/phases/03-deterministic-recommendation-engine/03-CONTEXT.md (D-07, D-08)
    .planning/phases/03-deterministic-recommendation-engine/03-RESEARCH.md (Pattern 6, lines 316-320; Open Question 1, lines 438-441 -- the "search is the explicit escape hatch" resolution this plan implements via `apply_hard_exclude=False`)
  </read_first>
  <behavior>
    - `resolve_search_result("", profile)` and `resolve_search_result("   ", profile)` both return `{"status": "empty_query"}`; a mocked `fetch_ohlcv` is never called for either input.
    - `resolve_search_result("NOTAREAL", profile)` with `fetch_scorable_row` mocked to return `{"status": "not_found"}` returns `{"status": "not_found", "ticker": "NOTAREAL"}`.
    - `resolve_search_result("NEWIPO", profile)` with `fetch_scorable_row` mocked to return `{"status": "insufficient_data", "chart_df": <df>}` returns `{"status": "insufficient_data", "ticker": "NEWIPO", "chart_df": <the same df>}`.
    - `resolve_search_result("AAPL", profile)` with `fetch_scorable_row` mocked to return `{"status": "ok", "chart_df": <df>, "feature_row": {"ticker": "AAPL", "asset_class": "Stocks", "sector": "Tech", "returns": 0.01, "volatility_20": 0.02, "rsi_14": 55.0}}` and `load_universe_rows` mocked to return a synthetic list of >=3 Stocks peer rows returns `{"status": "scored", "ticker": "AAPL", "chart_df": <df>, "composite_score": ..., "composite_score_display": ..., "sub_scores": {...}, "sub_scores_display": {...}, "explanation": ...}`.
    - Single-source-of-truth: calling `resolve_search_result("AAPL", profile)` (with AAPL's row plus N synthetic Stocks peers mocked as above) and separately calling `src.recommendation.engine.build_recommendations(profile, pd.DataFrame(peers + [AAPL's feature_row]))` with the identical peer set produce the exact same `composite_score` for AAPL (both paths call `score_universe` with the identical combined DataFrame content).
    - A searched ticker whose `sector`/`asset_class` would be hard-excluded by the profile (e.g. `excluded_sectors` containing its sector) still returns `status == "scored"` with a real `composite_score` -- `resolve_search_result` calls `score_universe(..., apply_hard_exclude=False)`.
  </behavior>
  <action>
    Create `src/pages/search.py`. Import `from src.pages._universe_loader import fetch_scorable_row, load_universe_rows`, `from src.recommendation.universe import infer_asset_class, ASSET_CLASS_TICKERS, ASSET_CLASS_SECTORS`, `from src.recommendation.engine import score_universe`, plus `pandas as pd`. Define `resolve_search_result(ticker: str, profile: dict) -> dict`: `ticker = (ticker or "").strip()`; if not `ticker`, return `{"status": "empty_query"}`. Compute `asset_class = infer_asset_class(ticker)` and `sector = ASSET_CLASS_SECTORS.get(ticker)`. Call `result = fetch_scorable_row(ticker, asset_class, sector)`. If `result["status"] == "not_found"`, return `{"status": "not_found", "ticker": ticker}`. If `result["status"] == "insufficient_data"`, return `{"status": "insufficient_data", "ticker": ticker, "chart_df": result["chart_df"]}`. Otherwise (`status == "ok"`): build `peer_tickers = [t for t in ASSET_CLASS_TICKERS[asset_class] if t != ticker]`, `peer_metadata = [(t, asset_class, ASSET_CLASS_SECTORS.get(t)) for t in peer_tickers]`, call `peer_rows, _unscorable = load_universe_rows(peer_metadata)`, build `combined_df = pd.DataFrame(peer_rows + [result["feature_row"]])`, call `scored_df = score_universe(profile, combined_df, apply_hard_exclude=False)` (the escape-hatch parameter -- search bypasses the ranked-list's hard-exclude filter, per Open Question 1's resolution), find `matching = scored_df[scored_df["ticker"] == ticker]`; if `matching.empty` return `{"status": "not_found", "ticker": ticker}` (defensive fallback, should not normally occur); else return `{"status": "scored", "ticker": ticker, "chart_df": result["chart_df"], **matching.iloc[0].to_dict()}`.

    Create `tests/test_recommendation_search.py` covering every `<behavior>` bullet, patching `src.pages.search.fetch_scorable_row` and `src.pages.search.load_universe_rows` (the names as imported into `src.pages.search`, per `tests/test_ticker_validation.py`'s convention) with `unittest.mock.patch`/`MagicMock`, and a `test_fetch_ohlcv_never_called_for_blank_query` that additionally patches `src.pages._universe_loader.fetch_ohlcv` and asserts zero calls when `resolve_search_result("", profile)` runs.
  </action>
  <acceptance_criteria>
    - `resolve_search_result` is defined in `src/pages/search.py` with the signature above.
    - `pytest tests/test_recommendation_search.py -x -q` passes.
    - No `import yfinance` anywhere in `src/pages/search.py`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_recommendation_search.py -x -q &amp;&amp; ! grep -q "import yfinance" src/pages/search.py</automated>
  </verify>
  <done>`resolve_search_result` distinguishes empty-query/not-found/insufficient-data/scored states, reuses the identical `score_universe` scoring path as the ranked list (single source of truth), and applies the `apply_hard_exclude=False` search escape hatch -- proven by a fully-mocked unit test suite including a direct cross-check against `build_recommendations`'s output for the same synthetic data.</done>
</task>

<task type="auto">
  <name>Task 2: Search page rendering -- form, D-07/D-08 branches, drill-in view</name>
  <files>src/pages/search.py</files>
  <read_first>
    src/pages/profile.py (form/submit-button pattern to replicate: `st.form` wrapping the input, `st.form_submit_button`)
    .planning/phases/03-deterministic-recommendation-engine/03-UI-SPEC.md (full Copywriting Contract for this page's exact strings)
  </read_first>
  <action>
    In `src/pages/search.py`, add `import streamlit as st`, `from src.auth.session import require_auth`, `from src.components.disclaimer import render_disclaimer_banner`, `from src.components.charts import render_breakdown_bar_chart, render_price_history_chart`. Declare module-level copy constants verbatim from the UI-SPEC Copywriting Contract: `PAGE_HEADING = "Search"`, `PAGE_SUBHEADING = "Look up any asset — stock, ETF, crypto, gold, or forex pair — even if it's not in your recommendations."`, `SEARCH_INPUT_LABEL = "Ticker Symbol"`, `SEARCH_INPUT_PLACEHOLDER = "e.g. AAPL, BTC-USD, GC=F, EURUSD=X"`, `SEARCH_BUTTON_LABEL = "Search"`, `EMPTY_STATE_MESSAGE = "Search for any asset to see its score, breakdown, and price history."`, `NOT_FOUND_TEMPLATE = 'We couldn\'t find "{ticker}" — check the symbol and try again.'`, `INSUFFICIENT_DATA_BADGE = "Insufficient data for scoring"`, `INSUFFICIENT_DATA_BODY_TEMPLATE = "We don't have enough price history for {ticker} yet to compute a score — here's what we have."`, `SCORE_LABEL_TEMPLATE = "{score}/100"`, `BREAKDOWN_HEADING = "Score Breakdown"`.

    Add `from src.data.profile import fetch_profile` to the top-level imports. Define `render_search_page() -> None`. First statement: `user = require_auth()`. `access_token = st.session_state["access_token"]`; `profile = fetch_profile(access_token, user.id) or {}`. Render `st.title(PAGE_HEADING)`, `st.write(PAGE_SUBHEADING)`, `render_disclaimer_banner()`. Read `query_ticker = st.query_params.get("ticker", "")` (pre-filled when arriving via a Recommendations-page "View Details" click, Plan 06). Wrap the input in `with st.form("search_form"):`: `ticker_input = st.text_input(SEARCH_INPUT_LABEL, value=query_ticker, placeholder=SEARCH_INPUT_PLACEHOLDER)`; `submitted = st.form_submit_button(SEARCH_BUTTON_LABEL)`. After the form, determine `active_ticker = ticker_input if (submitted or query_ticker) else ""`. If not `active_ticker.strip()`, render `st.caption(EMPTY_STATE_MESSAGE)` and `return`. Call `result = resolve_search_result(active_ticker, profile)`. Branch on `result["status"]`: `"not_found"` -> `st.error(NOT_FOUND_TEMPLATE.format(ticker=result["ticker"]))`; `"insufficient_data"` -> render `st.warning(INSUFFICIENT_DATA_BADGE)`, `st.write(INSUFFICIENT_DATA_BODY_TEMPLATE.format(ticker=result["ticker"]))`, then `render_price_history_chart(result["chart_df"], key=f"chart_{result['ticker']}")`; `"scored"` -> render the ticker as a heading, `SCORE_LABEL_TEMPLATE.format(score=result["composite_score_display"])`, `render_price_history_chart(result["chart_df"], key=f"chart_{result['ticker']}")`, `st.subheader(BREAKDOWN_HEADING)`, `render_breakdown_bar_chart(result["sub_scores_display"], key=f"breakdown_{result['ticker']}")`, and `st.write(result["explanation"])`.
  </action>
  <acceptance_criteria>
    - `render_search_page` is defined and callable with no arguments; its first executable statement calls `require_auth()`.
    - The file contains the exact strings `"Search"`, `"Ticker Symbol"`, `"Insufficient data for scoring"`.
    - The file contains a call to `resolve_search_result(`.
    - No occurrence of "buy", "sell", or "you should" (case-insensitive) in the file.
    - `python -c "import ast; ast.parse(open('src/pages/search.py').read())"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "def render_search_page" src/pages/search.py &amp;&amp; grep -c "require_auth" src/pages/search.py &amp;&amp; grep -c "Ticker Symbol" src/pages/search.py &amp;&amp; grep -c "Insufficient data for scoring" src/pages/search.py &amp;&amp; grep -c "resolve_search_result(" src/pages/search.py &amp;&amp; ! grep -Eiq "\bbuy\b|\bsell\b|you should" src/pages/search.py &amp;&amp; python -c "import ast; ast.parse(open('src/pages/search.py').read())"</automated>
  </verify>
  <done>`src/pages/search.py` renders a require_auth()-gated free-text ticker search, correctly branching on empty-query/not-found/insufficient-data/scored states with the exact UI-SPEC copy, reusing `resolve_search_result` for all scoring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Free-text search input -> `resolve_search_result` -> `fetch_scorable_row` -> `fetch_ohlcv` | The only untrusted user-supplied string this phase introduces (any prior ticker input in this codebase, e.g. `profile.py`'s holdings grid, was Phase 2's scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-01 | Tampering | Search ticker input -> `fetch_ohlcv` chokepoint | high | mitigate | `resolve_search_result` passes `ticker` only as a plain function argument to `fetch_scorable_row` (which itself only ever calls `fetch_ohlcv(ticker)` as a library call, never string-interpolated into SQL/shell/query text) — proven by this plan's own unit tests plus `src/data/cache.py`'s existing `?`-bound-parameter discipline (T-01-03), inherited unchanged, no new SQL path introduced |
| T-03-03 | Spoofing / Elevation of Privilege | `src/pages/search.py` (`require_auth()` gate) | high | mitigate | `require_auth()` called first and only (Task 2) |
| T-03-02 | Information Disclosure | Per-user profile-derived search score | medium | mitigate | `resolve_search_result` is uncached, computed fresh per call, never written to any shared/global cache |
| T-03-04 | Tampering (business-rule integrity) | `apply_hard_exclude=False` search escape hatch | low | accept | Deliberate, documented behavior difference from the ranked list (D-07's explicit "search bypasses preference restrictions" framing, not a bug) — the profile's own data (sector/asset-type preferences) is never exposed to any OTHER user, only interpreted differently for the searching user's own results |
</threat_model>

<verification>
`pytest tests/test_recommendation_search.py -x -q` passes; the grep/AST checks confirm structure, copy fidelity, and the absence of any direct yfinance import. Full end-to-end human verification happens in Plan 08's end-of-phase `<human-check>`.
</verification>

<success_criteria>
A signed-in user can search any ticker (curated or not) and see the correct one of four states (empty/not-found/insufficient-data/scored), with scored results using the exact same engine computation as the ranked list — satisfying REC-04.
</success_criteria>

<output>
Create `.planning/phases/03-deterministic-recommendation-engine/03-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `resolve_search_result(ticker, profile)`, `render_search_page()` — `src/pages/search.py`
</content>
