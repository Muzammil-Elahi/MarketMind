---
phase: 04-multi-model-prediction-walk-forward-backtesting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/prediction/__init__.py
autonomous: false
requirements: [PRED-02]
must_haves:
  truths:
    - "xgboost==3.3.0, prophet==1.2.1, and scikit-learn==1.9.0 are all pinned in requirements.txt after this plan, alongside every pre-existing Phase 1/2/3 entry, unmodified and unreordered."
    - "None of the three packages is installed/pinned before a human explicitly approves their legitimacy via the blocking checkpoint -- all three were flagged SUS by the automated package-legitimacy tool due to a known PyPI-metadata-gap limitation (04-RESEARCH.md's override note), not a genuine risk signal, but the checkpoint still runs procedurally."
    - "src/prediction/__init__.py exists as a package marker documenting the package's zero-I/O contract (no streamlit/yfinance/sqlite3 imports anywhere under src/prediction/), matching src/recommendation/__init__.py's precedent."
  artifacts:
    - path: "requirements.txt"
      provides: "xgboost==3.3.0 (quantile-regression forecasting), prophet==1.2.1 (additive-regression forecasting with native CI), scikit-learn==1.9.0 (TimeSeriesSplit for walk-forward folds) -- the three new v1 model-layer dependencies this phase introduces"
    - path: "src/prediction/__init__.py"
      provides: "The package marker every subsequent Phase 4 plan's src/prediction/*.py file lives under"
  key_links: []
  prohibitions:
    - "MUST NOT install or pin any of the three packages before Task 1's checkpoint is explicitly approved by a human -- never auto-approvable regardless of workflow.mode=yolo, per the Package Legitimacy Gate (same as 03-03-PLAN.md's numpy/plotly precedent)."
---

<objective>
Add and pin the three new third-party dependencies this phase introduces -- `xgboost` (quantile-regression forecasting, Plan 04), `prophet` (additive-regression forecasting with native confidence intervals, Plan 05), and `scikit-learn` (`TimeSeriesSplit`-based walk-forward fold generation, Plan 03) -- behind a single blocking human-verify legitimacy checkpoint, and create the `src/prediction/` package skeleton every later plan in this phase builds on.

Purpose: 04-RESEARCH.md's automated Package Legitimacy Audit flagged all three (plus the transitive `cmdstanpy` dependency) `SUS` due to a known PyPI-metadata-gap tool limitation (no weekly-download/repo-URL signal resolved), not a genuine risk signal -- but per the Package Legitimacy Gate this still requires explicit human sign-off before any install, exactly mirroring 03-03-PLAN.md's `numpy`/`plotly` precedent. Every other plan in this phase (03 through 09) depends on this plan completing first, since every one of them either imports one of these three packages directly or lives under the `src/prediction/` package this plan creates.

Output: `requirements.txt` with `xgboost==3.3.0`, `prophet==1.2.1`, and `scikit-learn==1.9.0` pinned, all three installed and import-verified; `src/prediction/__init__.py` created as the package marker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-CONTEXT.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Verify xgboost, prophet, and scikit-learn package legitimacy before install</name>
  <action>Pause for human verification of all three packages' legitimacy before Task 2 installs any of them -- this checkpoint is mandatory per the Package Legitimacy Gate and is never auto-approvable, even though this project's workflow.mode is yolo.</action>
  <what-built>Nothing yet -- this gate precedes pinning three new runtime dependencies. `xgboost==3.3.0`: 04-RESEARCH.md's Package Legitimacy Audit flagged it `SUS` with reasons `too-new`/`unknown-downloads`/`no-repository` -- the `too-new` flag reflects the tool checking the *latest* PyPI release (3.4.0, five days before research), not the pinned 3.3.0 (a 2025-era release); this is a known audit-tool metadata-gap limitation, not a genuine risk signal. `prophet==1.2.1`: flagged `SUS` (`unknown-downloads`) despite a resolved, active GitHub source repo (facebook.github.io/prophet). `scikit-learn==1.9.0`: flagged `SUS` (`unknown-downloads`/`no-repository`) despite being one of the most foundational, decades-old scientific-Python packages. All three are explicitly named with detailed version-compatibility reasoning in `CLAUDE.md`'s Recommended Stack table.</what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/xgboost/ and confirm the package page shows the XGBoost developers/DMLC as the maintaining organization, a release history including a `3.3.0` entry, and a linked source repository at https://github.com/dmlc/xgboost with active commit/release history.
    2. Visit https://pypi.org/project/prophet/ and confirm the package page shows Facebook/Meta (or the Prophet project) as the maintaining organization, a release history including a `1.2.1` entry, and a linked source repository at https://github.com/facebook/prophet with active commit/release history.
    3. Visit https://pypi.org/project/scikit-learn/ and confirm the package page shows the scikit-learn developers as the maintaining organization, a release history including a `1.9.0` entry, and a linked source repository at https://github.com/scikit-learn/scikit-learn with active commit/release history.
    4. Confirm the exact package names to be installed are `xgboost`, `prophet`, and `scikit-learn` -- not lookalikes such as `xgboost-cpu`, `fbprophet` (the old, renamed-away package name), `py-xgboost`, or `sklearn` (the deprecated PyPI alias name -- the real package name is `scikit-learn`, importable as `sklearn`).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with all three installs, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install and pin xgboost/prophet/scikit-learn, create the prediction package skeleton</name>
  <files>requirements.txt, src/prediction/__init__.py</files>
  <read_first>
    requirements.txt (current 9-line pinned manifest from Phase 1/2/3 -- append, do not reorder or remove existing lines)
    src/recommendation/__init__.py (the package-marker docstring convention to replicate for src/prediction/__init__.py)
    .planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md (Standard Stack / Installation block -- exact version rationale for all three packages)
  </read_first>
  <action>
    Run `pip install xgboost==3.3.0 prophet==1.2.1 scikit-learn==1.9.0`. Add all three as new lines to `requirements.txt`, appended after the existing nine lines in the order `xgboost==3.3.0`, `prophet==1.2.1`, `scikit-learn==1.9.0` -- do not touch `requirements-dev.txt`, do not reorder or remove any pre-existing line. Do not add `cmdstanpy` as its own `requirements.txt` line -- it remains a transitive dependency pulled in by `prophet`, per `CLAUDE.md`'s "auto-installed dep" framing. After the installs, run `python -c "import xgboost; import prophet; import sklearn; from sklearn.model_selection import TimeSeriesSplit"` to confirm all three import successfully (including the specific `TimeSeriesSplit` symbol Plan 03 needs) before any later plan writes code depending on them. Record the observed `prophet` import time in this task's SUMMARY (per STATE.md's flagged cold-start-risk concern) -- this is a local Windows dev-machine measurement, not proof of Streamlit Community Cloud's Debian build-image behavior, but it is the only empirical data point available from this environment; note explicitly in the SUMMARY that the actual Streamlit Cloud deploy validation (build log shows a wheel install, not a CmdStan compile step) remains an outstanding STATE.md-flagged item to check at actual deploy time, not resolvable from local execution.

    Create `src/prediction/__init__.py` as a package marker with a docstring stating this package is pure/zero-I/O -- no `streamlit`, `yfinance`, `sqlite3`, or network/database/LLM calls anywhere under `src/prediction/`; only `pandas`/`numpy`/`xgboost`/`prophet`(import-guarded)/`sklearn` and sibling `src.prediction` imports are permitted -- mirroring `src/recommendation/__init__.py`'s exact docstring pattern and module-boundary discipline.
  </action>
  <acceptance_criteria>
    - `requirements.txt` contains exactly the lines `xgboost==3.3.0`, `prophet==1.2.1`, and `scikit-learn==1.9.0`, in addition to the nine pre-existing Phase 1/2/3 lines (unchanged, unreordered).
    - `python -c "import xgboost; import prophet; import sklearn; from sklearn.model_selection import TimeSeriesSplit"` exits 0.
    - `src/prediction/__init__.py` exists and contains a docstring stating the package's zero-I/O contract.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "xgboost==3.3.0" requirements.txt &amp;&amp; grep -c "prophet==1.2.1" requirements.txt &amp;&amp; grep -c "scikit-learn==1.9.0" requirements.txt &amp;&amp; grep -c "streamlit==1.59.2" requirements.txt &amp;&amp; python -c "import xgboost; import prophet; import sklearn; from sklearn.model_selection import TimeSeriesSplit" &amp;&amp; test -f src/prediction/__init__.py</automated>
  </verify>
  <done>`xgboost==3.3.0`, `prophet==1.2.1`, and `scikit-learn==1.9.0` are installed and pinned in `requirements.txt` alongside the nine pre-existing entries, with no existing line removed or reordered, and `src/prediction/__init__.py` exists documenting the package's zero-I/O contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev machine <-> PyPI (`xgboost`/`prophet`/`scikit-learn` installs) | Untrusted third-party package supply chain; all three `SUS`-flagged by the automated legitimacy gate (metadata-gap false positives per 04-RESEARCH.md's override note), plus `prophet`'s transitive `cmdstanpy` dependency which carries a compiled-artifact install path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-01 | Tampering (supply chain) | `xgboost==3.3.0` / `prophet==1.2.1` / `scikit-learn==1.9.0` installs | high | mitigate | Task 1's blocking human-verify checkpoint confirms each package's identity/maintainer/repository before any install runs; never auto-approvable, even under `workflow.mode=yolo` |
| T-04-02 | Tampering (supply chain, transitive) | `cmdstanpy` (Prophet's Stan backend, pulled in transitively, not directly pinned) | medium | mitigate | Covered by Task 1's Prophet legitimacy check (transitive dependency of an already-verified package); Plan 05's import-guard additionally degrades gracefully at runtime if the compiled CmdStan artifact fails to load, so a bad transitive install never crashes the whole page |

</threat_model>

<verification>
`python -c "import xgboost; import prophet; import sklearn; from sklearn.model_selection import TimeSeriesSplit"` exits 0 after Task 2; the grep checks confirm all three new lines are pinned in `requirements.txt` with no pre-existing line disturbed; `src/prediction/__init__.py` exists; the blocking checkpoint in Task 1 confirms human sign-off preceded all three installs.
</verification>

<success_criteria>
All three ML/backtest dependencies are installed, pinned, and import-verified, with their legitimacy explicitly confirmed by a human before installation, and the `src/prediction/` package skeleton exists -- every later plan in this phase (03 through 09) can now safely import from `src/prediction/` or these three packages with no further gating.
</success_criteria>

<output>
Create `.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `xgboost==3.3.0`, `prophet==1.2.1`, `scikit-learn==1.9.0` entries -- `requirements.txt`
- `src/prediction/__init__.py` -- package marker, zero-I/O contract docstring
