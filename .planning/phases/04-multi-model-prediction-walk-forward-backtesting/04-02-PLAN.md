---
phase: 04-multi-model-prediction-walk-forward-backtesting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/charts.py
  - tests/test_components.py
autonomous: true
requirements: [PRED-03]
must_haves:
  truths:
    - "build_forecast_figure(price_df, forecast_index, forecast_values, ci_lower, ci_upper) is a pure function (no st.* calls) returning a go.Figure with exactly 4 traces: the reused historical price line (from build_price_history_figure), an invisible CI-upper-bound line, an invisible CI-lower-bound line with fill='tonexty', and the visible dashed forecast line -- matching 04-RESEARCH.md Pattern 6 and 04-UI-SPEC.md's Color table exactly."
    - "FORECAST_COLOR == '#0EA5E9' and CI_FILL_COLOR == 'rgba(14, 165, 233, 0.2)', declared as module-level constants (matching CHART_MARK_COLOR's existing convention), never inlined into a trace call."
    - "PRED-03 adjacency: for every point in ci_lower/forecast/ci_upper, ci_lower[i] <= forecast[i] <= ci_upper[i] holds for the synthetic fixtures used in this plan's tests -- build_forecast_figure never reorders or clips its input arrays, so any invariant violation in the caller's data is visible unchanged in the figure's trace y-values (the invariant itself is enforced by each model module in Plans 04/05, not here)."
    - statement: "The forecast/CI chart at narrow viewport width relies on st.plotly_chart's default responsive behavior (matching Phase 3's price-history chart precedent) -- no custom breakpoint logic is added by build_forecast_figure/render_forecast_chart; verified visually at implementation/UI-review time, not by an automated test (04-UI-SPEC.md UI Considerations, overflow row 1, backstop)."
      verification: backstop
    - statement: "An extreme CI band width (e.g. a very high-volatility asset at a 90-day horizon) is handled by Plotly's default y-axis autoscale with no numeric clamping/truncation in build_forecast_figure, and does not visually distort the historical-price portion of the same chart -- verified visually at implementation/UI-review time, not by an automated test (04-UI-SPEC.md UI Considerations, overflow row 2, backstop)."
      verification: backstop
  artifacts:
    - path: "src/components/charts.py"
      provides: "build_forecast_figure(price_df, forecast_index, forecast_values, ci_lower, ci_upper) -> go.Figure, render_forecast_chart(..., key) -> None, FORECAST_COLOR, CI_FILL_COLOR"
  key_links:
    - from: "src/components/charts.py build_forecast_figure"
      to: "src/components/charts.py build_price_history_figure"
      via: "build_forecast_figure calls build_price_history_figure(price_df) first and adds 3 more traces on top -- never reimplements the historical-line trace"
      pattern: "build_price_history_figure\\("
  prohibitions:
    - "MUST NOT reimplement the historical price line trace inside build_forecast_figure -- it must call build_price_history_figure(price_df) and add traces to the returned figure, per the existing pure-builder reuse convention (03-UI-SPEC.md/03-PATTERNS.md precedent)."
---

<objective>
Extend `src/components/charts.py` with a forecast chart builder that overlays a dashed forecast line and a shaded confidence-interval band on top of the existing historical price chart (PRED-03).

Purpose: this is the one new chart-rendering primitive Plans 08/09 (the search-page UI extension) need to visually satisfy PRED-03's confidence-interval requirement. It has zero dependency on the new ML packages or the `src/prediction/` package -- it is a pure Plotly-figure builder taking already-computed arrays as arguments, so it can be built and tested fully in parallel with Plan 01's package install and Plans 03-07's model/backtest code.

Output: `build_forecast_figure`/`render_forecast_chart` added to `src/components/charts.py`, with unit tests extending `tests/test_components.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-CONTEXT.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forecast chart builder with shaded CI band</name>
  <files>src/components/charts.py, tests/test_components.py</files>
  <read_first>
    src/components/charts.py (full file, already read this session -- extend, do not rewrite; keep CHART_MARK_COLOR and every existing function unchanged)
    tests/test_components.py (full file, already read this session -- extend with new tests in the same style, do not remove or rewrite existing tests)
    .planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md (Pattern 6, "Forecast chart with a shaded CI band" -- exact three-trace `fill='tonexty'` code)
    .planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-UI-SPEC.md (Color table -- exact FORECAST_COLOR/CI band hex/rgba values; Copywriting Contract -- "Shaded band shows an 80% confidence interval." caption, rendered by the caller, not this module)
  </read_first>
  <behavior>
    - `build_forecast_figure(price_df, forecast_index, forecast_values, ci_lower, ci_upper)` returns a `go.Figure` with exactly 4 traces in this order: [0] the historical price line (identical to `build_price_history_figure(price_df)`'s single trace), [1] the invisible CI-upper-bound `Scatter` (`line=dict(width=0)`, `showlegend=False`, `hoverinfo="skip"`), [2] the invisible CI-lower-bound `Scatter` (`fill="tonexty"`, `fillcolor=CI_FILL_COLOR`, `line=dict(width=0)`, `showlegend=False`, `hoverinfo="skip"`), [3] the visible forecast line (`line=dict(color=FORECAST_COLOR, dash="dash")`).
    - Trace [1]'s `x`/`y` equal `forecast_index`/`ci_upper` exactly (no reordering); trace [2]'s `x`/`y` equal `forecast_index`/`ci_lower`; trace [3]'s `x`/`y` equal `forecast_index`/`forecast_values`.
    - `FORECAST_COLOR == "#0EA5E9"`; `CI_FILL_COLOR == "rgba(14, 165, 233, 0.2)"`.
    - For a synthetic fixture where `ci_lower[i] <= forecast_values[i] <= ci_upper[i]` holds for every `i`, the built figure's trace y-arrays preserve that same relationship unchanged (no clipping/reordering by the builder).
    - `render_forecast_chart(price_df, forecast_index, forecast_values, ci_lower, ci_upper, key)` calls `st.plotly_chart` with the figure returned by `build_forecast_figure(...)` and the given `key` -- thin wrapper, no business logic, matching `render_price_history_chart`'s existing shape.
  </behavior>
  <action>
    In `src/components/charts.py`, add two module-level constants immediately after the existing `CHART_MARK_COLOR = "#334155"` line: `FORECAST_COLOR = "#0EA5E9"` and `CI_FILL_COLOR = "rgba(14, 165, 233, 0.2)"`. Add `build_forecast_figure(price_df: pd.DataFrame, forecast_index, forecast_values, ci_lower, ci_upper) -> go.Figure` implementing 04-RESEARCH.md's Pattern 6 code verbatim: call `fig = build_price_history_figure(price_df)`, then `fig.add_trace(...)` three times in the exact order given in `<behavior>` above (upper-bound invisible line, lower-bound invisible line with `fill="tonexty"`, then the visible dashed forecast line). Add `render_forecast_chart(price_df, forecast_index, forecast_values, ci_lower, ci_upper, key: str) -> None` as a thin `st.plotly_chart(build_forecast_figure(...), key=key)` wrapper, mirroring `render_price_history_chart`'s exact shape (docstring, single-line body).

    In `tests/test_components.py`, add tests for every `<behavior>` bullet above, following the existing file's synthetic-`pd.DataFrame`/no-mocking style (e.g. `test_build_forecast_figure_returns_four_traces_in_order`, `test_build_forecast_figure_uses_forecast_and_ci_fill_colors`, `test_forecast_color_and_ci_fill_color_match_ui_spec_exactly`, `test_build_forecast_figure_preserves_ci_lower_forecast_ci_upper_ordering`). Import `build_forecast_figure`, `FORECAST_COLOR`, `CI_FILL_COLOR` alongside the existing imports at the top of the file (do not remove or reorder the existing `build_breakdown_figure`/`build_price_history_figure`/`CHART_MARK_COLOR` imports).
  </action>
  <acceptance_criteria>
    - `build_forecast_figure`, `render_forecast_chart`, `FORECAST_COLOR`, `CI_FILL_COLOR` are all defined in `src/components/charts.py`.
    - `pytest tests/test_components.py -x -q` passes, including every new test.
    - `build_forecast_figure`'s first trace is byte-identical in `x`/`y` to `build_price_history_figure(price_df)`'s single trace (proven by a direct comparison test).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_components.py -x -q</automated>
  </verify>
  <done>`build_forecast_figure`/`render_forecast_chart` exist in `src/components/charts.py`, reuse `build_price_history_figure` for the historical line rather than reimplementing it, and render a 4-trace figure (historical line + CI band + dashed forecast line) using the exact `04-UI-SPEC.md`-specified colors -- proven by a passing test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new this plan | `build_forecast_figure`/`render_forecast_chart` are pure chart-rendering functions over already-computed, already-trusted numeric arrays (produced by Plans 04/05/06's model/engine code) -- no new trust boundary is crossed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-07 | Tampering (business-rule integrity / lookahead bias) | Walk-forward fold generation and per-fold fit/predict loop | high | mitigate | Not implemented by this plan -- owned by Plan 03 (`walk_forward.py` + structural/leakage tests) and Plan 06 (`backtest.py`'s per-fold usage) |

</threat_model>

<verification>
`pytest tests/test_components.py -x -q` passes, covering both the pre-existing Phase 3 chart tests (unmodified) and this plan's new forecast-chart tests.
</verification>

<success_criteria>
`build_forecast_figure`/`render_forecast_chart` exist, are fully unit-tested with zero Streamlit-runtime dependency, and are ready for Plans 08/09 to call with real forecast/CI arrays with no rework.
</success_criteria>

<output>
Create `.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `build_forecast_figure(price_df, forecast_index, forecast_values, ci_lower, ci_upper)`, `render_forecast_chart(..., key)`, `FORECAST_COLOR`, `CI_FILL_COLOR` -- `src/components/charts.py`
