---
phase: 04-multi-model-prediction-walk-forward-backtesting
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/prediction/prophet_model.py
  - tests/test_prediction_prophet.py
autonomous: true
requirements: [PRED-02, PRED-03]
must_haves:
  truths:
    - "PROPHET_AVAILABLE is True after a successful `from prophet import Prophet` and False if that import (or any exception during it) fails -- the guard is a module-level try/except around the import statement itself, evaluated exactly once at module load time, per 04-RESEARCH.md Pattern 4."
    - "forecast_forward(close, horizon_days) raises a RuntimeError with an explanatory message when PROPHET_AVAILABLE is False, rather than letting an AttributeError/NameError on an unbound Prophet symbol propagate -- callers (Plan 06's engine.py) can catch this one specific, predictable failure mode."
    - "When PROPHET_AVAILABLE is True, forecast_forward(close, horizon_days) returns {'forecast': ndarray, 'ci_lower': ndarray, 'ci_upper': ndarray}, each of length horizon_days, sourced from Prophet's own yhat/yhat_lower/yhat_upper columns at interval_width=0.80 -- ci_lower[i] <= forecast[i] <= ci_upper[i] for every i (PRED-03)."
    - "No other module in src/prediction/ or src/pages/ contains an unguarded `from prophet import Prophet` at module import time -- prophet_model.py is the only file where this exact import is ever attempted, so a failed Prophet install/build degrades exactly one model option, never the whole page (04-RESEARCH.md Anti-Pattern)."
  artifacts:
    - path: "src/prediction/prophet_model.py"
      provides: "PROPHET_AVAILABLE, forecast_forward(close, horizon_days) -> dict, INTERVAL_WIDTH"
  key_links: []
  prohibitions:
    - "MUST NOT place an unguarded `from prophet import Prophet` anywhere outside prophet_model.py's own try/except block -- every other module that needs Prophet's forecast must import prophet_model and check PROPHET_AVAILABLE, never import the prophet package itself."
    - "MUST NOT let a Prophet import/fit failure raise an unhandled exception that crashes module import of src.prediction.engine or src.prediction.backtest -- the try/except around the import must catch Exception broadly (a cmdstanpy backend failure at import time is not always a clean ImportError), logging via logger.exception before setting PROPHET_AVAILABLE = False."
---

<objective>
Build the Prophet forecast model behind an import-guard so a failed or degraded `prophet`/`cmdstanpy` install in the deployed Streamlit Community Cloud environment (STATE.md's flagged risk) disables exactly this one model option, never the whole drill-in page.

Purpose: this is this phase's single highest-risk integration point -- 04-RESEARCH.md's Pitfall 1 documents both a positive signal (prebuilt manylinux wheels bundling CmdStan since Prophet 1.1) and a real, unresolved community-reported failure mode on Streamlit Cloud specifically. The import-guard pattern this plan implements is the concrete code-level mitigation; the actual empirical Streamlit Cloud deploy validation STATE.md also requires remains an execution-time/deploy-time check outside this plan's local-dev scope (noted explicitly in this plan's SUMMARY, not silently dropped).

Output: `src/prediction/prophet_model.py` with `PROPHET_AVAILABLE`/`forecast_forward`, plus a test file that exercises one real (non-mocked) small Prophet fit and several mocked/guarded-path tests for the orchestration logic -- Prophet fits are the slowest tests in this phase's suite (04-RESEARCH.md Wave 0 Gaps), so fixture size is kept at the practical minimum.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-CONTEXT.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prophet model, import-guarded (D-08-style graceful degradation)</name>
  <files>src/prediction/prophet_model.py, tests/test_prediction_prophet.py</files>
  <read_first>
    src/pages/_universe_loader.py (lines 38-42's logger.exception + graceful-status-return convention -- the closest in-repo precedent for a caught-and-logged failure path, per 04-PATTERNS.md's Pattern Assignments)
    src/prediction/sma_model.py (Plan 04's output, already in your context this session -- forecast_forward's dict-shape contract this module must match)
    .planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md (Pattern 4 -- exact import-guard code and forecast_forward code; Pitfall 1 -- the full risk narrative and mitigation checklist)
  </read_first>
  <behavior>
    - `PROPHET_AVAILABLE` is `True` in this environment (Plan 01 already installed and import-verified `prophet==1.2.1`).
    - Calling `forecast_forward(close, horizon_days=7)` with a real, small synthetic `close` Series (at least `MIN_PREDICTION_HISTORY_ROWS`-scale data is not required for this unit test -- a smaller fixture, e.g. 60-90 rows, is sufficient to exercise Prophet's own fit/predict path quickly) returns a dict with keys `forecast`/`ci_lower`/`ci_upper`, each of length 7.
    - `ci_lower[i] <= forecast[i] <= ci_upper[i]` for every `i` in the returned arrays (PRED-03; Prophet's own `yhat_lower`/`yhat`/`yhat_upper` guarantee this by construction, but the test asserts it directly against the real fit's output).
    - With `prophet_model.PROPHET_AVAILABLE` monkeypatched to `False` (simulating a failed import without needing to actually break the real installed package), `forecast_forward(close, horizon_days=7)` raises `RuntimeError` with a message mentioning Prophet is unavailable.
    - `INTERVAL_WIDTH == 0.80` (matches `sma_model.Z_80PCT`'s implied 80% band, per 04-RESEARCH.md Assumption A-07).
  </behavior>
  <action>
    Create `src/prediction/prophet_model.py` with a module-level `import logging`, `logger = logging.getLogger(__name__)`, and a guarded import block per 04-RESEARCH.md Pattern 4's exact code: `try: from prophet import Prophet; PROPHET_AVAILABLE = True` / `except Exception: logger.exception("Prophet import failed -- disabling the Prophet model option"); PROPHET_AVAILABLE = False`. Define `INTERVAL_WIDTH = 0.80`. Define `forecast_forward(close: pd.Series, horizon_days: int) -> dict`: if `not PROPHET_AVAILABLE`, raise `RuntimeError("Prophet is not available in this environment")`; else build `df = pd.DataFrame({"ds": close.index, "y": close.values})`, `model = Prophet(interval_width=INTERVAL_WIDTH)`, `model.fit(df)`, `future = model.make_future_dataframe(periods=horizon_days)`, `forecast = model.predict(future).tail(horizon_days)`; return `{"forecast": forecast["yhat"].to_numpy(), "ci_lower": forecast["yhat_lower"].to_numpy(), "ci_upper": forecast["yhat_upper"].to_numpy()}`.

    Create `tests/test_prediction_prophet.py` with: (1) one real, non-mocked `forecast_forward` call against a small synthetic `close` Series fixture (index via `pd.date_range`, values with a mild upward trend plus noise so Prophet's fit is well-defined) covering the "PROPHET_AVAILABLE is True" behavior bullets above -- keep this the *only* real Prophet fit in the file to bound test runtime; (2) a `monkeypatch.setattr("src.prediction.prophet_model.PROPHET_AVAILABLE", False)`-based test proving the `RuntimeError` guard path, with no real Prophet call attempted in that test; (3) a direct assertion `INTERVAL_WIDTH == 0.80`.
  </action>
  <acceptance_criteria>
    - `PROPHET_AVAILABLE`, `forecast_forward`, `INTERVAL_WIDTH` are all defined in `src/prediction/prophet_model.py`.
    - `pytest tests/test_prediction_prophet.py -x -q` passes (expect this to be the slowest file in the `test_prediction_*` suite -- acceptable per 04-RESEARCH.md's Wave 0 Gaps note).
    - `grep -c "from prophet import Prophet" src/prediction/prophet_model.py` equals exactly `1` (the guarded import is the only place this exact import line appears anywhere in `src/prediction/`).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_prediction_prophet.py -x -q &amp;&amp; test "$(grep -c 'from prophet import Prophet' src/prediction/prophet_model.py)" = "1"</automated>
  </verify>
  <done>`prophet_model.py` exposes `PROPHET_AVAILABLE` and `forecast_forward` behind a broad `except Exception` import guard, matching `sma_model.forecast_forward`'s dict-shape contract exactly, with `RuntimeError` raised predictably when unavailable -- proven by one real (non-mocked) small Prophet fit plus a monkeypatched guard-path test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `prophet`/`cmdstanpy` compiled-artifact load at import time | The one place in this phase where a third-party package's own native/compiled backend can fail independently of the Python import machinery itself (04-RESEARCH.md Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-02 | Tampering (supply chain, transitive) | `cmdstanpy` (Prophet's Stan backend, transitive) | medium | mitigate | This plan implements the runtime half of the mitigation: a broad `except Exception` import guard (not a narrower `except ImportError`, since a cmdstanpy backend failure at import time is not always a clean `ImportError`) sets `PROPHET_AVAILABLE = False` and logs via `logger.exception`, so a degraded install disables only the Prophet option |
| T-04-06 | Repudiation / Information Disclosure | Prophet import/runtime failure surfacing a raw stack trace to the UI | low | mitigate | `forecast_forward` raises one specific, predictable `RuntimeError` when `PROPHET_AVAILABLE` is `False`, rather than letting an unbound-symbol `NameError`/`AttributeError` propagate with an internal traceback; Plan 08 owns catching this and rendering the fixed fallback copy instead of a raw exception |

</threat_model>

<verification>
`pytest tests/test_prediction_prophet.py -x -q` passes; the grep confirms exactly one `from prophet import Prophet` line exists in the entire `src/prediction/` package (inside the guard, nowhere else).
</verification>

<success_criteria>
The Prophet model exists behind a broad import guard matching the other two models' `forecast_forward` interface exactly, with its one real Prophet fit test proving the actual installed package produces a valid forecast+CI on this machine -- ready for Plan 06's `backtest.py`/`engine.py` to dispatch to, and ready to degrade gracefully if the Streamlit Cloud build environment's Prophet install is degraded (empirical deploy-time validation remains outstanding per STATE.md, noted in this plan's SUMMARY, not resolvable from local execution).
</success_criteria>

<output>
Create `.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `PROPHET_AVAILABLE`, `forecast_forward(close, horizon_days)`, `INTERVAL_WIDTH` -- `src/prediction/prophet_model.py`
