---
phase: 04-multi-model-prediction-walk-forward-backtesting
plan: 08
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-06", "04-07"]
files_modified:
  - src/pages/search.py
  - tests/test_prediction_search.py
autonomous: true
requirements: [PRED-01, PRED-02, PRED-03, PRED-04]
must_haves:
  truths:
    - "D-02: the historical price chart renders immediately after a ticker resolves, with no model pre-selected -- the model dropdown's index defaults to None (placeholder 'Select a model'), and no forecast chart/backtest table renders until the user explicitly picks a model AND clicks Generate Forecast."
    - "D-01: the model picker is a single st.selectbox (one model shown/selected at a time) -- never tabs, never a permanent side-by-side layout, for the single-model flow this plan builds (D-06's separate multi-model compare view is Plan 09's concern, not this plan's)."
    - "D-03: the horizon selector offers exactly the 3 preset values 7/30/90 (displayed 'X Days') via a closed st.selectbox -- never a free slider, never a 4th value."
    - "D-04: resolve_forecast_request (the Generate Forecast testable core) is only invoked when the Generate Forecast button is clicked in that render pass -- selecting a different model or horizon value alone, with no button click, never triggers a new forecast generation."
    - "D-07/D-08: when fetch_prediction_data's status is 'insufficient_data', the model dropdown and Generate Forecast button are both rendered disabled, the INSUFFICIENT_HISTORY_MESSAGE copy is shown in their place, and the historical price chart (from resolve_search_result's own chart_df) still renders above it unconditionally -- the non-blocking D-08 precedent, now applied to Phase 4's stricter threshold."
    - "Prophet-unavailable fallback: when resolve_forecast_request returns {'status': 'prophet_unavailable'}, the PROPHET_UNAVAILABLE_MESSAGE copy renders and the SMA/XGBoost options remain fully selectable in the model dropdown on the next rerun -- one bad model's unavailability never disables the other two."
    - "resolve_forecast_request is st.cache_data-decorated keyed on (ticker, model, horizon_days, feature_frame, price_series, asset_class) -- calling it twice with identical arguments in the same script run or across reruns invokes engine.generate_forecast at most once (T-04-03 DoS mitigation, 04-RESEARCH.md Pitfall 6)."
    - "Forecast/backtest results are stored in st.session_state keyed by a string including ticker, model, and horizon_days -- switching to a different ticker, model, or horizon never displays a stale forecast left over from a previous selection (PRED-02 unclassified edge, resolved)."
    - "Backtest metrics are displayed via metrics.format_metrics_for_display (round-half-up), never Python's bare round() or an unrounded float, and the Sharpe value is labeled 'Sharpe Ratio (Simulated)' verbatim per 04-UI-SPEC.md's Copywriting Contract -- never just 'Sharpe' or 'Sharpe Ratio' alone."
    - statement: "The Generate Forecast click's loading state (SMA instant / XGBoost seconds / Prophet multi-second) uses a native st.spinner around the resolve_forecast_request call, with no custom skeleton UI -- Streamlit's synchronous execution already blocks further widget interaction while it runs; verified visually at implementation/UI-review time, not by an automated test (04-UI-SPEC.md UI Considerations, loading row 1, backstop)."
      verification: backstop
    - statement: "The Prophet-unavailable and insufficient-history explanatory messages are short, fixed-shape template strings rendered via standard st.warning/st.error text wrapping, with no custom truncation logic -- verified visually at implementation/UI-review time, not by an automated test (04-UI-SPEC.md UI Considerations, long-text row, backstop)."
      verification: backstop
  artifacts:
    - path: "src/pages/search.py"
      provides: "resolve_forecast_request(ticker, model, horizon_days, feature_frame, price_series, asset_class) -> dict (cached testable core), render_search_page() extended with the D-01/D-02/D-03/D-04/D-05/D-07/D-08 prediction section"
  key_links:
    - from: "src/pages/search.py render_search_page"
      to: "src/pages/_prediction_loader.py fetch_prediction_data, src/prediction/engine.py generate_forecast, src/components/charts.py build_forecast_figure/render_forecast_chart"
      via: "the extended render function calls fetch_prediction_data once per ticker resolution, then resolve_forecast_request only on Generate Forecast click, then render_forecast_chart with the returned arrays"
      pattern: "fetch_prediction_data\\("
  prohibitions:
    - "MUST NOT let render_search_page() compute any forecast/backtest math inline -- every number displayed must originate from engine.generate_forecast's return dict, never recomputed or duplicated in the page layer (page-thin/module-thick split)."
    - "MUST NOT use directive language ('buy', 'sell', 'you should') or promise accuracy anywhere in this plan's new copy -- every string must come verbatim from 04-UI-SPEC.md's Copywriting Contract table (COMPLY-02-adjacent, binding on all copy written from this phase forward per the UI-SPEC's own note)."
---

<objective>
Extend `src/pages/search.py` with the D-01 through D-05 and D-07/D-08 single-model prediction flow: model dropdown, horizon selector, an explicit "Generate Forecast" button, the resulting forecast chart with CI band, and a backtest metrics table -- satisfying PRED-01 (unchanged historical chart), PRED-02 (model+horizon selection and generation), PRED-03 (CI band), and PRED-04 (backtest metrics display) end-to-end on the drill-in page.

Purpose: this is where every prior plan's pure/I/O module gets wired into a real, user-facing surface. Plan 09 (D-06's "Compare All Models") depends on this plan's session-state/loader/engine wiring already existing, so this plan must land first and establish the exact session-state key scheme and rendering structure Plan 09 extends.

Output: `src/pages/search.py` extended (existing `resolve_search_result`/Phase 3 REC-04 behavior untouched), plus `tests/test_prediction_search.py` -- fully-mocked unit tests for the new testable core, no live network call, no Streamlit-runtime-dependent assertions (matching the existing convention that `render_search_page()` itself is exercised only by the phase's end-of-phase human verification, not a unit test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-CONTEXT.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-PATTERNS.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testable forecast-request core (resolve_forecast_request, cached)</name>
  <files>src/pages/search.py, tests/test_prediction_search.py</files>
  <read_first>
    src/pages/search.py (full file, already read this session -- extend, do not rewrite; every existing function/constant/import must remain unless this task's action explicitly says to change it)
    tests/test_recommendation_search.py (full file, already read this session -- the exact unittest.mock.patch-the-imported-name convention this plan's new tests must follow)
    src/prediction/engine.py (Plan 06's output -- generate_forecast's exact signature/return shape)
    src/pages/_prediction_loader.py (Plan 07's output -- fetch_prediction_data's exact signature/return shape)
    src/data/cache.py test precedent (tests/test_cache.py's isolated_cache fixture -- the st.cache_data .clear() convention this plan's tests must use to avoid cross-test cache leakage)
  </read_first>
  <behavior>
    - `resolve_forecast_request(ticker, "sma", 7, feature_frame, price_series, "Stocks")` calls `src.pages.search.generate_forecast` exactly once and returns its return value unchanged (thin caching wrapper, no added logic beyond caching).
    - Calling `resolve_forecast_request` twice with byte-identical arguments invokes the underlying `generate_forecast` mock only once (the second call is a cache hit) -- proven via a mock call-count assertion, with `resolve_forecast_request.clear()` called in a test fixture teardown so this test's cache state never leaks into another test.
  </behavior>
  <action>
    In `src/pages/search.py`, add new imports (stdlib/third-party first, then `src.*` alphabetized by module path, appended to the existing import block without reordering or removing any current import line): `from src.components.charts import build_forecast_figure, render_forecast_chart` (alongside the existing `charts` import line -- combine into one `from src.components.charts import (...)` block with the pre-existing `render_breakdown_bar_chart, render_price_history_chart` names), `from src.config import CACHE_TTL_SECONDS`, `from src.pages._prediction_loader import fetch_prediction_data`, `from src.prediction.engine import MODEL_LABELS, VALID_HORIZONS, generate_forecast`, `from src.prediction.metrics import format_metrics_for_display`.

    Define `resolve_forecast_request(ticker: str, model: str, horizon_days: int, feature_frame: pd.DataFrame, price_series: pd.Series, asset_class: str) -> dict` decorated with `@st.cache_data(ttl=CACHE_TTL_SECONDS, show_spinner=False)` -- its entire body is `return generate_forecast(ticker, model, horizon_days, feature_frame, price_series, asset_class)`. Document in its docstring that this is the DoS-mitigation caching boundary (T-04-03): `engine.py` stays zero-I/O and cannot import `streamlit` itself, so the `st.cache_data` wrapper must live here, at the page layer, keyed on all six arguments including the `feature_frame`/`price_series` DataFrame/Series content (Streamlit's built-in DataFrame/Series hashing handles this).

    Create `tests/test_prediction_search.py` covering both `<behavior>` bullets, patching `src.pages.search.generate_forecast` via `unittest.mock.patch` (the name as imported into `src.pages.search`, per the established convention), with a `pytest.fixture(autouse=True)` that calls `resolve_forecast_request.clear()` before and after every test (mirroring `tests/test_cache.py`'s `isolated_cache` fixture pattern) so no cached result leaks between tests. Use small synthetic `feature_frame`/`price_series` fixtures (content does not need to be realistic for this task -- only argument identity/cache-key behavior is under test).
  </action>
  <acceptance_criteria>
    - `resolve_forecast_request` is defined in `src/pages/search.py`, decorated with `st.cache_data`.
    - `pytest tests/test_prediction_search.py -x -q` passes.
    - Every pre-existing test in `tests/test_recommendation_search.py` still passes unmodified (`pytest tests/test_recommendation_search.py -x -q` green, proving this task's import/wiring additions did not break Phase 3's `resolve_search_result`).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_prediction_search.py tests/test_recommendation_search.py -x -q</automated>
  </verify>
  <done>`resolve_forecast_request` exists as the page's `st.cache_data`-wrapped, testable core for the Generate Forecast button, proven to genuinely deduplicate identical calls, with Phase 3's existing search-page tests still fully green.</done>
</task>

<task type="auto">
  <name>Task 2: Render the D-01-D-05/D-07/D-08 single-model prediction section</name>
  <files>src/pages/search.py</files>
  <read_first>
    src/pages/search.py (this task builds directly on Task 1's edits from earlier in this same plan -- re-read is unnecessary if Task 1 already ran in this session; otherwise read the current file state before editing)
    .planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-UI-SPEC.md (Copywriting Contract table -- every string this task renders must come from this table verbatim; Color table -- Accent color reserved for the Generate Forecast button only; UI Considerations table -- the empty/loading/error/partial state rows this task must satisfy)
  </read_first>
  <action>
    Add module-level string constants (appended after the existing `BREAKDOWN_HEADING` constant, following the file's exact `_TEMPLATE`-suffix-for-`.format()`-strings convention) copied verbatim from 04-UI-SPEC.md's Copywriting Contract: `MODEL_DROPDOWN_LABEL = "Prediction Model"`, `MODEL_DROPDOWN_PLACEHOLDER = "Select a model"`, `HORIZON_SELECTOR_LABEL = "Forecast Horizon"`, `GENERATE_FORECAST_LABEL = "Generate Forecast"`, `PRE_GENERATION_HINT = "Select a model and horizon, then click Generate Forecast to see a prediction."`, `CI_CAPTION = "Shaded band shows an 80% confidence interval."`, `BACKTEST_SECTION_HEADING = "Backtested Accuracy"`, `BACKTEST_HYPOTHETICAL_CAPTION = "Backtested using walk-forward validation on historical data. Hypothetical results — past performance does not guarantee future accuracy."`, `METRIC_LABELS = {"rmse": "RMSE", "directional_accuracy": "Directional Accuracy", "sharpe": "Sharpe Ratio (Simulated)"}`, `INSUFFICIENT_HISTORY_MESSAGE = "Not enough price history to generate a reliable forecast for this asset."`, `PROPHET_UNAVAILABLE_MESSAGE = "Prophet isn't available right now — try SMA Baseline or XGBoost instead."`, `FORECAST_ERROR_MESSAGE = "We couldn't generate a forecast right now. Please try again shortly."`, `HORIZON_LABELS = {7: "7 Days", 30: "30 Days", 90: "90 Days"}`.

    Refactor `render_search_page()`'s body so its existing `insufficient_data`/`scored` branches (currently each ending in an early `return`) instead converge on a shared trailing call: after rendering the Phase-3 `insufficient_data` warning+chart OR the Phase-3 `scored` subheader+chart+breakdown+explanation (both branches unchanged in their own content), both paths fall through to `_render_prediction_section(result["ticker"], infer_asset_class(result["ticker"]))` instead of returning early (the `not_found` branch's early `return` is unchanged -- prediction never renders for a ticker that does not exist at all).

    Define a new private `_render_prediction_section(ticker: str, asset_class: str) -> None`: call `prediction_data = fetch_prediction_data(ticker)`; if `prediction_data["status"] == "not_found"`, return silently (defensive fallback -- should not normally occur, mirroring `resolve_search_result`'s own documented defensive-fallback precedent, since `resolve_search_result` already proved this ticker resolves). Render the three D-01/D-03/D-04 controls on one row via `st.columns(3)`: `model_choice = st.selectbox(MODEL_DROPDOWN_LABEL, options=list(MODEL_LABELS), format_func=lambda m: MODEL_LABELS[m], index=None, placeholder=MODEL_DROPDOWN_PLACEHOLDER, disabled=(prediction_data["status"] == "insufficient_data"), key=f"model_{ticker}")`; `horizon_choice = st.selectbox(HORIZON_SELECTOR_LABEL, options=sorted(VALID_HORIZONS), format_func=lambda h: HORIZON_LABELS[h], index=1, disabled=(prediction_data["status"] == "insufficient_data"), key=f"horizon_{ticker}")` (`index=1` defaults to `30` days -- the middle preset; D-02's "no default" rule applies to the model dropdown only, per 04-CONTEXT.md D-02's exact wording); `generate_clicked = st.button(GENERATE_FORECAST_LABEL, disabled=(prediction_data["status"] == "insufficient_data" or model_choice is None), key=f"generate_{ticker}")`.

    If `prediction_data["status"] == "insufficient_data"`, render `st.warning(INSUFFICIENT_HISTORY_MESSAGE)` and return (D-07/D-08 -- the price chart above already rendered unconditionally via the Phase-3 branch this function is called from). If `model_choice is None`, render `st.caption(PRE_GENERATION_HINT)` and return (D-02's "no forecast section until a model is picked" rule -- note per 04-UI-SPEC.md's own Copywriting Contract, this hint is the correct copy for the "model/horizon chosen or not, not yet generated" pre-generation states collectively; do not additionally render anything before a model is chosen beyond the controls themselves).

    Compute `result_key = f"forecast_{ticker}_{model_choice}_{horizon_choice}"`. If `generate_clicked`: call `with st.spinner(f"Generating {MODEL_LABELS[model_choice]} forecast..."): st.session_state[result_key] = resolve_forecast_request(ticker, model_choice, horizon_choice, prediction_data["feature_frame"], prediction_data["price_series"], asset_class)`. Read `forecast_result = st.session_state.get(result_key)`. If `forecast_result is None`: render `st.caption(PRE_GENERATION_HINT)` and return. If `forecast_result["status"] == "prophet_unavailable"`: render `st.warning(PROPHET_UNAVAILABLE_MESSAGE)` and return. If `forecast_result["status"] == "error"`: render `st.error(FORECAST_ERROR_MESSAGE)` and return. Otherwise (`status == "ok"`): call `render_forecast_chart(prediction_data["chart_df"], forecast_result["forecast_index"], forecast_result["forecast"], forecast_result["ci_lower"], forecast_result["ci_upper"], key=f"forecast_chart_{ticker}_{model_choice}_{horizon_choice}")`; render `st.caption(CI_CAPTION)`; render `st.subheader(BACKTEST_SECTION_HEADING)`; call `_render_backtest_metrics_table(forecast_result["backtest_metrics"])`; render `st.caption(BACKTEST_HYPOTHETICAL_CAPTION)`.

    Define a private `_render_backtest_metrics_table(backtest_metrics: dict) -> None`: call `display_values = format_metrics_for_display(backtest_metrics)`; render inside `st.container(border=True)` (matching the disclaimer banner's card-surface convention, and D-05's "mirrors Phase 3's Score Breakdown pattern" instruction) one `st.metric(METRIC_LABELS[key], display_values[key])` call per key in `("rmse", "directional_accuracy", "sharpe")`, in that fixed order (never re-sorted).
  </action>
  <acceptance_criteria>
    - `render_search_page()` no longer returns early from the `insufficient_data`/`scored` branches -- both fall through to `_render_prediction_section`.
    - `_render_prediction_section`, `_render_backtest_metrics_table` are both defined in `src/pages/search.py`.
    - Every user-facing string added by this task is a module-level constant, none inlined directly into an `st.*` call.
    - `python -c "import src.pages.search"` exits 0 (proves the extended page still imports cleanly with no syntax/import errors).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import src.pages.search" &amp;&amp; grep -c "_render_prediction_section" src/pages/search.py</automated>
  </verify>
  <done>The search/drill-in page now shows the D-01 model dropdown (no default), D-03 horizon selector, D-04 explicit Generate Forecast button, the PRED-03 forecast+CI chart, and the D-05 backtest metrics table for the selected model, with D-07/D-08's insufficient-history and Prophet-unavailable fallback states both rendering the exact 04-UI-SPEC.md copy -- verified by a clean import and structural presence check (full interactive verification happens at the phase's end-of-phase human-check checkpoint, per this project's human_verify_mode=end-of-phase setting).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `require_auth()`-gated page (unchanged) | No new auth surface -- the model/horizon controls and Generate Forecast action are additional widgets on the same already-gated `search.py` page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-03 | Denial of Service | Free-tier CPU exhaustion via repeated "Generate Forecast" clicks | medium | mitigate | This plan implements the primary mitigation: `resolve_forecast_request`'s `st.cache_data(ttl=CACHE_TTL_SECONDS)` memoization serves a cached result instantly on a repeat click for the same `(ticker, model, horizon_days, feature_frame, price_series, asset_class)`, and Streamlit's synchronous execution model already blocks further widget interaction while a script run is in progress |
| T-04-06 | Repudiation / Information Disclosure | Prophet import/runtime failure surfacing a raw stack trace to the UI | low | mitigate | This plan owns the UI half: `forecast_result["status"] == "prophet_unavailable"` renders the fixed `PROPHET_UNAVAILABLE_MESSAGE` copy, never a raw exception; Plan 05 owns the underlying guard that produces this status |
| T-04-05 | Tampering (parameter manipulation) | `model`/`horizon_days` values reaching `generate_forecast` | medium | mitigate | Owned by Plan 06 -- `engine.generate_forecast` independently validates both values; this plan's `st.selectbox` options are additionally already constrained to the exact valid sets (`MODEL_LABELS`/`VALID_HORIZONS`), so a real user session never triggers the validation error path, but the server-side check remains the actual security boundary |

</threat_model>

<verification>
`pytest tests/test_prediction_search.py tests/test_recommendation_search.py -x -q` passes; `python -c "import src.pages.search"` exits 0; the full suite (`pytest`) remains green, proving no regression to any Phase 1/2/3 test.
</verification>

<success_criteria>
The drill-in page delivers PRED-01/PRED-02/PRED-03/PRED-04's single-model flow end-to-end, using only already-built, already-tested sibling modules -- no forecast/backtest math is duplicated in the page layer, and every UI-SPEC-mandated copy string and state (empty/loading/error/partial) is represented.
</success_criteria>

<output>
Create `.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-08-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `resolve_forecast_request(ticker, model, horizon_days, feature_frame, price_series, asset_class)`, `_render_prediction_section(ticker, asset_class)`, `_render_backtest_metrics_table(backtest_metrics)` -- `src/pages/search.py`
- `MODEL_DROPDOWN_LABEL`, `MODEL_DROPDOWN_PLACEHOLDER`, `HORIZON_SELECTOR_LABEL`, `GENERATE_FORECAST_LABEL`, `PRE_GENERATION_HINT`, `CI_CAPTION`, `BACKTEST_SECTION_HEADING`, `BACKTEST_HYPOTHETICAL_CAPTION`, `METRIC_LABELS`, `INSUFFICIENT_HISTORY_MESSAGE`, `PROPHET_UNAVAILABLE_MESSAGE`, `FORECAST_ERROR_MESSAGE`, `HORIZON_LABELS` -- `src/pages/search.py`
