---
phase: 04-multi-model-prediction-walk-forward-backtesting
plan: 07
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/pages/_prediction_loader.py
  - tests/test_prediction_loader.py
autonomous: true
requirements: [PRED-01, PRED-02]
must_haves:
  truths:
    - "fetch_prediction_data(ticker) calls fetch_ohlcv(ticker, period='5y') -- never the default period='1y' -- so this loader's SQLite/st.cache_data cache entry is independent of Phase 3's _universe_loader.py entry for the same ticker (by design, per 04-RESEARCH.md Pitfall 3, not a bug to consolidate)."
    - "fetch_prediction_data returns {'status': 'not_found'} when fetch_ohlcv raises or returns an empty DataFrame (D-07), {'status': 'insufficient_data', 'chart_df': df} when the fetch succeeds but assemble_feature_frame(df).dropna() has fewer than MIN_PREDICTION_HISTORY_ROWS (750) rows (D-08), and {'status': 'ok', 'chart_df': df, 'feature_frame': feature_frame, 'price_series': aligned_close} when at least 750 rows exist."
    - "PRED-01 boundary: an asset with exactly 750 post-dropna feature rows returns status 'ok' (>=750, not >750); an asset with exactly 749 rows returns status 'insufficient_data' -- the boundary is inclusive on the sufficient side, proven by two adjacent-row-count tests, not just a generic 'thin vs full' test."
    - "MIN_PREDICTION_HISTORY_ROWS is imported from src.prediction.walk_forward, never re-declared as a separate literal 750 in this file."
    - "price_series in the 'ok' result is df['Close'] aligned (via .loc) to feature_frame's post-dropna index -- not the raw, unaligned df['Close'] -- so every row in price_series has a corresponding, non-NaN feature_frame row at the same index (required for xgboost_model.fit_predict's direct-target-shift alignment in Plan 06)."
    - "chart_df in every status branch is the raw, full 5-year fetch_ohlcv result (not dropna'd/truncated) -- so the historical price chart (PRED-01) always shows the complete fetched history regardless of the feature-row-count gate outcome."
  artifacts:
    - path: "src/pages/_prediction_loader.py"
      provides: "fetch_prediction_data(ticker) -> dict"
  key_links:
    - from: "src/pages/_prediction_loader.py"
      to: "src/data/prices.py, src/features/feature_frame.py, src/prediction/walk_forward.py"
      via: "fetch_prediction_data calls fetch_ohlcv(ticker, period='5y') then assemble_feature_frame(df), gated by the imported MIN_PREDICTION_HISTORY_ROWS -- never reimplements fetch or feature math"
      pattern: "fetch_ohlcv\\(ticker, period=.5y.\\)"
  prohibitions:
    - "MUST NOT call yfinance directly or reimplement any part of assemble_feature_frame's rolling-window logic in this file -- it is I/O-only glue around the two existing chokepoints, per src/pages/_universe_loader.py's established precedent."
    - "MUST NOT accept a profile/sector/asset_class parameter this loader does not use -- unlike _universe_loader.fetch_scorable_row, prediction has no profile-fit/sector logic, so fetch_prediction_data(ticker) takes only the ticker (single-responsibility deviation from the sector-carrying sibling loader, documented in this file's module docstring)."
---

<objective>
Build the I/O-performing prediction data loader that fetches 5 years of history for one asset and applies the D-07/D-08 `MIN_PREDICTION_HISTORY_ROWS` gate, mirroring `src/pages/_universe_loader.py`'s established discriminated-union return shape.

Purpose: this is the one place Plan 08's search-page extension gets the `feature_frame`/`price_series`/`chart_df` triple `engine.generate_forecast` and the historical price chart both need, and the one place D-07's stricter-than-Phase-3 threshold is actually enforced against real fetched data -- the model dropdown and "Generate Forecast" button (D-08) are enabled/disabled based on this loader's status alone.

Output: `src/pages/_prediction_loader.py` with `fetch_prediction_data(ticker)`, plus a fully-mocked test file (no live network call) mirroring `tests/test_universe_loader.py`'s exact patching convention.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-CONTEXT.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prediction data loader (D-07/D-08's stricter minimum-history gate)</name>
  <files>src/pages/_prediction_loader.py, tests/test_prediction_loader.py</files>
  <read_first>
    src/pages/_universe_loader.py (full file, already read this session -- the exact discriminated-union return-shape and try/except/logger.exception convention to replicate)
    tests/test_universe_loader.py (full file, already read this session -- the exact unittest.mock.patch convention: patch the name as imported into src.pages._prediction_loader, not src.data.prices)
    src/prediction/walk_forward.py (Plan 03's output, already in your context this session -- MIN_PREDICTION_HISTORY_ROWS's exact value/import path)
    src/data/prices.py, src/features/feature_frame.py (the two chokepoints this loader calls -- signatures already in your context this session)
  </read_first>
  <behavior>
    - `fetch_prediction_data("NOTATICKER")` returns `{"status": "not_found"}` when `fetch_ohlcv` raises any exception.
    - `fetch_prediction_data("NOTATICKER")` returns `{"status": "not_found"}` when `fetch_ohlcv` returns an empty `DataFrame`.
    - `fetch_prediction_data(...)` calls `fetch_ohlcv(ticker, period="5y")` -- asserted via a mock call-args check, not just a return-value check (proving the `period="5y"` argument is actually passed, not defaulted to `"1y"`).
    - A synthetic fixture with exactly 749 post-dropna feature rows returns `{"status": "insufficient_data", "chart_df": df}` where `df` is the exact, untouched DataFrame `fetch_ohlcv` returned (not dropna'd, not truncated).
    - A synthetic fixture with exactly 750 post-dropna feature rows returns `{"status": "ok", ...}` (the boundary-inclusive test, D-07/PRED-01 boundary).
    - For the `"ok"` case, `feature_frame` has no `NaN` values, `price_series.index.equals(feature_frame.index)` is `True` (aligned), and `chart_df` is the full, untouched, non-dropna'd `fetch_ohlcv` result (longer than `feature_frame`/`price_series` by the rolling-window warm-up row count).
  </behavior>
  <action>
    Create `src/pages/_prediction_loader.py` with a module docstring explaining this file performs I/O (unlike `src/prediction/`) and is deliberately located under `src/pages/`, mirroring `_universe_loader.py`'s exact justification, plus one added sentence noting `fetch_prediction_data` takes only `ticker` (no `asset_class`/`sector` parameters) since prediction has no profile-fit/sector logic -- a deliberate single-responsibility deviation from `_universe_loader.fetch_scorable_row`'s wider signature. Import `logging`, `from src.data.prices import fetch_ohlcv`, `from src.features.feature_frame import assemble_feature_frame`, `from src.prediction.walk_forward import MIN_PREDICTION_HISTORY_ROWS`. Define `logger = logging.getLogger(__name__)`. Define `fetch_prediction_data(ticker: str) -> dict`: wrap `df, _source = fetch_ohlcv(ticker, period="5y")` in `try/except Exception: logger.exception("fetch_prediction_data failed for %s", ticker); return {"status": "not_found"}`; if `df.empty: return {"status": "not_found"}`; compute `feature_frame = assemble_feature_frame(df).dropna()`; if `len(feature_frame) < MIN_PREDICTION_HISTORY_ROWS: return {"status": "insufficient_data", "chart_df": df}`; else compute `price_series = df["Close"].loc[feature_frame.index]` and return `{"status": "ok", "chart_df": df, "feature_frame": feature_frame, "price_series": price_series}`.

    Create `tests/test_prediction_loader.py` covering every `<behavior>` bullet, patching `src.pages._prediction_loader.fetch_ohlcv` via `unittest.mock.patch` exactly as `tests/test_universe_loader.py` does. Build the 749-row and 750-row synthetic fixtures by generating a raw OHLCV `DataFrame` long enough that, after `assemble_feature_frame(...).dropna()` strips its rolling-window warm-up rows (the first ~20, per Phase 2's `MIN_HISTORY_ROWS` derivation), exactly 749 and exactly 750 rows remain respectively -- compute the exact raw row count needed (warm-up rows + target feature-row count) rather than guessing, and assert the resulting `len(feature_frame)` equals the intended target before asserting on `fetch_prediction_data`'s status, so the fixture's own correctness is self-verified.
  </action>
  <acceptance_criteria>
    - `fetch_prediction_data` is defined in `src/pages/_prediction_loader.py`.
    - `pytest tests/test_prediction_loader.py -x -q` passes, including the exact-749-vs-exact-750 boundary tests.
    - No live network call occurs anywhere in the test file (only `fetch_ohlcv` is ever mocked, never called for real).
    - `grep -c "period=.5y." src/pages/_prediction_loader.py` is at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_prediction_loader.py -x -q &amp;&amp; test "$(grep -c 'period="5y"' src/pages/_prediction_loader.py)" -ge "1"</automated>
  </verify>
  <done>`fetch_prediction_data` fetches 5 years of history via the existing chokepoints, applies the D-07/D-08 `MIN_PREDICTION_HISTORY_ROWS=750` gate with an inclusive boundary at exactly 750, and returns an aligned `feature_frame`/`price_series` pair alongside the full untruncated `chart_df` -- proven by a fully-mocked, boundary-tested unit test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Ticker string (already resolved by Phase 3's search flow) -> new `fetch_ohlcv(ticker, period="5y")` call site | The same `ticker` value Phase 3's `resolve_search_result` already validated via its own `fetch_ohlcv(ticker)` call, now reused at a second call site with a longer `period` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-04 | Tampering/Injection | Untrusted ticker string reaching `fetch_ohlcv(ticker, period="5y")` | low | accept | Already mitigated by Phase 1/3's existing parameterized-SQL chokepoint discipline (T-01-03) and yfinance's own input handling -- this plan introduces no new validation because none is needed: the `ticker` value is the same one Phase 3's search flow already resolved before this loader is ever called (Plan 08 only calls `fetch_prediction_data` after `resolve_search_result` returns a non-`not_found` status) |

</threat_model>

<verification>
`pytest tests/test_prediction_loader.py -x -q` passes; the grep confirms the `period="5y"` argument is actually present in the fetch call; no live network call occurs anywhere in this plan's test file.
</verification>

<success_criteria>
`fetch_prediction_data` exists, is zero-network in its own test suite, and is ready for Plan 08's search-page extension to call immediately after a ticker resolves, with no rework.
</success_criteria>

<output>
Create `.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `fetch_prediction_data(ticker)` -- `src/pages/_prediction_loader.py`
