---
phase: 04-multi-model-prediction-walk-forward-backtesting
plan: 09
type: execute
wave: 5
depends_on: ["04-08"]
files_modified:
  - src/pages/search.py
  - tests/test_prediction_search.py
autonomous: true
requirements: [PRED-02, PRED-04]
must_haves:
  truths:
    - "D-06: clicking 'Compare All Models' shows BOTH an @st.dialog modal AND a persistent yellow st.warning banner carrying matching time-cost copy -- neither surface alone satisfies D-06, both must render together while the comparison runs."
    - "D-06: a completion notification (st.toast, icon=':material/check_circle:') fires exactly once when all 3 models' comparison results are ready, using the exact 'Model comparison ready.' copy."
    - "PRED-03/PRED-04 ordering: the 3-column comparison view always renders in the fixed order sma, xgboost, prophet (MODEL_LABELS' declared insertion order) -- never re-sorted by score, RMSE, or any other metric value."
    - "PRED-04 error handling: if the Prophet column's result is {'status': 'prophet_unavailable'}, that one column renders PROPHET_UNAVAILABLE_MESSAGE while the SMA and XGBoost columns render their normal metrics tables -- one model's unavailability never blocks or hides the other two columns' results (mirrors 04-RESEARCH.md's Prophet-import-guard anti-pattern guidance, applied to the compare view)."
    - "'Compare All Models' reuses resolve_forecast_request (Plan 08) for each of the 3 models -- if the user already generated a single-model forecast individually before comparing, that specific model's cached result is reused rather than retrained (T-04-03 DoS mitigation, extended to the compare flow)."
    - "The 'Compare All Models' button is disabled whenever prediction_data['status'] != 'ok' (insufficient-history assets never expose this action either) -- consistent with the single-model flow's own disabled-state rule."
  artifacts:
    - path: "src/pages/search.py"
      provides: "_render_compare_all_models(ticker, horizon_choice, prediction_data, asset_class) -> None, appended to _render_prediction_section's rendering flow"
  key_links:
    - from: "src/pages/search.py _render_compare_all_models"
      to: "src/pages/search.py resolve_forecast_request"
      via: "the sequential 3-model comparison loop calls resolve_forecast_request once per model key in MODEL_LABELS' order -- never calls engine.generate_forecast directly, so the same st.cache_data boundary applies"
      pattern: "resolve_forecast_request\\("
  prohibitions:
    - "MUST NOT run the 3-model comparison in parallel (e.g. via st.fragment(parallel=True) or a thread pool) -- Streamlit Community Cloud's free tier is a single-CPU container, so sequential execution is the correct choice, not a missed-parallelism bug (04-RESEARCH.md Pattern 5 note)."
    - "MUST NOT invent copy not present in 04-UI-SPEC.md's Copywriting Contract table for the modal/banner/toast text -- the one exception is a minimal, mechanically-necessary dialog-dismiss button label ('Start Comparison'), documented in this plan's action as a Claude-discretion implementation detail per 04-CONTEXT.md's own note that the exact D-06 widget/mechanism is left to implementation."
---

<objective>
Add D-06's "Compare All Models" action to the search page: a button that opens an `@st.dialog` modal, shows a persistent yellow `st.warning` banner for the duration of a sequential 3-model comparison, fires a completion `st.toast`, and renders all 3 models' backtest metrics side-by-side in a 3-column layout.

Purpose: this is the one UI interaction pattern in this phase with no existing in-repo precedent (`st.dialog`, `st.toast`, and a `st.session_state`-driven persistent banner are all new to this codebase). It depends on Plan 08's `resolve_forecast_request`/session-state scheme already existing on `search.py`, and modifies the same file -- hence the sequential wave after Plan 08, not a parallel one.

Output: `src/pages/search.py` further extended with the D-06 flow, plus additional tests in `tests/test_prediction_search.py` for the testable pieces of this flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-CONTEXT.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md
@.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compare All Models -- dialog + persistent banner + toast + 3-column results</name>
  <files>src/pages/search.py, tests/test_prediction_search.py</files>
  <read_first>
    src/pages/search.py (current state after Plan 08's edits -- read the file fresh before editing, since this task extends `_render_prediction_section` and adds new module-level constants alongside Plan 08's)
    .planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-RESEARCH.md (Pattern 5 -- exact `@st.dialog`/persistent-banner/`st.toast` code to adapt, including the `inspect.signature`-verified API notes)
    .planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-UI-SPEC.md (Copywriting Contract -- exact modal heading/body, persistent banner, toast, and "Model Comparison" section-heading copy; UI Considerations table -- the "Compare All Models" loading/error/populated/zero-one-many rows this task must satisfy)
  </read_first>
  <action>
    In `src/pages/search.py`, add module-level constants (appended after Plan 08's `HORIZON_LABELS` constant): `COMPARE_ALL_MODELS_LABEL = "Compare All Models"`, `COMPARE_MODAL_HEADING = "Comparing All Models"`, `COMPARE_MODAL_BODY = "Training and backtesting all 3 models can take a while (Prophet in particular). This page will stay open while it runs."`, `COMPARE_PERSISTENT_BANNER = "Comparing all models for this asset — this may take a moment."`, `COMPARE_TOAST_MESSAGE = "Model comparison ready."`, `COMPARE_START_BUTTON_LABEL = "Start Comparison"` (the one mechanically-necessary label not in the Copywriting Contract table, per this plan's frontmatter prohibition note), `COMPARE_RESULTS_HEADING = "Model Comparison"`.

    Define a private `@st.dialog(COMPARE_MODAL_HEADING)`-decorated function `_compare_all_models_dialog(ticker: str) -> None`: render `st.warning(COMPARE_MODAL_BODY)`; if `st.button(COMPARE_START_BUTTON_LABEL, key=f"start_compare_{ticker}")`, set `st.session_state[f"comparing_{ticker}"] = True` and call `st.rerun()` (closes the dialog; the persistent banner + sequential computation take over on the next run, per 04-RESEARCH.md Pattern 5's exact mechanism).

    Define a private `_render_compare_all_models(ticker: str, horizon_days: int, prediction_data: dict, asset_class: str) -> None`: render `compare_clicked = st.button(COMPARE_ALL_MODELS_LABEL, disabled=(prediction_data["status"] != "ok"), key=f"compare_all_{ticker}")`; if `compare_clicked`, call `_compare_all_models_dialog(ticker)` (opens the modal for this render pass only). If `st.session_state.get(f"comparing_{ticker}")`: render `st.warning(COMPARE_PERSISTENT_BANNER)` (the persistent banner, independent of the dialog); compute `compare_results = {}`; with `st.spinner(...)`, loop over `model_key in MODEL_LABELS` in that fixed declared order and call `compare_results[model_key] = resolve_forecast_request(ticker, model_key, horizon_days, prediction_data["feature_frame"], prediction_data["price_series"], asset_class)` for each; after the loop, set `st.session_state[f"compare_results_{ticker}_{horizon_days}"] = compare_results`, set `st.session_state[f"comparing_{ticker}"] = False`, call `st.toast(COMPARE_TOAST_MESSAGE, icon=":material/check_circle:")`, then `st.rerun()` (so the persistent banner disappears on the next render and the results below display cleanly).

    After that block, read `compare_results = st.session_state.get(f"compare_results_{ticker}_{horizon_days}")`; if not `None`: render `st.subheader(COMPARE_RESULTS_HEADING)`; render `st.columns(3)`, and for each `(column, model_key)` pair zipped against `MODEL_LABELS` in its fixed insertion order, render inside that column: `st.write(f"**{MODEL_LABELS[model_key]}**")`, then branch on `compare_results[model_key]["status"]` -- `"prophet_unavailable"` renders `st.warning(PROPHET_UNAVAILABLE_MESSAGE)` (imported from Plan 08's constants), `"error"` renders `st.error(FORECAST_ERROR_MESSAGE)`, `"ok"` calls `_render_backtest_metrics_table(compare_results[model_key]["backtest_metrics"])` (Plan 08's helper, reused unmodified -- never a second, bespoke comparison-table implementation, per 04-RESEARCH.md's Open Question 1 resolution).

    Wire `_render_compare_all_models(ticker, horizon_choice, prediction_data, asset_class)` into `_render_prediction_section`'s existing body (from Plan 08), called once after the single-model forecast section's rendering completes (regardless of whether a single-model forecast has been generated yet) -- but only reachable when `prediction_data["status"] == "ok"` (the same branch guard Plan 08's insufficient-data early-return already establishes, so this call site is naturally never reached for an insufficient-history asset).
  </action>
  <acceptance_criteria>
    - `_compare_all_models_dialog`, `_render_compare_all_models` are both defined in `src/pages/search.py`.
    - `python -c "import src.pages.search"` exits 0.
    - `grep -c "st.dialog" src/pages/search.py` is at least `1`; `grep -c "st.toast" src/pages/search.py` is at least `1`.
    - A test proves `_render_compare_all_models`'s sequential-loop ordering: patching `resolve_forecast_request` with a `side_effect` that records call order, then asserting the recorded model-key order is exactly `["sma", "xgboost", "prophet"]`.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import src.pages.search" &amp;&amp; pytest tests/test_prediction_search.py -x -q &amp;&amp; test "$(grep -c 'st.dialog' src/pages/search.py)" -ge "1" &amp;&amp; test "$(grep -c 'st.toast' src/pages/search.py)" -ge "1"</automated>
  </verify>
  <done>D-06's "Compare All Models" action opens a modal, shows a persistent warning banner, sequentially reuses `resolve_forecast_request` for all 3 models in a fixed order, fires a completion toast, and renders a 3-column result view that degrades gracefully when Prophet is unavailable -- verified by a clean import, a call-order test, and structural presence checks (full interactive verification happens at the phase's end-of-phase human-check checkpoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `require_auth()`-gated page (unchanged) | No new auth surface -- "Compare All Models" is an additional widget/dialog on the same already-gated page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-03 | Denial of Service | Free-tier CPU exhaustion via repeated "Compare All Models" clicks (up to 3 sequential trainings per click) | medium | mitigate | This plan reuses Plan 08's `resolve_forecast_request` cache boundary for every one of the 3 sequential calls, so a repeat "Compare All Models" click for the same ticker/horizon is a full cache hit across all 3 models; the comparison loop is deliberately sequential (never parallel), matching the single-CPU free-tier container's actual capacity |
| T-04-06 | Repudiation / Information Disclosure | Prophet import/runtime failure surfacing a raw stack trace to the UI | low | mitigate | The Prophet column of the 3-column compare view renders the fixed `PROPHET_UNAVAILABLE_MESSAGE`/`FORECAST_ERROR_MESSAGE` copy on a non-"ok" status, identical treatment to the single-model flow (Plan 08) |

</threat_model>

<verification>
`pytest tests/test_prediction_search.py tests/test_recommendation_search.py -x -q` passes; `python -c "import src.pages.search"` exits 0; both structural greps confirm `st.dialog`/`st.toast` are present; the full suite (`pytest`) remains green.
</verification>

<success_criteria>
D-06 is fully implemented end-to-end -- modal + persistent banner + sequential cached comparison + completion toast + 3-column fixed-order result view with graceful Prophet degradation -- completing this phase's full requirements/decision coverage (PRED-01 through PRED-04, D-01 through D-08).
</success_criteria>

<output>
Create `.planning/phases/04-multi-model-prediction-walk-forward-backtesting/04-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `_compare_all_models_dialog(ticker)`, `_render_compare_all_models(ticker, horizon_days, prediction_data, asset_class)` -- `src/pages/search.py`
- `COMPARE_ALL_MODELS_LABEL`, `COMPARE_MODAL_HEADING`, `COMPARE_MODAL_BODY`, `COMPARE_PERSISTENT_BANNER`, `COMPARE_TOAST_MESSAGE`, `COMPARE_START_BUTTON_LABEL`, `COMPARE_RESULTS_HEADING` -- `src/pages/search.py`
