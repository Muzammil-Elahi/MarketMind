---
phase: 01-foundation-data-layer-caching-auth
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pages/login.py
  - src/pages/home.py
  - src/app.py
autonomous: false
requirements: [AUTH-01, AUTH-03]
must_haves:
  truths:
    - "Unauthenticated users see a single login/signup page; auth-gated pages are absent from st.navigation entirely (not visible-but-redirecting) until logged in (D-03)."
    - "The home page calls require_auth() before rendering any content, per D-04 (no per-page inline auth checks)."
    - "The login page's empty/unfilled form does not submit — native Streamlit form validation blocks it (UI-SPEC empty-state row)."
    - "The nav shell is constructed synchronously off st.session_state.get('logged_in') on every rerun — there is no intermediate/unresolved nav state (UI-SPEC loading-state row, covered)."
    - "Invalid-credential and magic-link-send-failure errors render the exact Copywriting Contract strings (UI-SPEC error rows)."
    - "A session-expired mid-session event (require_auth() failing on a gated page) renders the exact Copywriting Contract session-expired string and halts render."
    - "The placeholder home page renders the exact 'You're in' empty-state heading and body from the Copywriting Contract — no profile/recommendation content exists yet, matching Phase 1's scope boundary."
    - statement: "Login/signup/magic-link form submit shows Streamlit's native in-flight spinner or disabled-button state while the auth call is pending (UI-SPEC loading row, backstop)."
      verification: backstop
    - statement: "Partially-filled login/signup forms surface Streamlit's native per-field required-state highlighting (UI-SPEC partial row, backstop)."
      verification: backstop
    - statement: "An unusually long email address does not break the login form's layout — standard st.text_input scroll/truncate behavior applies (UI-SPEC long-text row, covered)."
  artifacts:
    - path: "src/pages/login.py"
      provides: "render_login_page() — email/password + Create Account + magic-link tabs"
    - path: "src/pages/home.py"
      provides: "render_home_page() — require_auth()-gated placeholder + Log Out"
    - path: "src/app.py"
      provides: "entrypoint — conditional st.navigation per D-03/Pattern 5"
  key_links:
    - from: "src/app.py"
      to: "src/pages/login.py, src/pages/home.py"
      via: "st.navigation({...}) constructed conditionally on st.session_state.get('logged_in')"
      pattern: "st\\.navigation\\("
    - from: "src/pages/home.py"
      to: "src/auth/session.py"
      via: "require_auth() called at the top of the page body, before any other rendering"
      pattern: "require_auth\\(\\)"
  prohibitions:
    - "MUST NOT add OAuth/social login buttons to the login page — D-01 restricts Phase 1 auth to email/password and magic link only."
    - "MUST NOT render any investor-profile, recommendation, or prediction content on the home page — it is a placeholder only, per CONTEXT.md's Phase Boundary; that content belongs to Phase 2+."
---

<objective>
Build the visible Phase 1 UI surface: the login/signup page (email/password + magic link, per D-01 and UI-SPEC), the placeholder home page (require_auth()-gated, "You're in" empty state), and the app.py entrypoint wiring conditional st.navigation so gated pages are hidden entirely (not redirect-visible) until login (D-03).

Purpose: this is the only user-facing surface Phase 1 ships (UI-SPEC's own scope note confirms no profile/recommendation/prediction UI exists yet). It is also where AUTH-01's "stays logged in across reload" and D-03's "hidden, not visible-but-redirecting" behaviors become observable to an actual human, hence the checkpoint at the end of this plan.

Output: src/pages/login.py, src/pages/home.py, src/app.py — the full login-to-home flow, ready for a human to click through.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer-caching-auth/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer-caching-auth/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer-caching-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-data-layer-caching-auth/01-02-SUMMARY.md
</context>

<interfaces>
From src/auth/session.py (Plan 02, already on disk):
sign_up(email, password), sign_in(email, password), sign_in_with_magic_link(email), require_auth() -> User, sign_out() — all raise AuthApiError on failure; sign_up/sign_in set st.session_state["access_token"]/["refresh_token"]/["logged_in"] on success.

From .planning/phases/01-foundation-data-layer-caching-auth/01-UI-SPEC.md Copywriting Contract (exact strings, reuse verbatim):
Primary CTA (login): "Log In" · Primary CTA (signup): "Create Account" · Primary CTA (magic link): "Send Magic Link" · Secondary action (logout): "Log Out" · Empty state heading: "You're in" · Empty state body: "This is your home base for MarketMind. Your investor profile and recommendations are coming in a future update — check back soon." · Error (invalid credentials): "We couldn't verify that email and password. Double-check them and try again." · Error (magic link send failure): "We couldn't send your login link. Check your email address and try again." · Error (session expired): "Your session expired — please log in again."
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Login/signup page — email/password + magic link</name>
  <files>src/pages/login.py</files>
  <read_first>
    src/auth/session.py
    .planning/phases/01-foundation-data-layer-caching-auth/01-UI-SPEC.md (full file — Typography, Color, Copywriting Contract, UI Considerations)
    .planning/phases/01-foundation-data-layer-caching-auth/01-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Create src/pages/login.py exporting render_login_page() -> None. Render the app title/wordmark "MarketMind" as a Display-role heading. Use st.tabs(["Log In", "Create Account", "Magic Link"]) for the three auth paths. Each tab wraps its inputs in an st.form so native required-field validation blocks empty submission (UI-SPEC empty-state row): the "Log In" tab has email + password st.text_input fields and a submit button labeled exactly "Log In" that calls src.auth.session.sign_in(email, password), catching AuthApiError and rendering st.error with the exact invalid-credentials Copywriting Contract string on failure, and st.rerun() on success. The "Create Account" tab has email + password fields and a submit button labeled exactly "Create Account" that calls sign_up(email, password); per D-02 there is no email-verification interstitial — on success the user is immediately logged in (st.rerun()); on failure (including a duplicate-email attempt) render an error state, reusing the same invalid-credentials-style Copywriting Contract error text since no separate duplicate-account copy exists in UI-SPEC. The "Magic Link" tab has a single email field and a submit button labeled exactly "Send Magic Link" that calls sign_in_with_magic_link(email); on success show an info message that the link was sent; on failure render st.error with the exact magic-link-send-failure Copywriting Contract string.

    Do not add any OAuth/social-provider button to this page (D-01). Do not add any field beyond email/password/magic-link-email — no name, phone, or profile field belongs here.
  </action>
  <acceptance_criteria>
    - render_login_page is defined and callable with no arguments.
    - The file contains the exact strings "Log In", "Create Account", and "Send Magic Link" as button labels.
    - The file contains the exact invalid-credentials and magic-link-failure error strings from the Copywriting Contract.
    - No OAuth/social-provider identifier (e.g. "google", "github", "oauth") appears in the file.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "def render_login_page" src/pages/login.py &amp;&amp; grep -c "Create Account" src/pages/login.py &amp;&amp; grep -c "Send Magic Link" src/pages/login.py &amp;&amp; ! grep -Eqi "oauth|sign_in_with_oauth" src/pages/login.py</automated>
  </verify>
  <done>src/pages/login.py renders the three-tab login/signup/magic-link form, wired to src/auth/session.py, using the Copywriting Contract's exact button and error strings, with no OAuth surface.</done>
</task>

<task type="auto">
  <name>Task 2: Placeholder home page + app entrypoint navigation</name>
  <files>src/pages/home.py, src/app.py</files>
  <read_first>
    src/auth/session.py
    .planning/phases/01-foundation-data-layer-caching-auth/01-RESEARCH.md (Pattern 5, lines 332-347)
    .planning/phases/01-foundation-data-layer-caching-auth/01-UI-SPEC.md (Copywriting Contract, UI Considerations)
  </read_first>
  <action>
    Create src/pages/home.py exporting render_home_page() -> None. As the very first statement in the function body, call require_auth() from src.auth.session (per D-04 — no inline auth logic duplicated here, this is the only auth check on the page). Render the exact Empty-state heading "You're in" and the exact Empty-state body text from the Copywriting Contract. Add a "Log Out" button that calls src.auth.session.sign_out() and then st.rerun().

    Create src/app.py as the entrypoint. Import render_login_page from src.pages.login and render_home_page from src.pages.home. Construct st.Page objects: a login page (st.Page(render_login_page, title="Log In", url_path="login")) and a home page (st.Page(render_home_page, title="Home", url_path="home", default=True)). Conditionally build the navigation: if st.session_state.get("logged_in") is falsy, call st.navigation([login_page]) so no gated page is reachable at all (D-03 — hidden, not visible-but-redirecting); otherwise call st.navigation({"Account": [logout-capable home page and any future gated pages], "Home": [home_page]}) — for Phase 1 scope this reduces to st.navigation([home_page]) since Home is the only gated page that exists. Call pg.run() as the final statement.
  </action>
  <acceptance_criteria>
    - render_home_page's first executable statement calls require_auth().
    - home.py contains the exact "You're in" heading string.
    - app.py contains a call to st.navigation and branches on st.session_state.get("logged_in").
    - app.py's syntax is valid Python (parses with ast.parse with no error).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "require_auth" src/pages/home.py &amp;&amp; grep -c "You're in" src/pages/home.py &amp;&amp; grep -c "st.navigation" src/app.py &amp;&amp; python -c "import ast; ast.parse(open('src/app.py').read())"</automated>
  </verify>
  <done>src/pages/home.py is require_auth()-gated and renders the exact placeholder copy plus a working Log Out action; src/app.py conditionally constructs st.navigation so the home page is entirely absent from navigation until login, per D-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the login-to-home flow</name>
  <action>Pause for human verification of the full login/signup/magic-link/logout flow built in Tasks 1-2 before this plan is considered complete.</action>
  <what-built>The full Phase 1 UI surface: login/signup/magic-link page, require_auth()-gated placeholder home page, and the conditional-navigation app entrypoint. All automated tests for the underlying auth/cache logic already passed in Plans 02/03.</what-built>
  <how-to-verify>
    1. Ensure the local Supabase stack from Plan 01 is running: `npx supabase status`.
    2. Run the app locally: `streamlit run src/app.py`.
    3. Confirm only the login page is reachable — no other page appears in the sidebar/nav.
    4. Create an account via the "Create Account" tab with a real-looking test email/password; confirm you land on the Home page immediately with no email-verification step (D-02).
    5. Confirm the Home page shows the "You're in" heading and body text, and a "Log Out" button.
    6. Click "Log Out"; confirm you are returned to the login-only nav state.
    7. Log back in via the "Log In" tab with the same credentials; confirm success.
    8. Try the "Magic Link" tab with the same email; confirm an info message appears (no crash) — actual link click-through is not required for this check since Plan 02's automated test already proved the send path via the local Inbucket instance.
    9. Try submitting the login form with both fields empty; confirm the form does not submit (native validation).
    10. Try an invalid password; confirm the exact "We couldn't verify that email and password..." message appears.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser session ↔ Streamlit process | st.session_state is the only per-user store; untrusted form input crosses here |
| Streamlit process ↔ Supabase Auth/Postgres | JWT-bearing HTTPS calls; get_user() is the server-side validation boundary |
| Streamlit process ↔ Yahoo/yfinance | Untrusted external network response (market data), no auth, potential malformed payload |
| Streamlit process ↔ local SQLite disk cache | Trusted local filesystem, but ticker input must still be parameterized before reaching SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Information Disclosure | src/data/supabase_client.py, src/auth/session.py (cache_resource surface) | critical | mitigate | Owned by Plan 02; this plan's pages call require_auth()/sign_out() only through that module's public functions, never bypassing them |
| T-01-02 | Elevation of Privilege | src/data/supabase_client.py, .streamlit/secrets.toml.example | critical | mitigate | Owned by Plan 01/02 |
| T-01-03 | Tampering | src/data/cache.py (SQLite queries) | high | mitigate | Owned by Plan 03 |
| T-01-04 | Spoofing | src/auth/session.py::require_auth() | high | mitigate | Owned by Plan 02; home.py calls it as the sole gate, per D-04 (Task 2) |
| T-01-05 | Denial of Service | src/data/cache.py (yfinance fetch) | medium | mitigate | Owned by Plan 03 |
| T-01-06 | Spoofing / DoS | Supabase Auth password sign-in (brute force) | medium | transfer | Owned by Plan 02 |
| T-01-07 | Information Disclosure | data/price_cache.db (local disk cache) | low | accept | Owned by Plan 03 |
| T-01-SC | Tampering | pip installs | high | mitigate | No new package introduced in this plan beyond Plan 01's audited set |
</threat_model>

<verification>
python -c "import ast; ast.parse(open('src/app.py').read())" and the equivalent parse for login.py/home.py succeed; the grep checks above confirm require_auth()/st.navigation wiring and Copywriting Contract string fidelity; the human checkpoint confirms the visible flow end-to-end.
</verification>

<success_criteria>
A human can sign up, land on Home immediately, log out, log back in, and confirm the login page is the only reachable page when logged out — matching D-01 through D-04 and the UI-SPEC Copywriting Contract exactly.
</success_criteria>

<output>
Create .planning/phases/01-foundation-data-layer-caching-auth/01-04-SUMMARY.md when done
</output>

## Artifacts this phase produces (this plan's contribution)

- render_login_page() — src/pages/login.py
- render_home_page() — src/pages/home.py
- app.py entrypoint with conditional st.navigation — src/app.py
