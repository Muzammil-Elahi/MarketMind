---
phase: 01-foundation-data-layer-caching-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/supabase_client.py
  - src/auth/session.py
  - tests/conftest.py
  - tests/test_auth_flow.py
autonomous: true
requirements: [AUTH-01, AUTH-03]
must_haves:
  truths:
    - "get_supabase_client() is decorated with st.cache_resource and returns a client constructed only from SUPABASE_URL/SUPABASE_ANON_KEY — no token, session, or user identity is ever attached to it."
    - "require_auth() calls auth.get_user(token) — never auth.get_session() — as the sole source of trust for whether a request is authenticated (D-04, resolves Pitfall 1)."
    - "sign_up() with 'Confirm email' disabled at the project level returns a populated session immediately, so a user can log in right after signing up with no separate verification step (D-02)."
    - "Signing up twice with the same email does not create two profiles rows or crash the app — the second sign_up() attempt surfaces a recognizable error/duplicate state rather than silently succeeding as if it were a new account."
    - "There is no 'unconfirmed account' login-blocked state to handle: because email confirmation is disabled project-wide (D-02), sign_in_with_password() succeeds immediately after sign_up() with no separate verification gate."
    - "require_auth() attempts refresh_session() using the stored refresh token before giving up on an expired access token, so a user's login survives a normal page reload rather than forcing re-authentication on every token expiry."
    - "sign_in() updates profiles.last_login for the signed-in user's row via a short-lived per-call authenticated client — never the shared get_supabase_client() cache_resource instance — delivering COVERAGE.md's 'Postgres CRUD on the profiles table' SELECT/UPDATE persistence proof (D-10)."
  artifacts:
    - path: "src/auth/session.py"
      provides: "sign_up, sign_in, sign_in_with_magic_link, require_auth, sign_out"
    - path: "src/data/supabase_client.py"
      provides: "get_supabase_client() — st.cache_resource-shared stateless client"
    - path: "tests/test_auth_flow.py"
      provides: "AUTH-01 behavior proof against the local Supabase stack"
  key_links:
    - from: "src/auth/session.py"
      to: "src/data/supabase_client.py"
      via: "require_auth()/sign_up()/sign_in() all call get_supabase_client() for the connection, then route tokens through st.session_state only"
      pattern: "get_supabase_client\\(\\)"
  prohibitions:
    - "MUST NOT collect any field beyond email and password at signup — no name, phone, or investor-profile field belongs on this form (D-10 scopes the persisted stub to user_id/created_at/last_login only)."
    - "MUST NOT add OAuth/social login provider calls — D-01 restricts Phase 1 auth to email/password and magic link only."
---

<objective>
Build the auth module: a stateless, `st.cache_resource`-shared Supabase client and the central `require_auth()` gate plus sign-up/sign-in/magic-link/sign-out wrappers, proven against the local Supabase stack from Plan 01. This is the phase's highest-risk code (AUTH-03's session-isolation guarantee starts here).

Purpose: D-04 mandates a single central auth-check helper called by every gated page — no per-page inline checks. This plan is where that helper and its supporting client live; Plan 04 (pages) and Plan 05 (isolation/RLS tests) both depend on it.

Output: `src/data/supabase_client.py`, `src/auth/session.py`, `tests/conftest.py` (local-stack fixtures), `tests/test_auth_flow.py` — all AUTH-01 behavior proven with automated tests against the real local Supabase stack, no mocks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer-caching-auth/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer-caching-auth/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer-caching-auth/01-01-SUMMARY.md
</context>

<interfaces>
From `src/config.py` (Plan 01, already on disk):
`get_config(key: str, default: str | None = None) -> str | None` — reads `st.secrets` first, falls back to `os.environ`/`.env`.
`CACHE_TTL_SECONDS = 3600` — not used by this plan, referenced by Plan 03.

From the local Supabase stack (Plan 01, already running): `npx supabase status -o env` prints `API_URL`, `ANON_KEY`, `DB_URL`, `INBUCKET_URL` for the running local stack — this plan's `tests/conftest.py` must shell out to this command (or parse cached output) to configure the test Supabase client, rather than hand-writing local URLs/keys.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stateless cached Supabase client</name>
  <files>src/data/supabase_client.py</files>
  <read_first>
    .planning/phases/01-foundation-data-layer-caching-auth/01-RESEARCH.md (Pattern 2, lines 219-236; Pitfall 2, lines 374-378; Pitfall 5, lines 392-396)
    src/config.py
  </read_first>
  <!-- D-06 (Claude's discretion, resolved here): the client object is st.cache_resource-shared
       since it is stateless; the non-discretionary constraint — token/identity never lives on
       this cached object, only in st.session_state — is enforced by the behavior/action below. -->
  <behavior>
    - D-06 resolved: get_supabase_client() is `st.cache_resource`-shared (stateless connection, not constructed per-call), satisfying CONTEXT.md's deferred-to-implementation choice while honoring the non-discretionary constraint that follows.
    - get_supabase_client() returns a `supabase.Client` instance built from `create_client(get_config("SUPABASE_URL"), get_config("SUPABASE_ANON_KEY"))`.
    - Calling get_supabase_client() twice within the same process returns the identical object (proves `st.cache_resource` sharing is active).
    - The returned client has never had `.auth.sign_in_with_password(...)` or any other authenticating call made on it inside this module — signing in happens only in `src/auth/session.py`, using tokens routed through `st.session_state`, never baked into this cached client.
  </behavior>
  <action>
    D-06 resolved: the client object is `st.cache_resource`-shared (stateless connection), not constructed per-call — CONTEXT.md deferred this choice to implementation time, and this task makes the call. Create `src/data/supabase_client.py`. Import `create_client` and `Client` from `supabase`, and `st` from `streamlit`, and `get_config` from `src.config`. Define `get_supabase_client() -> Client`, decorated with `@st.cache_resource`, that constructs and returns `create_client(get_config("SUPABASE_URL"), get_config("SUPABASE_ANON_KEY"))`. Add a module docstring/comment stating this client is safe to share across all users because it is stateless (anon/publishable key only, no signed-in session attached) — per-user tokens live exclusively in `st.session_state`, set and read by `src/auth/session.py`. Do not call any `.auth.sign_in_*`/`.auth.sign_up` method inside this file.
  </action>
  <acceptance_criteria>
    - `get_supabase_client` is decorated with `st.cache_resource`.
    - The function body references `get_config("SUPABASE_URL")` and `get_config("SUPABASE_ANON_KEY")`.
    - No `.auth.sign_in` or `.auth.sign_up` call appears anywhere in this file.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "st.cache_resource" src/data/supabase_client.py && grep -c "SUPABASE_ANON_KEY" src/data/supabase_client.py && ! grep -Eq "\.auth\.sign_(in|up)" src/data/supabase_client.py</automated>
  </verify>
  <done>`src/data/supabase_client.py` exists, exports a cache_resource-decorated `get_supabase_client()` built from the anon key only, and contains no authenticating call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: require_auth() gate + sign-up/sign-in/magic-link/sign-out wrappers</name>
  <files>src/auth/session.py, tests/conftest.py, tests/test_auth_flow.py</files>
  <read_first>
    .planning/phases/01-foundation-data-layer-caching-auth/01-RESEARCH.md (Pattern 1 lines 193-217, Pattern 4 lines 286-330 — profiles schema/last_login column, Code Examples lines 404-446, Open Question 2 lines 475-478)
    .planning/phases/01-foundation-data-layer-caching-auth/01-CONTEXT.md (D-01, D-02, D-04, D-05, D-10)
    .planning/phases/01-foundation-data-layer-caching-auth/COVERAGE.md (Postgres CRUD on the profiles table row)
    src/data/supabase_client.py
  </read_first>
  <behavior>
    - sign_up(email, password): calls `get_supabase_client().auth.sign_up({"email": email, "password": password})`; on success sets `st.session_state["access_token"]`, `st.session_state["refresh_token"]`, `st.session_state["logged_in"] = True` from `response.session`, and returns the response. With email confirmation disabled at the project level, `response.session` is non-null immediately (D-02) — assert this in the test.
    - sign_up(email, password) called twice with the same email: the second call either raises an `AuthApiError`/returns a response the app treats as "already registered" — assert the app does not silently create a second `profiles` row and does not crash; the caller (later, `pages/login.py`) is expected to surface an error state.
    - sign_in(email, password): calls `sign_in_with_password`; on success sets the same three `st.session_state` keys; on invalid credentials raises `AuthApiError`.
    - sign_in(email, password) on success also updates `profiles.last_login` to the current UTC timestamp for that user's row, via `_touch_last_login(access_token, user_id)` — a short-lived, per-call authenticated client (never the shared `get_supabase_client()` cache_resource instance) issues the UPDATE so the write is scoped to this one request and the shared client stays exactly as stateless as Task 1 requires. Assert `last_login` changes across two sequential `sign_in()` calls for the same user (this is COVERAGE.md's "Postgres CRUD on the profiles table" SELECT/UPDATE persistence proof, D-10).
    - sign_in_with_magic_link(email): calls `sign_in_with_otp({"email": email, "options": {"email_redirect_to": ...}})` and does not raise on a valid email; verify the local Inbucket instance (from `npx supabase status -o env`'s `INBUCKET_URL`) received an email for that address, proving the magic-link send path actually works end-to-end without needing a human to click anything.
    - require_auth(): with no `access_token` in `st.session_state`, halts (does not return a user). With a valid `access_token`, calls `get_supabase_client().auth.get_user(token)` (never `get_session()`) and returns `.user`. With an expired `access_token` but a present `refresh_token`, calls `refresh_session()`, updates `st.session_state` with the new tokens, and retries `get_user()` before giving up. With both tokens invalid/expired, clears `st.session_state` and halts.
    - sign_out(): calls `get_supabase_client().auth.sign_out()` and clears the auth keys from `st.session_state` (this session only — never a shared/global object).
  </behavior>
  <action>
    Create `src/auth/session.py` importing `streamlit as st`, `get_supabase_client` from `src.data.supabase_client`, and the `AuthApiError` exception — verify the exact import path against the installed `supabase`/`supabase_auth` package (RESEARCH.md flags `from supabase_auth.errors import AuthApiError` as `[ASSUMED]`, Open Question 2) by inspecting the installed package's `__init__.py`/module layout at implementation time rather than trusting the research doc blindly.

    Implement `sign_up(email: str, password: str) -> AuthResponse`, `sign_in(email: str, password: str) -> AuthResponse`, `sign_in_with_magic_link(email: str) -> None`, `require_auth() -> User`, and `sign_out() -> None` exactly per the behavior block above. `require_auth()` must call `st.stop()` after halting (missing token, or both refresh and re-verify failed) so no gated page content renders past that point.

    Implement a private `_touch_last_login(access_token: str, user_id: str) -> None` helper: build a fresh, uncached client via `create_client(get_config("SUPABASE_URL"), get_config("SUPABASE_ANON_KEY"))` (import `create_client` directly from `supabase` — never reuse the `get_supabase_client()` cache_resource instance for this), call `.postgrest.auth(access_token)` on that fresh instance to attach the just-authenticated user's JWT for this call only, then execute `.table("profiles").update({"last_login": datetime.now(timezone.utc).isoformat()}).eq("user_id", user_id).execute()`. Let the fresh client go out of scope immediately after — it is never stored in `st.session_state` or any cached/global object, so the shared client stays exactly as stateless as Task 1 requires. Call `_touch_last_login()` from `sign_in()` immediately after a successful `sign_in_with_password()` response, using `response.session.access_token` and `response.user.id`. Do not call this from `sign_up()` (the trigger already creates the row with `last_login` left null — that null-then-populated transition is itself part of the persistence proof) or from `require_auth()` (which runs on every page render/rerun; writing on every rerun would be unnecessary write volume for a value that should only reflect explicit login events).

    Create `tests/conftest.py` with a session-scoped fixture `supabase_env` that shells out to `npx supabase status -o env`, parses `API_URL`/`ANON_KEY`/`DB_URL`/`INBUCKET_URL` from its output, and sets them as `SUPABASE_URL`/`SUPABASE_ANON_KEY` (etc.) in `os.environ` for the test process before any test imports `src.config`/`src.data.supabase_client`. Add a function-scoped fixture `test_user_factory` that calls `sign_up()` with a freshly generated unique email (e.g. `f"test-{uuid4().hex}@example.com"`) and a fixed test password, returning the email/password/response tuple — used by this plan's and Plan 05's tests to create distinct real users against the local stack.

    Create `tests/test_auth_flow.py` covering every bullet in the behavior block above, using `test_user_factory` and direct calls into `src.auth.session`.
  </action>
  <acceptance_criteria>
    - `require_auth` contains a call to `.auth.get_user(` and contains no call to `.auth.get_session(`.
    - `sign_up`, `sign_in`, `sign_in_with_magic_link`, `require_auth`, `sign_out` are all defined at module scope in `src/auth/session.py`.
    - `sign_in` calls `_touch_last_login` (or an equivalent inline UPDATE) so `profiles.last_login` changes on each successful password sign-in, using a fresh non-cached client — never `get_supabase_client()` — for that write.
    - `tests/test_auth_flow.py` passes against the live local Supabase stack (no mocking of `supabase-py` itself).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "def require_auth" src/auth/session.py && grep -c "\.auth\.get_user(" src/auth/session.py && grep -c "last_login" src/auth/session.py && ! grep -Eq "\.auth\.get_session\(" src/auth/session.py && pytest tests/test_auth_flow.py -x -q</automated>
  </verify>
  <done>`src/auth/session.py` exports all five functions with the specified server-verified/token-in-session-state behavior, plus `_touch_last_login` wired into `sign_in()`; `pytest tests/test_auth_flow.py -x -q` passes against the local Supabase stack, proving AUTH-01's signup/login/duplicate-signup/immediate-login behavior and AUTH-02's `last_login` persistence proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser session ↔ Streamlit process | `st.session_state` is the only per-user store; untrusted form input crosses here |
| Streamlit process ↔ Supabase Auth/Postgres | JWT-bearing HTTPS calls; `get_user()` is the server-side validation boundary |
| Streamlit process ↔ Yahoo/yfinance | Untrusted external network response (market data), no auth, potential malformed payload |
| Streamlit process ↔ local SQLite disk cache | Trusted local filesystem, but ticker input must still be parameterized before reaching SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Information Disclosure | `src/data/supabase_client.py`, `src/auth/session.py` (`cache_resource` surface) | critical | mitigate | Token/identity confined to `st.session_state` only; `get_supabase_client()` stays stateless (Task 1); `require_auth`/`sign_out` never write tokens onto the cached client; `_touch_last_login()` builds its own short-lived, uncached client for the RLS-scoped `last_login` UPDATE rather than attaching a token to the shared `get_supabase_client()` instance — verified by the isolation test in Plan 05 |
| T-01-02 | Elevation of Privilege | `src/data/supabase_client.py`, `.streamlit/secrets.toml.example` | critical | mitigate | Client constructed with the anon/publishable key only; no elevated-privilege credential is loaded |
| T-01-03 | Tampering | `src/data/cache.py` (SQLite queries) | high | mitigate | Owned by Plan 03 — parameterized queries only |
| T-01-04 | Spoofing | `src/auth/session.py::require_auth()` | high | mitigate | `require_auth()` calls `auth.get_user(token)` (server-verified), never `get_session()` — Task 2 |
| T-01-05 | Denial of Service | `src/data/cache.py` (yfinance fetch) | medium | mitigate | Owned by Plan 03 |
| T-01-06 | Spoofing / DoS | Supabase Auth password sign-in (brute force) | medium | transfer | Login rate-limiting is owned by Supabase Auth (GoTrue) server-side, not reimplemented here |
| T-01-07 | Information Disclosure | `data/price_cache.db` (local disk cache) | low | accept | Owned by Plan 03 — public market data only |
| T-01-SC | Tampering | pip installs | high | mitigate | No new package introduced in this plan beyond Plan 01's audited set |
</threat_model>

<verification>
`pytest tests/test_auth_flow.py -x -q` passes against the live local Supabase stack; `require_auth()` demonstrably calls `get_user()` and never `get_session()`; `get_supabase_client()` returns the same cached instance across calls with no authenticating method ever invoked on it directly.
</verification>

<success_criteria>
A user can sign up (email/password or magic link) and immediately be considered logged in (D-02); `require_auth()` is the single server-verified gate every later page will call; duplicate signup and expired-token-refresh are both proven, not assumed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer-caching-auth/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `get_supabase_client()` — `src/data/supabase_client.py`
- `sign_up(email, password)`, `sign_in(email, password)`, `sign_in_with_magic_link(email)`, `require_auth()`, `sign_out()` — `src/auth/session.py`
- `_touch_last_login(access_token, user_id)` (private, called from `sign_in()`) — `src/auth/session.py`
- `supabase_env` fixture, `test_user_factory` fixture — `tests/conftest.py`
